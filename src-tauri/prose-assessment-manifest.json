[
  {
    "lessonId": "prerequisite-trace",
    "lessonRevision": "2026-08-07-r1",
    "lessonTitle": "Trace the tools before the model",
    "lessonContext": "Start with the notation; no Python or calculus is assumed\nThis course begins before machine-learning notation. An example is one recorded case, such as one delivery. A feature is a measured input about that case, and a target is the value or category to predict. A class is one possible category, such as positive or negative. Nothing on this page assumes that you already know Python, NumPy, matrix notation, derivatives, or probability.\n\nStart with the notation; no Python or calculus is assumed\nPython reads instructions in order. In `count = 3`, the equals sign stores the value 3 under the variable name count; it does not claim that two algebraic expressions are forever equal. Square brackets make a list, so `[1, 2, 3]` is one ordered collection. `def center(values):` begins a reusable function whose indented lines belong to it. Calling `center([1, 2, 3])` supplies a list, and `return` sends the function's result back to the caller. A `for` loop repeats an indented step for each item.\n\nStart with the notation; no Python or calculus is assumed\nNumPy is a Python tool for arrays, which are ordered tables of numbers. `import numpy as np` gives that tool the short name `np`, and `np.array([1, 2, 3])` constructs an array. A shape reports axis lengths in order. NumPy prints the shape of a one-dimensional array with three positions as `(3,)`; the comma marks a one-item Python tuple and does not add a second axis. A 4 by 3 shape means four rows and three columns. Here each row is one example and each column is one feature. If x and prediction are one-dimensional arrays, `np.column_stack((x, prediction))` makes x column zero and prediction column one, so every row is one plotted `(x, y)` coordinate pair.\n\nStart with the notation; no Python or calculus is assumed\nArithmetic notation can be read as instructions too. In `u = 2x + 1`, `2x` means 2 times x; calculate that product and then add 1. In `y = u^2`, the superscript 2 means multiply u by itself. A slope says how much an output changes when an input rises by one. A derivative is the slope at one particular input. For this page, accept two supplied rules: the slope of `u^2` with respect to u is `2u`, and the slope of `2x + 1` with respect to x is 2. The chain rule multiplies those adjacent slopes.\n\nStart with the notation; no Python or calculus is assumed\nA probability can begin as a count divided by a total count. If 80 of 100 target labels are negative, the observed negative fraction is 80/100 = 0.80 = 80 percent. A majority-class baseline always predicts the most frequent class, so it is correct on those 80 negative cases and wrong on the 20 positive cases. It uses no features and gives a simple comparison floor for any model evaluated on comparable cases.\n\nTerms you need · Program state\nThe current values held by variables at a specific point while a program runs.\n\nTerms you need · Variable and assignment\nA variable is a name for a stored value; Python's equals sign assigns the value on its right to the name on its left.\n\nTerms you need · Function call and return\nA function call supplies inputs to reusable instructions, and return sends the computed result back to the caller.\n\nTerms you need · Python list\nAn ordered collection written between square brackets, such as three numeric values written as [1, 2, 3].\n\nTerms you need · NumPy array and shape\nAn ordered table of values whose shape lists each axis length. A one-dimensional array with three positions has shape (3,), where the comma marks a one-item tuple.\n\nTerms you need · Coordinate pair\nTwo ordered values written as (x, y), where x gives horizontal position and y gives vertical position.\n\nTerms you need · Derivative and chain rule\nA derivative is a local slope; the chain rule multiplies adjacent local slopes when one calculation feeds another.\n\nTerms you need · Majority-class baseline\nThe accuracy of a rule that always predicts the most frequent target category and uses no features.\n\nRead one complete trace from symbols to results\nA batch contains four examples with three features each. A separate plotting table contains three x-y points. The function uses u = 2x + 1 and y = u squared at x = 1. A dataset with two target categories has 80 negative and 20 positive targets.\n\nRead one complete trace from symbols to results · Read the Python symbols\n`values = [1, 2, 3]` stores one list under the name values. A function call such as `sum(values)` produces 6, `len(values)` produces 3, and division gives the average 2. A function can return a new list after subtracting that average from each item.\n\nRead one complete trace from symbols to results · Write the batch shape\nFour examples form the row axis and three features form the column axis, so the batch shape is 4 by 3.\n\nRead one complete trace from symbols to results · Trace scores and plot columns\nMultiplying each four-row example by three weights combines its three feature values and leaves four scores. Separately, `column_stack((x, prediction))` makes a 3 by 2 plotting table: three coordinate-pair rows, with x in column zero and the matching prediction in column one.\n\nRead one complete trace from symbols to results · Evaluate the composed function\nAt x = 1, the inner value is u = 2(1) + 1 = 3. The outer derivative is 2u = 6, and the inner derivative is 2.\n\nRead one complete trace from symbols to results · Multiply the local derivatives\nThe chain rule gives dy/dx = (dy/du)(du/dx) = 6 times 2 = 12. Each factor belongs to one adjacent calculation.\n\nRead one complete trace from symbols to results · Compute the baseline\nThere are 100 targets in total. Always predicting the negative class is correct for 80 of them, so the majority-class baseline is 80/100 = 0.80, or 80 percent.\n\nRead one complete trace from symbols to results\nA trustworthy result exposes which axis remains, which local slopes multiply, and which count produced a probability.\n\nCommon confusions\nShape is ordered. Under the lesson's row-by-example convention, the first axis has four examples and the second has three features.\n\nCommon confusions\nThe outer rate describes change with respect to the intermediate value u. Multiply it by the inner rate to obtain change with respect to x.\n\nCommon confusions\nThe majority rule ignores every feature. It is a comparison floor created only by the 80-to-20 target counts.\n\nBefore you predict\nRead Python assignment, lists, function calls, loops, and return as explicit state-changing instructions.\n\nBefore you predict\nTrack array axes in order and state which axis an operation combines or preserves.\n\nBefore you predict\nFor a composition, evaluate the intermediate value and multiply the adjacent local derivatives.\n\nBefore you predict\nDerive a majority baseline from class counts before judging a model's accuracy.\n\nStart here: this lesson supplies the prerequisites\nMany introductory machine-learning courses require Python, arrays, algebra, statistics, and sometimes calculus before the first lesson. Trace does not send you elsewhere for that prerequisite: the notation guide above and the worked specimen on this page teach the exact subset used here.\nIn the code lab, you are not expected to invent a program. Read the supplied comments and working example, predict the named values, run it, and repair one demonstrated array operation. Later lessons repeat this inspect-before-modify pattern.\nA final number is easier to trust when its intermediate state remains visible. Keep dimensions beside arrays, name each adjacent slope, and write the count behind every probability.\n\nShapes describe which axes can interact\nA batch X with shape 4 by 3 contains four examples and three features per example. Multiplying X by a weight vector with three entries combines the feature axis and leaves one score per example, so the result has shape 4.\nThe operation is valid because the three feature positions align with the three weights. The batch axis is not summed away.\nIn NumPy, shape reports axis lengths in order. A one-dimensional array with three positions prints as (3,): the comma belongs to Python's one-item tuple notation and does not mean there is a second axis. Given one-dimensional x and prediction arrays, np.column_stack((x, prediction)) makes each input a column, producing one (x, prediction) row per observation. A plotting table with shape 3 by 2 therefore has three points; transposing it to 2 by 3 swaps the meaning of rows and columns.\n\nA composed change has two local factors\nLet u = 2x + 1 and y = u squared. At x = 1, u = 3. The outer derivative dy/du is 2u = 6 and the inner derivative du/dx is 2.\nThe chain rule multiplies those local rates: dy/dx = 6 times 2 = 12. Each factor answers how one adjacent quantity changes.\n\nA baseline states the performance floor\nClasses are the possible target-label categories. In this binary example, 80 of 100 recorded cases are negative and the other 20 are positive. Always predicting the negative majority is therefore correct 80 percent of the time, and that majority baseline uses no features.\nA model that scores 75 percent on those same kinds of cases has not beaten the simplest available rule, even if 75 percent sounds high in isolation.",
    "activityId": "00-mechanism-explanation",
    "activityPrompt": "Explain why a 4 by 3 batch times three weights produces four scores, why the derivative at x = 1 is 12, and why 80 percent is the relevant baseline.",
    "activityGuidance": "Name the preserved axis, both local derivative factors, and the majority count.",
    "criteria": [
      {
        "id": "00-preserved-batch",
        "label": "preserve four examples while combining three features"
      },
      {
        "id": "00-chain-factors",
        "label": "multiply the outer factor 6 by the inner factor 2"
      },
      {
        "id": "00-majority-count",
        "label": "derive the baseline from 80 of 100 cases"
      }
    ],
    "demonstratedFeedback": "The trace connects each answer to the operation that produced it.",
    "unsupportedFeedback": "Show what is combined, what is preserved, and which two adjacent derivatives multiply."
  },
  {
    "lessonId": "prerequisite-trace",
    "lessonRevision": "2026-08-07-r1",
    "lessonTitle": "Trace the tools before the model",
    "lessonContext": "Start with the notation; no Python or calculus is assumed\nThis course begins before machine-learning notation. An example is one recorded case, such as one delivery. A feature is a measured input about that case, and a target is the value or category to predict. A class is one possible category, such as positive or negative. Nothing on this page assumes that you already know Python, NumPy, matrix notation, derivatives, or probability.\n\nStart with the notation; no Python or calculus is assumed\nPython reads instructions in order. In `count = 3`, the equals sign stores the value 3 under the variable name count; it does not claim that two algebraic expressions are forever equal. Square brackets make a list, so `[1, 2, 3]` is one ordered collection. `def center(values):` begins a reusable function whose indented lines belong to it. Calling `center([1, 2, 3])` supplies a list, and `return` sends the function's result back to the caller. A `for` loop repeats an indented step for each item.\n\nStart with the notation; no Python or calculus is assumed\nNumPy is a Python tool for arrays, which are ordered tables of numbers. `import numpy as np` gives that tool the short name `np`, and `np.array([1, 2, 3])` constructs an array. A shape reports axis lengths in order. NumPy prints the shape of a one-dimensional array with three positions as `(3,)`; the comma marks a one-item Python tuple and does not add a second axis. A 4 by 3 shape means four rows and three columns. Here each row is one example and each column is one feature. If x and prediction are one-dimensional arrays, `np.column_stack((x, prediction))` makes x column zero and prediction column one, so every row is one plotted `(x, y)` coordinate pair.\n\nStart with the notation; no Python or calculus is assumed\nArithmetic notation can be read as instructions too. In `u = 2x + 1`, `2x` means 2 times x; calculate that product and then add 1. In `y = u^2`, the superscript 2 means multiply u by itself. A slope says how much an output changes when an input rises by one. A derivative is the slope at one particular input. For this page, accept two supplied rules: the slope of `u^2` with respect to u is `2u`, and the slope of `2x + 1` with respect to x is 2. The chain rule multiplies those adjacent slopes.\n\nStart with the notation; no Python or calculus is assumed\nA probability can begin as a count divided by a total count. If 80 of 100 target labels are negative, the observed negative fraction is 80/100 = 0.80 = 80 percent. A majority-class baseline always predicts the most frequent class, so it is correct on those 80 negative cases and wrong on the 20 positive cases. It uses no features and gives a simple comparison floor for any model evaluated on comparable cases.\n\nTerms you need · Program state\nThe current values held by variables at a specific point while a program runs.\n\nTerms you need · Variable and assignment\nA variable is a name for a stored value; Python's equals sign assigns the value on its right to the name on its left.\n\nTerms you need · Function call and return\nA function call supplies inputs to reusable instructions, and return sends the computed result back to the caller.\n\nTerms you need · Python list\nAn ordered collection written between square brackets, such as three numeric values written as [1, 2, 3].\n\nTerms you need · NumPy array and shape\nAn ordered table of values whose shape lists each axis length. A one-dimensional array with three positions has shape (3,), where the comma marks a one-item tuple.\n\nTerms you need · Coordinate pair\nTwo ordered values written as (x, y), where x gives horizontal position and y gives vertical position.\n\nTerms you need · Derivative and chain rule\nA derivative is a local slope; the chain rule multiplies adjacent local slopes when one calculation feeds another.\n\nTerms you need · Majority-class baseline\nThe accuracy of a rule that always predicts the most frequent target category and uses no features.\n\nRead one complete trace from symbols to results\nA batch contains four examples with three features each. A separate plotting table contains three x-y points. The function uses u = 2x + 1 and y = u squared at x = 1. A dataset with two target categories has 80 negative and 20 positive targets.\n\nRead one complete trace from symbols to results · Read the Python symbols\n`values = [1, 2, 3]` stores one list under the name values. A function call such as `sum(values)` produces 6, `len(values)` produces 3, and division gives the average 2. A function can return a new list after subtracting that average from each item.\n\nRead one complete trace from symbols to results · Write the batch shape\nFour examples form the row axis and three features form the column axis, so the batch shape is 4 by 3.\n\nRead one complete trace from symbols to results · Trace scores and plot columns\nMultiplying each four-row example by three weights combines its three feature values and leaves four scores. Separately, `column_stack((x, prediction))` makes a 3 by 2 plotting table: three coordinate-pair rows, with x in column zero and the matching prediction in column one.\n\nRead one complete trace from symbols to results · Evaluate the composed function\nAt x = 1, the inner value is u = 2(1) + 1 = 3. The outer derivative is 2u = 6, and the inner derivative is 2.\n\nRead one complete trace from symbols to results · Multiply the local derivatives\nThe chain rule gives dy/dx = (dy/du)(du/dx) = 6 times 2 = 12. Each factor belongs to one adjacent calculation.\n\nRead one complete trace from symbols to results · Compute the baseline\nThere are 100 targets in total. Always predicting the negative class is correct for 80 of them, so the majority-class baseline is 80/100 = 0.80, or 80 percent.\n\nRead one complete trace from symbols to results\nA trustworthy result exposes which axis remains, which local slopes multiply, and which count produced a probability.\n\nCommon confusions\nShape is ordered. Under the lesson's row-by-example convention, the first axis has four examples and the second has three features.\n\nCommon confusions\nThe outer rate describes change with respect to the intermediate value u. Multiply it by the inner rate to obtain change with respect to x.\n\nCommon confusions\nThe majority rule ignores every feature. It is a comparison floor created only by the 80-to-20 target counts.\n\nBefore you predict\nRead Python assignment, lists, function calls, loops, and return as explicit state-changing instructions.\n\nBefore you predict\nTrack array axes in order and state which axis an operation combines or preserves.\n\nBefore you predict\nFor a composition, evaluate the intermediate value and multiply the adjacent local derivatives.\n\nBefore you predict\nDerive a majority baseline from class counts before judging a model's accuracy.\n\nStart here: this lesson supplies the prerequisites\nMany introductory machine-learning courses require Python, arrays, algebra, statistics, and sometimes calculus before the first lesson. Trace does not send you elsewhere for that prerequisite: the notation guide above and the worked specimen on this page teach the exact subset used here.\nIn the code lab, you are not expected to invent a program. Read the supplied comments and working example, predict the named values, run it, and repair one demonstrated array operation. Later lessons repeat this inspect-before-modify pattern.\nA final number is easier to trust when its intermediate state remains visible. Keep dimensions beside arrays, name each adjacent slope, and write the count behind every probability.\n\nShapes describe which axes can interact\nA batch X with shape 4 by 3 contains four examples and three features per example. Multiplying X by a weight vector with three entries combines the feature axis and leaves one score per example, so the result has shape 4.\nThe operation is valid because the three feature positions align with the three weights. The batch axis is not summed away.\nIn NumPy, shape reports axis lengths in order. A one-dimensional array with three positions prints as (3,): the comma belongs to Python's one-item tuple notation and does not mean there is a second axis. Given one-dimensional x and prediction arrays, np.column_stack((x, prediction)) makes each input a column, producing one (x, prediction) row per observation. A plotting table with shape 3 by 2 therefore has three points; transposing it to 2 by 3 swaps the meaning of rows and columns.\n\nA composed change has two local factors\nLet u = 2x + 1 and y = u squared. At x = 1, u = 3. The outer derivative dy/du is 2u = 6 and the inner derivative du/dx is 2.\nThe chain rule multiplies those local rates: dy/dx = 6 times 2 = 12. Each factor answers how one adjacent quantity changes.\n\nA baseline states the performance floor\nClasses are the possible target-label categories. In this binary example, 80 of 100 recorded cases are negative and the other 20 are positive. Always predicting the negative majority is therefore correct 80 percent of the time, and that majority baseline uses no features.\nA model that scores 75 percent on those same kinds of cases has not beaten the simplest available rule, even if 75 percent sounds high in isolation.",
    "activityId": "00-sensor-transfer",
    "activityPrompt": "A sensor system receives 12 windows with 5 measurements each, averages the five measurements in each window into one scalar t, computes z = 3t - 2 and score = z^2, and has 90 normal versus 10 fault records. State the score-vector shape, dscore/dt at t = 2, and the accuracy of an always-normal baseline.",
    "activityGuidance": "Reduce each window to one t, map windows to the preserved axis, trace both derivative factors, and use the observed class counts.",
    "criteria": [
      {
        "id": "00-transfer-shape",
        "label": "produce one score for each of 12 windows"
      },
      {
        "id": "00-transfer-derivative",
        "label": "compute 24 from 2z times 3 at t equals 2"
      },
      {
        "id": "00-transfer-baseline",
        "label": "compute the 90 percent always-normal baseline"
      }
    ],
    "demonstratedFeedback": "You transferred all three traces to new quantities and labels.",
    "unsupportedFeedback": "Do not reuse the earlier numbers. Recompute the preserved axis, z at t = 2, and the new majority count."
  },
  {
    "lessonId": "data-and-baseline",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Data gives each quantity a role",
    "lessonContext": "Give every quantity one job and one time of availability\nSupervised learning uses recorded examples for which an outcome is known. An example is one row describing one case. A feature is an input available for that case, while a target is the outcome the model is asked to predict. The model produces a prediction, which is its estimate of the target. These names are not interchangeable: they describe when a value is available and what role it plays.\n\nGive every quantity one job and one time of availability\nTraining is the stage that uses recorded feature-target pairs to fit a rule. A parameter is an adjustable number stored in that rule, and fitting means choosing parameter values from training evidence. Inference is the later stage that applies the fitted rule to the features of a new case. The new target is not available during inference; it becomes known only after the real outcome occurs. Otherwise, the system would be reading the answer instead of predicting it.\n\nGive every quantity one job and one time of availability\nA baseline is a simple comparison rule that the fitted model should improve upon. For a numeric target, one baseline predicts the mean, which is the sum of the training targets divided by their count. This baseline ignores all features. It matters because a complicated model can produce numbers without extracting useful information. Comparing against an explicit no-feature rule asks whether learning from features actually helped.\n\nTerms you need · Example\nOne recorded case containing feature values and, during training, a known target.\n\nTerms you need · Feature\nAn input value available when the model must make a prediction.\n\nTerms you need · Target\nThe outcome that training observes and inference attempts to predict before it is known.\n\nTerms you need · Parameter\nAn adjustable number stored in the model and fitted from training examples.\n\nTerms you need · Training and inference\nTraining fits parameters from known examples; inference applies the fixed fitted rule to new features.\n\nTerms you need · Mean baseline\nA no-feature rule that predicts the average training target for every case.\n\nPredict delivery time and compare with a no-feature rule\nThree completed deliveries took 8, 10, and 12 minutes. For a new delivery, distance x is 2. A fitted rule multiplies x by 4 and then adds 4, while the actual target will later be observed as 12 minutes.\n\nPredict delivery time and compare with a no-feature rule · Assign the data roles\nDistance is a feature because it is known before delivery. Elapsed minutes is the target because that is the unknown outcome to predict.\n\nPredict delivery time and compare with a no-feature rule · Compute the training mean\nAdd the three known targets: 8 + 10 + 12 = 30. Divide by three examples to obtain the mean 10.\n\nPredict delivery time and compare with a no-feature rule · State the baseline\nThe mean baseline predicts 10 minutes for every new delivery. It does not inspect distance or any other feature.\n\nPredict delivery time and compare with a no-feature rule · Apply the fitted parameters\nThe fitted rule calculates 4 times x plus 4. For x = 2, its prediction is 4(2) + 4 = 12 minutes.\n\nPredict delivery time and compare with a no-feature rule · Separate inference from later evaluation\nInference may read x = 2 and the two stored parameters, but not the unknown target. After 12 minutes is observed, the fitted rule's residual is 12 - 12 = 0, while the baseline's residual is 10 - 12 = -2.\n\nPredict delivery time and compare with a no-feature rule\nThe feature, target, parameters, and prediction have different roles; the baseline reveals whether using fitted feature information improved the result.\n\nCommon confusions\nThe target is the unknown outcome. Training has past targets, but inference for a new case must operate before that case's target is known.\n\nCommon confusions\nFeature values vary across examples. Parameters belong to the fitted rule and change only when a training procedure updates them.\n\nCommon confusions\nIts purpose is comparison. Even a simple mean can expose that a more complex model has failed to gain useful predictive value.\n\nBefore you predict\nFeatures are known inputs, targets are outcomes, and predictions estimate those outcomes.\n\nBefore you predict\nTraining fits model parameters; inference applies the fitted parameters without reading or updating from the new target.\n\nBefore you predict\nA numeric model should be compared with a mean baseline that uses no features.\n\nA recorded row contains evidence, not instructions\nFor a completed delivery, distance and package mass can be features, while elapsed minutes is the target. The target is the outcome the prediction is meant to approximate.\nAt inference time for a new delivery, the features are available but the target is not. If elapsed minutes were already known, there would be nothing to predict.\n\nParameters belong to the reusable rule\nA parameter is an adjustable number carried by the model across examples. Training uses feature-target pairs to choose parameter values.\nAn input changes when the example changes. A parameter changes when training updates the rule.\n\nTraining and inference run different contracts\nDuring training, the system predicts on recorded examples, compares predictions with targets, and updates parameters. During inference, it receives new features and uses the fitted parameters without reading a target or updating the rule.\nThe same model structure can participate in both stages, but only training changes its fitted state.\n\nStart comparison with the mean\nFor training targets 8, 10, and 12 minutes, the mean is 10. A mean baseline predicts 10 for every delivery and ignores all features.\nA learned regression rule earns its complexity only if its evaluation loss improves on that baseline for untouched cases.",
    "activityId": "01-role-explanation",
    "activityPrompt": "Explain why a target may guide a parameter update during training but must be absent during inference, and why the mean prediction is a baseline rather than a learned feature rule.",
    "activityGuidance": "Use availability time, parameter state, and feature use in your explanation.",
    "criteria": [
      {
        "id": "01-target-training",
        "label": "use targets after predictions to evaluate training examples"
      },
      {
        "id": "01-inference-no-target",
        "label": "exclude the unknown target at inference"
      },
      {
        "id": "01-parameter-update",
        "label": "change parameters only during fitting"
      },
      {
        "id": "01-baseline-no-features",
        "label": "state that the mean baseline ignores features"
      }
    ],
    "demonstratedFeedback": "You separated the data roles, the two execution stages, and the comparison floor.",
    "unsupportedFeedback": "Anchor each value to when it is available and state whether it can change the model."
  },
  {
    "lessonId": "data-and-baseline",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Data gives each quantity a role",
    "lessonContext": "Give every quantity one job and one time of availability\nSupervised learning uses recorded examples for which an outcome is known. An example is one row describing one case. A feature is an input available for that case, while a target is the outcome the model is asked to predict. The model produces a prediction, which is its estimate of the target. These names are not interchangeable: they describe when a value is available and what role it plays.\n\nGive every quantity one job and one time of availability\nTraining is the stage that uses recorded feature-target pairs to fit a rule. A parameter is an adjustable number stored in that rule, and fitting means choosing parameter values from training evidence. Inference is the later stage that applies the fitted rule to the features of a new case. The new target is not available during inference; it becomes known only after the real outcome occurs. Otherwise, the system would be reading the answer instead of predicting it.\n\nGive every quantity one job and one time of availability\nA baseline is a simple comparison rule that the fitted model should improve upon. For a numeric target, one baseline predicts the mean, which is the sum of the training targets divided by their count. This baseline ignores all features. It matters because a complicated model can produce numbers without extracting useful information. Comparing against an explicit no-feature rule asks whether learning from features actually helped.\n\nTerms you need · Example\nOne recorded case containing feature values and, during training, a known target.\n\nTerms you need · Feature\nAn input value available when the model must make a prediction.\n\nTerms you need · Target\nThe outcome that training observes and inference attempts to predict before it is known.\n\nTerms you need · Parameter\nAn adjustable number stored in the model and fitted from training examples.\n\nTerms you need · Training and inference\nTraining fits parameters from known examples; inference applies the fixed fitted rule to new features.\n\nTerms you need · Mean baseline\nA no-feature rule that predicts the average training target for every case.\n\nPredict delivery time and compare with a no-feature rule\nThree completed deliveries took 8, 10, and 12 minutes. For a new delivery, distance x is 2. A fitted rule multiplies x by 4 and then adds 4, while the actual target will later be observed as 12 minutes.\n\nPredict delivery time and compare with a no-feature rule · Assign the data roles\nDistance is a feature because it is known before delivery. Elapsed minutes is the target because that is the unknown outcome to predict.\n\nPredict delivery time and compare with a no-feature rule · Compute the training mean\nAdd the three known targets: 8 + 10 + 12 = 30. Divide by three examples to obtain the mean 10.\n\nPredict delivery time and compare with a no-feature rule · State the baseline\nThe mean baseline predicts 10 minutes for every new delivery. It does not inspect distance or any other feature.\n\nPredict delivery time and compare with a no-feature rule · Apply the fitted parameters\nThe fitted rule calculates 4 times x plus 4. For x = 2, its prediction is 4(2) + 4 = 12 minutes.\n\nPredict delivery time and compare with a no-feature rule · Separate inference from later evaluation\nInference may read x = 2 and the two stored parameters, but not the unknown target. After 12 minutes is observed, the fitted rule's residual is 12 - 12 = 0, while the baseline's residual is 10 - 12 = -2.\n\nPredict delivery time and compare with a no-feature rule\nThe feature, target, parameters, and prediction have different roles; the baseline reveals whether using fitted feature information improved the result.\n\nCommon confusions\nThe target is the unknown outcome. Training has past targets, but inference for a new case must operate before that case's target is known.\n\nCommon confusions\nFeature values vary across examples. Parameters belong to the fitted rule and change only when a training procedure updates them.\n\nCommon confusions\nIts purpose is comparison. Even a simple mean can expose that a more complex model has failed to gain useful predictive value.\n\nBefore you predict\nFeatures are known inputs, targets are outcomes, and predictions estimate those outcomes.\n\nBefore you predict\nTraining fits model parameters; inference applies the fitted parameters without reading or updating from the new target.\n\nBefore you predict\nA numeric model should be compared with a mean baseline that uses no features.\n\nA recorded row contains evidence, not instructions\nFor a completed delivery, distance and package mass can be features, while elapsed minutes is the target. The target is the outcome the prediction is meant to approximate.\nAt inference time for a new delivery, the features are available but the target is not. If elapsed minutes were already known, there would be nothing to predict.\n\nParameters belong to the reusable rule\nA parameter is an adjustable number carried by the model across examples. Training uses feature-target pairs to choose parameter values.\nAn input changes when the example changes. A parameter changes when training updates the rule.\n\nTraining and inference run different contracts\nDuring training, the system predicts on recorded examples, compares predictions with targets, and updates parameters. During inference, it receives new features and uses the fitted parameters without reading a target or updating the rule.\nThe same model structure can participate in both stages, but only training changes its fitted state.\n\nStart comparison with the mean\nFor training targets 8, 10, and 12 minutes, the mean is 10. A mean baseline predicts 10 for every delivery and ignores all features.\nA learned regression rule earns its complexity only if its evaluation loss improves on that baseline for untouched cases.",
    "activityId": "01-energy-transfer",
    "activityPrompt": "A building model predicts tomorrow's electricity use from forecast temperature and occupancy. Historical use values average 240 kWh. Identify features, target, what training changes, what inference may read, and the baseline prediction.",
    "activityGuidance": "Map the delivery roles to this new domain without changing their timing.",
    "criteria": [
      {
        "id": "01-transfer-features",
        "label": "identify forecast temperature and occupancy as features"
      },
      {
        "id": "01-transfer-target",
        "label": "identify actual electricity use as the target"
      },
      {
        "id": "01-transfer-stages",
        "label": "fit parameters on history but do not update from tomorrow's unknown use"
      },
      {
        "id": "01-transfer-baseline",
        "label": "state the 240 kWh mean baseline"
      }
    ],
    "demonstratedFeedback": "The same role and timing contract now holds for energy demand.",
    "unsupportedFeedback": "Name what is known before tomorrow, what is observed afterward, and what the no-feature rule predicts."
  },
  {
    "lessonId": "linear-model",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Two parameters draw one line",
    "lessonContext": "Read a linear prediction as two visible contributions\nA one-feature linear model predicts a number from one numeric input. The input is written as x. The prediction is written as y_hat, read as 'y-hat,' to distinguish the model's estimate from the observed target y. The rule is y_hat = wx + b: multiply x by the weight w, then add the bias b. Both w and b are parameters fitted during training.\n\nRead a linear prediction as two visible contributions\nThe weight controls how much the prediction changes for a one-unit change in x. This rate is the line's slope. If w = 3, then moving x upward by one changes y_hat by 3 while b remains fixed. The bias is the prediction when x = 0 because wx then equals zero. On a graph, that value is the intercept, the point where the line crosses the vertical prediction axis.\n\nRead a linear prediction as two visible contributions\nThis separation matters because parameter changes do not have identical effects. Adding one to b raises every prediction by one. Adding one to w changes a particular prediction by x, so the effect is positive for positive x, zero at x = 0, and negative for negative x. Tracing multiplication before addition prevents the mistaken shortcut that a larger weight always raises every output.\n\nTerms you need · Linear model\nA prediction rule whose graph is a straight line for one feature.\n\nTerms you need · y_hat\nThe model's predicted value, kept distinct from the observed target y.\n\nTerms you need · Weight\nThe parameter w multiplied by x; it gives the prediction change per one unit of input.\n\nTerms you need · Bias\nThe parameter b added after multiplication; it equals the prediction at x = 0.\n\nTerms you need · Slope\nThe change in the vertical prediction for a one-unit increase in the horizontal input.\n\nTerms you need · Intercept\nThe vertical value where the line crosses x = 0, equal to the bias.\n\nTrace a signed temperature input through one line\nA thermostat predicts energy change with y_hat = 1.5x - 2. Here x is degrees relative to a reference temperature, so x can be positive, zero, or negative.\n\nTrace a signed temperature input through one line · Identify both parameters\nThe weight is w = 1.5, meaning 1.5 units of predicted energy change per degree. The bias is b = -2.\n\nTrace a signed temperature input through one line · Evaluate the reference point\nAt x = 0, the weighted contribution is 1.5(0) = 0. Therefore y_hat = 0 - 2 = -2, which is the intercept.\n\nTrace a signed temperature input through one line · Evaluate a positive input\nAt x = 4, multiplication contributes 1.5(4) = 6. Adding the bias gives y_hat = 6 - 2 = 4.\n\nTrace a signed temperature input through one line · Evaluate a negative input\nAt x = -4, multiplication contributes 1.5(-4) = -6. Adding the bias gives y_hat = -6 - 2 = -8.\n\nTrace a signed temperature input through one line · Change only the weight\nIf w rises from 1.5 to 2 while x = -4 and b = -2 stay fixed, the prediction becomes 2(-4) - 2 = -10. The larger weight lowers this output because x is negative.\n\nTrace a signed temperature input through one line\nThe bias shifts every prediction equally, while the weight's effect depends on both its change and the signed input value.\n\nCommon confusions\nThe weight is only one parameter. A prediction also depends on the current input and the bias through y_hat = wx + b.\n\nCommon confusions\nChanging b shifts the entire line vertically without changing its slope. Changing w alters the slope.\n\nCommon confusions\nThe prediction change caused by a weight change is multiplied by x. For a negative x, increasing w lowers y_hat.\n\nBefore you predict\nCompute y_hat by multiplying x by w and then adding b.\n\nBefore you predict\nInterpret w as slope and b as the prediction at x = 0.\n\nBefore you predict\nUse the sign of x before predicting how a weight change will move the output.\n\nThe complete model fits on one line\nA one-feature linear model predicts y_hat = wx + b. The feature x belongs to the case; weight w and bias b belong to the fitted model.\nThe formula is the prediction mechanism. Training may later choose w and b, but inference only evaluates the formula with their current values.\n\nBias sets the prediction at zero\nWith w = 3 and b = 4, an input x = 0 produces y_hat = 4. Changing b shifts every prediction by the same amount.\nOn a graph, b is where the line crosses the vertical prediction axis.\n\nWeight sets change per input unit\nWith w = 3, increasing x by one increases y_hat by three while b stays fixed. The weight is therefore the line's slope.\nAt x = 5, w = 3 and b = 4 give y_hat = 19. The multiplication contributes 15 and the bias contributes 4.\n\nThe sign of x matters\nIncreasing w raises y_hat when x is positive, changes nothing at x = 0, and lowers y_hat when x is negative. A weight does not have one output direction independent of the input.\nThis boundary condition prevents the shortcut that a larger weight always means a larger prediction.",
    "activityId": "02-parameter-explanation",
    "activityPrompt": "Explain how w and b affect y_hat = wx + b, including what happens at x = 0 and why increasing w can lower a prediction for negative x.",
    "activityGuidance": "Connect each parameter to a geometric and arithmetic effect, then state the sign boundary.",
    "criteria": [
      {
        "id": "02-weight-rate",
        "label": "identify weight as change in prediction per input unit"
      },
      {
        "id": "02-bias-zero",
        "label": "identify bias as the prediction at x equals zero"
      },
      {
        "id": "02-negative-boundary",
        "label": "explain the reversed effect at negative x"
      }
    ],
    "demonstratedFeedback": "You linked both parameters to the equation and stated the input-sign boundary.",
    "unsupportedFeedback": "State which parameter is multiplied by x, which is added, and test the rule at x = 0 and x below zero."
  },
  {
    "lessonId": "linear-model",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Two parameters draw one line",
    "lessonContext": "Read a linear prediction as two visible contributions\nA one-feature linear model predicts a number from one numeric input. The input is written as x. The prediction is written as y_hat, read as 'y-hat,' to distinguish the model's estimate from the observed target y. The rule is y_hat = wx + b: multiply x by the weight w, then add the bias b. Both w and b are parameters fitted during training.\n\nRead a linear prediction as two visible contributions\nThe weight controls how much the prediction changes for a one-unit change in x. This rate is the line's slope. If w = 3, then moving x upward by one changes y_hat by 3 while b remains fixed. The bias is the prediction when x = 0 because wx then equals zero. On a graph, that value is the intercept, the point where the line crosses the vertical prediction axis.\n\nRead a linear prediction as two visible contributions\nThis separation matters because parameter changes do not have identical effects. Adding one to b raises every prediction by one. Adding one to w changes a particular prediction by x, so the effect is positive for positive x, zero at x = 0, and negative for negative x. Tracing multiplication before addition prevents the mistaken shortcut that a larger weight always raises every output.\n\nTerms you need · Linear model\nA prediction rule whose graph is a straight line for one feature.\n\nTerms you need · y_hat\nThe model's predicted value, kept distinct from the observed target y.\n\nTerms you need · Weight\nThe parameter w multiplied by x; it gives the prediction change per one unit of input.\n\nTerms you need · Bias\nThe parameter b added after multiplication; it equals the prediction at x = 0.\n\nTerms you need · Slope\nThe change in the vertical prediction for a one-unit increase in the horizontal input.\n\nTerms you need · Intercept\nThe vertical value where the line crosses x = 0, equal to the bias.\n\nTrace a signed temperature input through one line\nA thermostat predicts energy change with y_hat = 1.5x - 2. Here x is degrees relative to a reference temperature, so x can be positive, zero, or negative.\n\nTrace a signed temperature input through one line · Identify both parameters\nThe weight is w = 1.5, meaning 1.5 units of predicted energy change per degree. The bias is b = -2.\n\nTrace a signed temperature input through one line · Evaluate the reference point\nAt x = 0, the weighted contribution is 1.5(0) = 0. Therefore y_hat = 0 - 2 = -2, which is the intercept.\n\nTrace a signed temperature input through one line · Evaluate a positive input\nAt x = 4, multiplication contributes 1.5(4) = 6. Adding the bias gives y_hat = 6 - 2 = 4.\n\nTrace a signed temperature input through one line · Evaluate a negative input\nAt x = -4, multiplication contributes 1.5(-4) = -6. Adding the bias gives y_hat = -6 - 2 = -8.\n\nTrace a signed temperature input through one line · Change only the weight\nIf w rises from 1.5 to 2 while x = -4 and b = -2 stay fixed, the prediction becomes 2(-4) - 2 = -10. The larger weight lowers this output because x is negative.\n\nTrace a signed temperature input through one line\nThe bias shifts every prediction equally, while the weight's effect depends on both its change and the signed input value.\n\nCommon confusions\nThe weight is only one parameter. A prediction also depends on the current input and the bias through y_hat = wx + b.\n\nCommon confusions\nChanging b shifts the entire line vertically without changing its slope. Changing w alters the slope.\n\nCommon confusions\nThe prediction change caused by a weight change is multiplied by x. For a negative x, increasing w lowers y_hat.\n\nBefore you predict\nCompute y_hat by multiplying x by w and then adding b.\n\nBefore you predict\nInterpret w as slope and b as the prediction at x = 0.\n\nBefore you predict\nUse the sign of x before predicting how a weight change will move the output.\n\nThe complete model fits on one line\nA one-feature linear model predicts y_hat = wx + b. The feature x belongs to the case; weight w and bias b belong to the fitted model.\nThe formula is the prediction mechanism. Training may later choose w and b, but inference only evaluates the formula with their current values.\n\nBias sets the prediction at zero\nWith w = 3 and b = 4, an input x = 0 produces y_hat = 4. Changing b shifts every prediction by the same amount.\nOn a graph, b is where the line crosses the vertical prediction axis.\n\nWeight sets change per input unit\nWith w = 3, increasing x by one increases y_hat by three while b stays fixed. The weight is therefore the line's slope.\nAt x = 5, w = 3 and b = 4 give y_hat = 19. The multiplication contributes 15 and the bias contributes 4.\n\nThe sign of x matters\nIncreasing w raises y_hat when x is positive, changes nothing at x = 0, and lowers y_hat when x is negative. A weight does not have one output direction independent of the input.\nThis boundary condition prevents the shortcut that a larger weight always means a larger prediction.",
    "activityId": "02-thermostat-transfer",
    "activityPrompt": "A delivery system predicts minutes relative to schedule as y_hat = -0.8x + 6, where x is hours relative to noon. Interpret the weight and bias, then compute predictions for x = 0 and x = -5 and explain why the two negative factors make the weighted contribution positive.",
    "activityGuidance": "Map slope and intercept to this signed time input, substitute both x values, and keep multiplication separate from the bias.",
    "criteria": [
      {
        "id": "02-transfer-weight",
        "label": "interpret minus 0.8 as predicted minutes per hour"
      },
      {
        "id": "02-transfer-bias",
        "label": "compute 6 at noon"
      },
      {
        "id": "02-transfer-negative",
        "label": "compute 10 at five hours before noon"
      }
    ],
    "demonstratedFeedback": "The line mechanism transfers to a signed time feature with new parameters.",
    "unsupportedFeedback": "Substitute each x explicitly before interpreting the direction."
  },
  {
    "lessonId": "loss-landscape",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Loss turns many residuals into a surface",
    "lessonContext": "Build one loss value from every prediction error\nA model needs a numeric way to compare its predictions with known targets. For one example, a residual is prediction minus target. A negative residual means the prediction is below the target; a positive residual means it is above. The residual's magnitude is the size of the miss in target units. Keeping the sign at this stage makes the direction of each error visible.\n\nBuild one loss value from every prediction error\nA loss combines errors into a quantity that a training procedure can compare. Mean squared error, abbreviated MSE, first squares every residual, then adds those squared values, and finally divides by the number of examples. Squaring makes every contribution nonnegative, so misses on opposite sides cannot cancel. It also gives larger residuals disproportionately larger contributions: doubling a residual multiplies its square by four.\n\nBuild one loss value from every prediction error\nFor a fixed dataset and fixed MSE definition, each candidate weight and bias produces one loss value. A loss landscape is the mapping from those parameter choices to their losses. A point on this landscape is not a separate observation; it summarizes all row-level squared errors for one parameter setting. This matters because model improvement means moving to parameters with lower loss, not merely making one selected prediction look better.\n\nTerms you need · Residual\nPrediction minus target for one example; its sign gives direction and its magnitude gives miss size.\n\nTerms you need · Squared error\nA residual multiplied by itself, producing a nonnegative contribution to loss.\n\nTerms you need · Mean squared error\nThe sum of all squared residuals divided by the number of examples.\n\nTerms you need · Loss\nA numeric summary used to compare how well parameter choices match known targets.\n\nTerms you need · Loss landscape\nThe loss associated with every considered parameter position for fixed data and a fixed loss rule.\n\nTerms you need · Outlier\nAn observation with a value or residual far from most others; under MSE, a large residual can contribute strongly.\n\nConstruct two points on a weight-to-MSE landscape\nThree fixed examples are (x = 0, target = 1), (x = 1, target = 3), and (x = 2, target = 5). The model is y_hat = wx + 1, so the bias stays fixed at 1 while the candidate weight changes.\n\nConstruct two points on a weight-to-MSE landscape · Predict with weight 1\nFor x values 0, 1, and 2, the predictions are 1, 2, and 3 because y_hat = 1x + 1.\n\nConstruct two points on a weight-to-MSE landscape · Compute signed residuals\nSubtract each target from its prediction: 1 - 1 = 0, 2 - 3 = -1, and 3 - 5 = -2.\n\nConstruct two points on a weight-to-MSE landscape · Square every residual\nThe squared errors are 0 squared = 0, (-1) squared = 1, and (-2) squared = 4. The negative signs no longer permit cancellation.\n\nConstruct two points on a weight-to-MSE landscape · Average the squares\nAdd 0 + 1 + 4 = 5 and divide by three examples. The MSE is 5/3, approximately 1.667, at weight 1.\n\nConstruct two points on a weight-to-MSE landscape · Evaluate weight 2\nWith y_hat = 2x + 1, the predictions are 1, 3, and 5. Every residual and squared error is zero, so MSE is zero.\n\nConstruct two points on a weight-to-MSE landscape · Place both landscape points\nThe fixed data and bias therefore map weight 1 to MSE 1.667 and weight 2 to MSE 0. These are two points on the same weight-to-loss landscape.\n\nConstruct two points on a weight-to-MSE landscape\nEvery landscape point must be reconstructed through predictions, residuals, squared errors, and their mean.\n\nCommon confusions\nMSE squares each residual first. Averaging signed residuals first lets positive and negative misses cancel.\n\nCommon confusions\nThe sign gives direction, not quality. Their magnitudes are 4 and 2, and their squared contributions are 16 and 4.\n\nCommon confusions\nMSE reveals strong influence from a large residual. Deletion requires separate evidence about measurement, scope, or the error costs being modeled.\n\nBefore you predict\nCompute each residual as prediction minus target and interpret its sign before squaring.\n\nBefore you predict\nMSE squares every residual and then averages, preventing cancellation and emphasizing large misses.\n\nBefore you predict\nA parameter setting identifies one loss-landscape point only after all examples contribute.\n\nStart with one signed miss\nFor one example, residual = prediction - target. A residual of -3 means the prediction is three units below the target; +3 means it is three units above.\nThe sign preserves direction. Its magnitude preserves the size of the miss in target units.\n\nSquare first, then average\nResiduals -1 and +3 would average to 1, hiding part of the error through cancellation. Their squared values are 1 and 9, so MSE is (1 + 9) / 2 = 5.\nSquaring makes every contribution nonnegative and gives a residual twice as large four times the squared contribution.\n\nA landscape indexes loss by parameters\nHold the examples and loss definition fixed. For each candidate weight and bias, compute all predictions and one MSE. Those parameter-to-loss values form a loss landscape.\nThe landscape is not physical terrain. It is a graph of the objective produced by a particular dataset, model, and loss.\n\nA large residual can dominate MSE\nChanging one target so its residual grows from 2 to 10 changes its squared contribution from 4 to 100. The row count is unchanged, but that example now has much more influence on MSE.\nThis is a property of the chosen loss, not proof that the row should be deleted. First investigate measurement, scope, and the cost represented by large misses.",
    "activityId": "03-loss-explanation",
    "activityPrompt": "Explain how fixed examples and one candidate weight produce a point on the MSE landscape, and why a large residual can dominate that point without canceling against an opposite residual.",
    "activityGuidance": "Trace prediction to residual to square to mean, then connect the mean to the parameter position.",
    "criteria": [
      {
        "id": "03-residual-chain",
        "label": "subtract target from prediction for each row"
      },
      {
        "id": "03-square-mean",
        "label": "square every residual before averaging"
      },
      {
        "id": "03-no-cancel",
        "label": "explain why opposite signs cannot cancel"
      },
      {
        "id": "03-parameter-index",
        "label": "associate the resulting MSE with the candidate parameters"
      }
    ],
    "demonstratedFeedback": "You reconstructed one landscape point from row-level errors and the loss definition.",
    "unsupportedFeedback": "Do not jump from parameter to loss. Name each intermediate quantity and operation."
  },
  {
    "lessonId": "loss-landscape",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Loss turns many residuals into a surface",
    "lessonContext": "Build one loss value from every prediction error\nA model needs a numeric way to compare its predictions with known targets. For one example, a residual is prediction minus target. A negative residual means the prediction is below the target; a positive residual means it is above. The residual's magnitude is the size of the miss in target units. Keeping the sign at this stage makes the direction of each error visible.\n\nBuild one loss value from every prediction error\nA loss combines errors into a quantity that a training procedure can compare. Mean squared error, abbreviated MSE, first squares every residual, then adds those squared values, and finally divides by the number of examples. Squaring makes every contribution nonnegative, so misses on opposite sides cannot cancel. It also gives larger residuals disproportionately larger contributions: doubling a residual multiplies its square by four.\n\nBuild one loss value from every prediction error\nFor a fixed dataset and fixed MSE definition, each candidate weight and bias produces one loss value. A loss landscape is the mapping from those parameter choices to their losses. A point on this landscape is not a separate observation; it summarizes all row-level squared errors for one parameter setting. This matters because model improvement means moving to parameters with lower loss, not merely making one selected prediction look better.\n\nTerms you need · Residual\nPrediction minus target for one example; its sign gives direction and its magnitude gives miss size.\n\nTerms you need · Squared error\nA residual multiplied by itself, producing a nonnegative contribution to loss.\n\nTerms you need · Mean squared error\nThe sum of all squared residuals divided by the number of examples.\n\nTerms you need · Loss\nA numeric summary used to compare how well parameter choices match known targets.\n\nTerms you need · Loss landscape\nThe loss associated with every considered parameter position for fixed data and a fixed loss rule.\n\nTerms you need · Outlier\nAn observation with a value or residual far from most others; under MSE, a large residual can contribute strongly.\n\nConstruct two points on a weight-to-MSE landscape\nThree fixed examples are (x = 0, target = 1), (x = 1, target = 3), and (x = 2, target = 5). The model is y_hat = wx + 1, so the bias stays fixed at 1 while the candidate weight changes.\n\nConstruct two points on a weight-to-MSE landscape · Predict with weight 1\nFor x values 0, 1, and 2, the predictions are 1, 2, and 3 because y_hat = 1x + 1.\n\nConstruct two points on a weight-to-MSE landscape · Compute signed residuals\nSubtract each target from its prediction: 1 - 1 = 0, 2 - 3 = -1, and 3 - 5 = -2.\n\nConstruct two points on a weight-to-MSE landscape · Square every residual\nThe squared errors are 0 squared = 0, (-1) squared = 1, and (-2) squared = 4. The negative signs no longer permit cancellation.\n\nConstruct two points on a weight-to-MSE landscape · Average the squares\nAdd 0 + 1 + 4 = 5 and divide by three examples. The MSE is 5/3, approximately 1.667, at weight 1.\n\nConstruct two points on a weight-to-MSE landscape · Evaluate weight 2\nWith y_hat = 2x + 1, the predictions are 1, 3, and 5. Every residual and squared error is zero, so MSE is zero.\n\nConstruct two points on a weight-to-MSE landscape · Place both landscape points\nThe fixed data and bias therefore map weight 1 to MSE 1.667 and weight 2 to MSE 0. These are two points on the same weight-to-loss landscape.\n\nConstruct two points on a weight-to-MSE landscape\nEvery landscape point must be reconstructed through predictions, residuals, squared errors, and their mean.\n\nCommon confusions\nMSE squares each residual first. Averaging signed residuals first lets positive and negative misses cancel.\n\nCommon confusions\nThe sign gives direction, not quality. Their magnitudes are 4 and 2, and their squared contributions are 16 and 4.\n\nCommon confusions\nMSE reveals strong influence from a large residual. Deletion requires separate evidence about measurement, scope, or the error costs being modeled.\n\nBefore you predict\nCompute each residual as prediction minus target and interpret its sign before squaring.\n\nBefore you predict\nMSE squares every residual and then averages, preventing cancellation and emphasizing large misses.\n\nBefore you predict\nA parameter setting identifies one loss-landscape point only after all examples contribute.\n\nStart with one signed miss\nFor one example, residual = prediction - target. A residual of -3 means the prediction is three units below the target; +3 means it is three units above.\nThe sign preserves direction. Its magnitude preserves the size of the miss in target units.\n\nSquare first, then average\nResiduals -1 and +3 would average to 1, hiding part of the error through cancellation. Their squared values are 1 and 9, so MSE is (1 + 9) / 2 = 5.\nSquaring makes every contribution nonnegative and gives a residual twice as large four times the squared contribution.\n\nA landscape indexes loss by parameters\nHold the examples and loss definition fixed. For each candidate weight and bias, compute all predictions and one MSE. Those parameter-to-loss values form a loss landscape.\nThe landscape is not physical terrain. It is a graph of the objective produced by a particular dataset, model, and loss.\n\nA large residual can dominate MSE\nChanging one target so its residual grows from 2 to 10 changes its squared contribution from 4 to 100. The row count is unchanged, but that example now has much more influence on MSE.\nThis is a property of the chosen loss, not proof that the row should be deleted. First investigate measurement, scope, and the cost represented by large misses.",
    "activityId": "03-calibration-transfer",
    "activityPrompt": "A scale calibration model predicts grams from voltage. Three residuals are -1, +1, and +8 grams. A calibration audit then corrects the third recorded target, changing that residual to +2 at the same parameter setting while the other two rows stay fixed. Compute both MSE values, interpret the +8 sign, and explain why the corrected target defines a new dataset and therefore a new loss landscape.",
    "activityGuidance": "Use the new units, show both MSE calculations, and distinguish a new dataset and landscape from movement within one fixed landscape.",
    "criteria": [
      {
        "id": "03-transfer-mse",
        "label": "compute the original MSE as 22"
      },
      {
        "id": "03-transfer-sign",
        "label": "interpret plus 8 as prediction above target"
      },
      {
        "id": "03-transfer-corrected",
        "label": "compute corrected MSE as 2 and identify a new dataset and landscape"
      }
    ],
    "demonstratedFeedback": "You distinguished a lower MSE at the same parameters on a corrected dataset from movement within the original landscape.",
    "unsupportedFeedback": "Square all three residuals before and after the correction, divide each sum by three, and name why changing a target changes the dataset-defined landscape."
  },
  {
    "lessonId": "gradient-descent",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Gradient descent follows repeated local measurements",
    "lessonContext": "Turn a local loss slope into a repeated update\nTraining seeks parameter values with lower loss. A gradient is a local slope: for one parameter, it measures how loss changes for a small increase in that parameter at its current value. A positive gradient means a small increase is locally uphill, while a negative gradient means a small increase is locally downhill. The word local is essential because the measurement describes the current neighborhood, not every distant parameter value.\n\nTurn a local loss slope into a repeated update\nGradient descent updates a parameter in the direction opposite the gradient. Its rule is new parameter = old parameter - learning rate times gradient. The learning rate is a positive number that scales the update distance. The minus sign chooses the downhill direction; the learning rate chooses how far to move. Subtracting a negative gradient therefore increases the parameter, because a negative quantity is being subtracted.\n\nTurn a local loss slope into a repeated update\nOne update does not finish the process. An iteration is one cycle of measuring the gradient, updating the parameter, and recomputing predictions and loss. After the parameter moves, the local slope can change, so the next iteration needs a new gradient. A learning rate that is too large can overshoot a low-loss region, causing loss to alternate or grow even though each step began in the locally correct direction.\n\nTerms you need · Gradient\nThe local slope of loss with respect to a parameter at its current value.\n\nTerms you need · Gradient descent\nA repeated procedure that moves parameters opposite the current gradient.\n\nTerms you need · Learning rate\nThe positive multiplier that controls how far a gradient update moves.\n\nTerms you need · Iteration\nOne measurement-update-recompute cycle in an optimization procedure.\n\nTerms you need · Overshoot\nA step that crosses a low-loss region because its distance is too large.\n\nFollow two gradient updates from the same starting weight\nThe model predicts y_hat = 1 + wx for rows (1, 3), (2, 5), and (3, 7). Its loss is the mean squared residual. It starts at weight w = 0 and uses learning rate 0.05.\n\nFollow two gradient updates from the same starting weight · Derive and average the row gradients\nFor one row, residual r = 1 + wx - y and dr/dw = x, so the chain rule gives d(r squared)/dw = 2r times x. At w = 0 the three contributions are 2(-2)(1) = -4, 2(-4)(2) = -16, and 2(-6)(3) = -36. Their mean is -56/3 = -18.666667.\n\nFollow two gradient updates from the same starting weight · Read and scale the first direction\nThe gradient is negative, so increasing the weight is locally downhill. Scale it by the learning rate: 0.05(-18.666667) = -0.933333.\n\nFollow two gradient updates from the same starting weight · Apply the first update\nCompute new w = 0 - (-0.933333) = 0.933333. The parameter increases because subtracting a negative amount is addition.\n\nFollow two gradient updates from the same starting weight · Recompute at the new position\nAt w = 0.933333, predictions and residuals have changed. The new gradient is approximately -9.955556, so reusing -18.666667 would describe the wrong position.\n\nFollow two gradient updates from the same starting weight · Apply the second update\nCompute w = 0.933333 - 0.05(-9.955556) = 1.431111. The loss after this second step is approximately 1.510, lower than the previous loss.\n\nFollow two gradient updates from the same starting weight · Interpret the trace\nThe updates move toward weight 2, where these rows have zero loss. A larger learning rate must be tested by comparing the whole loss trace, because one locally correct direction can still overshoot.\n\nFollow two gradient updates from the same starting weight\nDirection comes from the gradient sign, distance comes from the learning rate, and every new position requires a fresh measurement.\n\nCommon confusions\nThe gradient points toward increasing loss. Gradient descent uses its opposite, so subtracting a negative gradient increases the parameter.\n\nCommon confusions\nMoving changes predictions, residuals, and the local slope. Recompute the gradient at each new parameter position.\n\nCommon confusions\nThe direction is only local. An excessive learning rate can cross the low-loss region and land at a worse point.\n\nBefore you predict\nUse the gradient sign to identify the locally uphill direction, then move in the opposite direction.\n\nBefore you predict\nApply new = old - learning rate times gradient and recompute the gradient after every move.\n\nBefore you predict\nDiagnose overshoot from a complete loss trace while changing only the learning rate.\n\nThe gradient describes the current neighborhood\nFor one parameter, the gradient is the slope of loss with respect to that parameter at its current value. A positive gradient means a small parameter increase raises loss; a negative gradient means it lowers loss.\nThis is local evidence. It does not describe every distant part of the landscape.\n\nSubtract the measured slope\nFor one row with prediction 1 + wx, target y, and squared residual r squared, r = 1 + wx - y and dr/dw = x. The chain rule gives d(r squared)/dw = 2r x. Mean squared error averages that contribution over all rows.\nGradient descent applies parameter_new = parameter_old - learning_rate times gradient. The minus sign reverses the uphill direction reported by the gradient.\nThe learning rate scales the move without changing the measured gradient itself.\n\nEvery new position needs a new measurement\nIf weight is 0, gradient is -18.666667, and learning rate is 0.05, one update gives 0 - 0.05(-18.666667) = 0.933333.\nPredictions and residuals change at weight 0.933333, so the next update must use a newly computed gradient there.\n\nDirection and distance are separate\nA step can begin in the downhill direction and still cross the low-loss region. Repeated oversized steps can oscillate or diverge.\nTo test this diagnosis, keep data, initialization, model, and loss fixed, reduce only the learning rate, and compare complete traces.",
    "activityId": "04-descent-explanation",
    "activityPrompt": "Explain why subtracting a negative gradient increases the parameter, why the next gradient must be recomputed, and how a large learning rate can raise loss despite using the correct local direction.",
    "activityGuidance": "Separate sign, new position, and step distance.",
    "criteria": [
      {
        "id": "04-negative-sign",
        "label": "reverse a negative gradient into a positive parameter move"
      },
      {
        "id": "04-new-gradient",
        "label": "recompute after predictions and residuals change"
      },
      {
        "id": "04-overshoot-cause",
        "label": "connect a large learning rate to crossing the low-loss region"
      }
    ],
    "demonstratedFeedback": "You separated the measured direction from the distance moved and the next measurement.",
    "unsupportedFeedback": "Use the update equation once, then state which quantities changed before the second update."
  },
  {
    "lessonId": "gradient-descent",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Gradient descent follows repeated local measurements",
    "lessonContext": "Turn a local loss slope into a repeated update\nTraining seeks parameter values with lower loss. A gradient is a local slope: for one parameter, it measures how loss changes for a small increase in that parameter at its current value. A positive gradient means a small increase is locally uphill, while a negative gradient means a small increase is locally downhill. The word local is essential because the measurement describes the current neighborhood, not every distant parameter value.\n\nTurn a local loss slope into a repeated update\nGradient descent updates a parameter in the direction opposite the gradient. Its rule is new parameter = old parameter - learning rate times gradient. The learning rate is a positive number that scales the update distance. The minus sign chooses the downhill direction; the learning rate chooses how far to move. Subtracting a negative gradient therefore increases the parameter, because a negative quantity is being subtracted.\n\nTurn a local loss slope into a repeated update\nOne update does not finish the process. An iteration is one cycle of measuring the gradient, updating the parameter, and recomputing predictions and loss. After the parameter moves, the local slope can change, so the next iteration needs a new gradient. A learning rate that is too large can overshoot a low-loss region, causing loss to alternate or grow even though each step began in the locally correct direction.\n\nTerms you need · Gradient\nThe local slope of loss with respect to a parameter at its current value.\n\nTerms you need · Gradient descent\nA repeated procedure that moves parameters opposite the current gradient.\n\nTerms you need · Learning rate\nThe positive multiplier that controls how far a gradient update moves.\n\nTerms you need · Iteration\nOne measurement-update-recompute cycle in an optimization procedure.\n\nTerms you need · Overshoot\nA step that crosses a low-loss region because its distance is too large.\n\nFollow two gradient updates from the same starting weight\nThe model predicts y_hat = 1 + wx for rows (1, 3), (2, 5), and (3, 7). Its loss is the mean squared residual. It starts at weight w = 0 and uses learning rate 0.05.\n\nFollow two gradient updates from the same starting weight · Derive and average the row gradients\nFor one row, residual r = 1 + wx - y and dr/dw = x, so the chain rule gives d(r squared)/dw = 2r times x. At w = 0 the three contributions are 2(-2)(1) = -4, 2(-4)(2) = -16, and 2(-6)(3) = -36. Their mean is -56/3 = -18.666667.\n\nFollow two gradient updates from the same starting weight · Read and scale the first direction\nThe gradient is negative, so increasing the weight is locally downhill. Scale it by the learning rate: 0.05(-18.666667) = -0.933333.\n\nFollow two gradient updates from the same starting weight · Apply the first update\nCompute new w = 0 - (-0.933333) = 0.933333. The parameter increases because subtracting a negative amount is addition.\n\nFollow two gradient updates from the same starting weight · Recompute at the new position\nAt w = 0.933333, predictions and residuals have changed. The new gradient is approximately -9.955556, so reusing -18.666667 would describe the wrong position.\n\nFollow two gradient updates from the same starting weight · Apply the second update\nCompute w = 0.933333 - 0.05(-9.955556) = 1.431111. The loss after this second step is approximately 1.510, lower than the previous loss.\n\nFollow two gradient updates from the same starting weight · Interpret the trace\nThe updates move toward weight 2, where these rows have zero loss. A larger learning rate must be tested by comparing the whole loss trace, because one locally correct direction can still overshoot.\n\nFollow two gradient updates from the same starting weight\nDirection comes from the gradient sign, distance comes from the learning rate, and every new position requires a fresh measurement.\n\nCommon confusions\nThe gradient points toward increasing loss. Gradient descent uses its opposite, so subtracting a negative gradient increases the parameter.\n\nCommon confusions\nMoving changes predictions, residuals, and the local slope. Recompute the gradient at each new parameter position.\n\nCommon confusions\nThe direction is only local. An excessive learning rate can cross the low-loss region and land at a worse point.\n\nBefore you predict\nUse the gradient sign to identify the locally uphill direction, then move in the opposite direction.\n\nBefore you predict\nApply new = old - learning rate times gradient and recompute the gradient after every move.\n\nBefore you predict\nDiagnose overshoot from a complete loss trace while changing only the learning rate.\n\nThe gradient describes the current neighborhood\nFor one parameter, the gradient is the slope of loss with respect to that parameter at its current value. A positive gradient means a small parameter increase raises loss; a negative gradient means it lowers loss.\nThis is local evidence. It does not describe every distant part of the landscape.\n\nSubtract the measured slope\nFor one row with prediction 1 + wx, target y, and squared residual r squared, r = 1 + wx - y and dr/dw = x. The chain rule gives d(r squared)/dw = 2r x. Mean squared error averages that contribution over all rows.\nGradient descent applies parameter_new = parameter_old - learning_rate times gradient. The minus sign reverses the uphill direction reported by the gradient.\nThe learning rate scales the move without changing the measured gradient itself.\n\nEvery new position needs a new measurement\nIf weight is 0, gradient is -18.666667, and learning rate is 0.05, one update gives 0 - 0.05(-18.666667) = 0.933333.\nPredictions and residuals change at weight 0.933333, so the next update must use a newly computed gradient there.\n\nDirection and distance are separate\nA step can begin in the downhill direction and still cross the low-loss region. Repeated oversized steps can oscillate or diverge.\nTo test this diagnosis, keep data, initialization, model, and loss fixed, reduce only the learning rate, and compare complete traces.",
    "activityId": "04-valve-transfer",
    "activityPrompt": "A controller tunes a valve coefficient. At coefficient 4.0 the loss gradient is +8.0. Compute one update with learning rate 0.1, then explain what a trace alternating between coefficients 3.2 and 4.8 would test and which single variable you would change first.",
    "activityGuidance": "Map the same local update to a physical coefficient and preserve the controlled comparison.",
    "criteria": [
      {
        "id": "04-transfer-update",
        "label": "compute the new coefficient as 3.2"
      },
      {
        "id": "04-transfer-oscillation",
        "label": "diagnose repeated overshoot from the alternating trace"
      },
      {
        "id": "04-transfer-control",
        "label": "reduce only the learning rate first"
      }
    ],
    "demonstratedFeedback": "The update and controlled overshoot diagnosis transfer to the valve coefficient.",
    "unsupportedFeedback": "Apply new = old - rate times gradient, then change only the quantity that scales the step."
  },
  {
    "lessonId": "split-and-leakage",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Three splits protect three decisions",
    "lessonContext": "Protect model decisions with three separate data roles\nEvaluating a model requires data that did not determine the result being evaluated. A data split is a partition of recorded examples into groups with different jobs. The training split fits model parameters. A candidate is one possible fitted model or setting under consideration. The validation split compares candidates and supports development choices. The test split evaluates the frozen choice only after fitting and selection are complete.\n\nProtect model decisions with three separate data roles\nLeakage is an information path from data into a decision that the data was supposed to evaluate independently. Test leakage occurs if test values or test results influence fitting, preprocessing, stopping, feature choices, threshold choices, or candidate selection. No gradient needs to read test rows for leakage to occur. If a developer sees a test score and changes the chosen model, that score has entered the development process.\n\nProtect model decisions with three separate data roles\nGeneralization is performance on cases beyond those used to fit the model. An untouched test result estimates generalization only for cases represented by the same sampling and measurement process. Independence and coverage answer different questions: independence asks whether test information influenced the model, while coverage asks whether the evaluated cases represent the situations named in the claim. A clean test from one factory cannot establish performance at every factory.\n\nTerms you need · Training split\nThe examples used to fit model parameters.\n\nTerms you need · Validation split\nSeparate examples used to compare candidates and make development choices.\n\nTerms you need · Test split\nExamples reserved for final evaluation after the model and procedure are frozen.\n\nTerms you need · Candidate\nOne model, parameter setting, threshold, or other authored option being compared during selection.\n\nTerms you need · Leakage\nA path by which evaluation information influences fitting or model-development decisions.\n\nTerms you need · Generalization\nPerformance on relevant cases that were not used to fit the model.\n\nSelect with validation, then evaluate once with test data\nTraining targets are 8, 10, and 12. Validation targets are 9 and 13. Test targets are 20 and 22. Three constant-prediction candidates output 10, 11, or 12, and all comparisons use MSE.\n\nSelect with validation, then evaluate once with test data · Fit the training baseline\nThe training mean is (8 + 10 + 12) / 3 = 10. This fit uses only the training split.\n\nSelect with validation, then evaluate once with test data · Score candidate 10 on validation\nIts residuals are 10 - 9 = 1 and 10 - 13 = -3. MSE is (1 squared + (-3) squared) / 2 = 5.\n\nSelect with validation, then evaluate once with test data · Score the other candidates\nCandidate 11 has validation MSE ((2 squared) + (-2 squared)) / 2 = 4. Candidate 12 has MSE ((3 squared) + (-1 squared)) / 2 = 5.\n\nSelect with validation, then evaluate once with test data · Freeze the validation choice\nCandidate 11 has the lowest validation MSE, so select it and stop changing the model or selection procedure before opening the test result.\n\nSelect with validation, then evaluate once with test data · Evaluate on test once\nCandidate 11 has test residuals -9 and -11. Its final test MSE is (81 + 121) / 2 = 101.\n\nSelect with validation, then evaluate once with test data · Expose the leakage alternative\nIf the candidates were selected by test MSE, candidate 12 would look best with MSE 82. Reporting that same 82 as an untouched test estimate would be invalid because test feedback chose the candidate.\n\nSelect with validation, then evaluate once with test data\nA split's role is determined by how its information changes decisions, not by the label attached to the file.\n\nCommon confusions\nAny test-informed development choice creates a path into the selected result, including preprocessing, thresholds, stopping, or candidate selection.\n\nCommon confusions\nHuman decisions can carry the information. Once a test result changes development, those rows no longer provide an untouched final estimate.\n\nCommon confusions\nUntouched data protects against selection feedback. The claim still remains limited to populations, locations, times, and measurements represented by the test process.\n\nBefore you predict\nFit on training data, select among candidates with validation data, and evaluate the frozen choice on test data.\n\nBefore you predict\nTrace leakage through every decision influenced by test information, not only through parameter updates.\n\nBefore you predict\nKeep an independent test estimate separate from the coverage limits of the sampled cases.\n\nPartition before the answer can influence a decision\nTraining data fits parameters. Validation data compares model or hyperparameter choices. Test data evaluates the frozen process after fitting and selection are complete.\nThe labels do not make a split. Its role is defined by how information from it is used.\n\nValidation closes the development loop\nFit candidate models on training rows, compare their validation losses, and select one. A validation result can change the next candidate, so it becomes part of development evidence.\nAfter selection, refit only according to the authored procedure and freeze every choice before test evaluation.\n\nLeakage is an information path\nTest leakage occurs when test features, targets, metrics, or repeated feedback influence fitting, preprocessing, feature choices, stopping, or model selection.\nCalling the same rows test data afterward does not restore independence. A new untouched set is required for an honest final estimate.\n\nUntouched does not mean universal\nAn untouched test set estimates behavior for cases represented by the same sampling and measurement process. It does not establish behavior for missing locations, users, seasons, or sensors.\nIndependence protects the estimate from selection bias; coverage limits the situations to which that estimate applies.",
    "activityId": "05-leakage-explanation",
    "activityPrompt": "Explain why choosing a candidate from test loss contaminates the final estimate even when no gradient used test rows, and state the separate jobs of train, validation, and test.",
    "activityGuidance": "Trace the information path from a metric to a decision.",
    "criteria": [
      {
        "id": "05-train-job",
        "label": "fit parameters on training data"
      },
      {
        "id": "05-validation-job",
        "label": "select candidates with validation data"
      },
      {
        "id": "05-test-job",
        "label": "reserve test data for final evaluation"
      },
      {
        "id": "05-information-path",
        "label": "identify test metric influence on the chosen model"
      }
    ],
    "demonstratedFeedback": "You identified leakage as decision-relevant information, not merely shared rows.",
    "unsupportedFeedback": "State what changed after seeing the test metric; that changed decision is the leakage path."
  },
  {
    "lessonId": "split-and-leakage",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Three splits protect three decisions",
    "lessonContext": "Protect model decisions with three separate data roles\nEvaluating a model requires data that did not determine the result being evaluated. A data split is a partition of recorded examples into groups with different jobs. The training split fits model parameters. A candidate is one possible fitted model or setting under consideration. The validation split compares candidates and supports development choices. The test split evaluates the frozen choice only after fitting and selection are complete.\n\nProtect model decisions with three separate data roles\nLeakage is an information path from data into a decision that the data was supposed to evaluate independently. Test leakage occurs if test values or test results influence fitting, preprocessing, stopping, feature choices, threshold choices, or candidate selection. No gradient needs to read test rows for leakage to occur. If a developer sees a test score and changes the chosen model, that score has entered the development process.\n\nProtect model decisions with three separate data roles\nGeneralization is performance on cases beyond those used to fit the model. An untouched test result estimates generalization only for cases represented by the same sampling and measurement process. Independence and coverage answer different questions: independence asks whether test information influenced the model, while coverage asks whether the evaluated cases represent the situations named in the claim. A clean test from one factory cannot establish performance at every factory.\n\nTerms you need · Training split\nThe examples used to fit model parameters.\n\nTerms you need · Validation split\nSeparate examples used to compare candidates and make development choices.\n\nTerms you need · Test split\nExamples reserved for final evaluation after the model and procedure are frozen.\n\nTerms you need · Candidate\nOne model, parameter setting, threshold, or other authored option being compared during selection.\n\nTerms you need · Leakage\nA path by which evaluation information influences fitting or model-development decisions.\n\nTerms you need · Generalization\nPerformance on relevant cases that were not used to fit the model.\n\nSelect with validation, then evaluate once with test data\nTraining targets are 8, 10, and 12. Validation targets are 9 and 13. Test targets are 20 and 22. Three constant-prediction candidates output 10, 11, or 12, and all comparisons use MSE.\n\nSelect with validation, then evaluate once with test data · Fit the training baseline\nThe training mean is (8 + 10 + 12) / 3 = 10. This fit uses only the training split.\n\nSelect with validation, then evaluate once with test data · Score candidate 10 on validation\nIts residuals are 10 - 9 = 1 and 10 - 13 = -3. MSE is (1 squared + (-3) squared) / 2 = 5.\n\nSelect with validation, then evaluate once with test data · Score the other candidates\nCandidate 11 has validation MSE ((2 squared) + (-2 squared)) / 2 = 4. Candidate 12 has MSE ((3 squared) + (-1 squared)) / 2 = 5.\n\nSelect with validation, then evaluate once with test data · Freeze the validation choice\nCandidate 11 has the lowest validation MSE, so select it and stop changing the model or selection procedure before opening the test result.\n\nSelect with validation, then evaluate once with test data · Evaluate on test once\nCandidate 11 has test residuals -9 and -11. Its final test MSE is (81 + 121) / 2 = 101.\n\nSelect with validation, then evaluate once with test data · Expose the leakage alternative\nIf the candidates were selected by test MSE, candidate 12 would look best with MSE 82. Reporting that same 82 as an untouched test estimate would be invalid because test feedback chose the candidate.\n\nSelect with validation, then evaluate once with test data\nA split's role is determined by how its information changes decisions, not by the label attached to the file.\n\nCommon confusions\nAny test-informed development choice creates a path into the selected result, including preprocessing, thresholds, stopping, or candidate selection.\n\nCommon confusions\nHuman decisions can carry the information. Once a test result changes development, those rows no longer provide an untouched final estimate.\n\nCommon confusions\nUntouched data protects against selection feedback. The claim still remains limited to populations, locations, times, and measurements represented by the test process.\n\nBefore you predict\nFit on training data, select among candidates with validation data, and evaluate the frozen choice on test data.\n\nBefore you predict\nTrace leakage through every decision influenced by test information, not only through parameter updates.\n\nBefore you predict\nKeep an independent test estimate separate from the coverage limits of the sampled cases.\n\nPartition before the answer can influence a decision\nTraining data fits parameters. Validation data compares model or hyperparameter choices. Test data evaluates the frozen process after fitting and selection are complete.\nThe labels do not make a split. Its role is defined by how information from it is used.\n\nValidation closes the development loop\nFit candidate models on training rows, compare their validation losses, and select one. A validation result can change the next candidate, so it becomes part of development evidence.\nAfter selection, refit only according to the authored procedure and freeze every choice before test evaluation.\n\nLeakage is an information path\nTest leakage occurs when test features, targets, metrics, or repeated feedback influence fitting, preprocessing, feature choices, stopping, or model selection.\nCalling the same rows test data afterward does not restore independence. A new untouched set is required for an honest final estimate.\n\nUntouched does not mean universal\nAn untouched test set estimates behavior for cases represented by the same sampling and measurement process. It does not establish behavior for missing locations, users, seasons, or sensors.\nIndependence protects the estimate from selection bias; coverage limits the situations to which that estimate applies.",
    "activityId": "05-factory-transfer",
    "activityPrompt": "A factory trains a defect model on Line A, tunes its threshold by repeatedly checking Line B, then reports the best Line B result as the test score for all factories. Identify each data role, the leakage, the repair, and the remaining coverage limit.",
    "activityGuidance": "Separate independence from representativeness.",
    "criteria": [
      {
        "id": "05-transfer-roles",
        "label": "treat Line A as training and Line B as validation"
      },
      {
        "id": "05-transfer-leak",
        "label": "identify repeated Line B threshold selection as validation use"
      },
      {
        "id": "05-transfer-repair",
        "label": "require a new untouched final test set"
      },
      {
        "id": "05-transfer-scope",
        "label": "limit claims beyond represented lines or factories"
      }
    ],
    "demonstratedFeedback": "You repaired the information path and kept the deployment claim inside the data coverage.",
    "unsupportedFeedback": "Rename Line B by how it was used, then introduce data that influenced no decision."
  },
  {
    "lessonId": "capacity-curves",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Capacity changes what a model can fit",
    "lessonContext": "Diagnose capacity with paired training and held-out evidence\nModel capacity is the range of rules a model family can represent. A polynomial combines powers of an input, such as a constant, x, x squared, and higher powers. Its degree is the highest power included. A degree-one polynomial can draw a line; adding squared and higher-power terms allows more varied curves. Greater capacity expands what can be fitted, but it does not guarantee better predictions on new cases.\n\nDiagnose capacity with paired training and held-out evidence\nTraining loss measures errors on examples used to fit the model. Held-out loss measures errors on separate examples not used for that fit. Underfitting occurs when the current procedure cannot capture the relevant pattern, often appearing as high training and held-out losses that are both similar. Overfitting occurs when the fitted rule follows details specific to the training sample, appearing as very low training loss with substantially higher held-out loss.\n\nDiagnose capacity with paired training and held-out evidence\nA learning curve is a sequence of training and validation scores obtained by refitting the same procedure across several training-set sizes under one fixed validation protocol. It is not any graph with two loss bars. Learning curves matter because they can show how fit and held-out behavior change as more training examples are used. The test set still remains outside repeated diagnosis and model selection.\n\nTerms you need · Model capacity\nThe set of input-to-prediction rules that a model family can represent.\n\nTerms you need · Polynomial degree\nThe highest input power in a polynomial rule; higher degrees permit a wider family of curves.\n\nTerms you need · Held-out loss\nLoss measured on separate examples that were not used to fit the evaluated model.\n\nTerms you need · Underfitting\nFailure to capture the pattern, supported by high, similar training and held-out losses under a fixed protocol.\n\nTerms you need · Overfitting\nFitting training-sample details that do not transfer, supported by low training loss and much higher held-out loss.\n\nTerms you need · Learning curve\nTraining and validation performance measured after refitting across multiple training sizes with one fixed validation protocol.\n\nCompare a matching quadratic with a high-degree interpolator\nTraining rows follow y = x squared at x values -2, -1, 0, 1, and 2. Validation rows use x values -1.5, 0.5, and 1.5 with targets 2.25, 0.25, and 2.25.\n\nCompare a matching quadratic with a high-degree interpolator · Evaluate the quadratic rule\nThe degree-two rule y_hat = x squared predicts 4, 1, 0, 1, and 4 on training. Every residual is zero, so training MSE is zero.\n\nCompare a matching quadratic with a high-degree interpolator · Check the quadratic on validation\nAt -1.5, 0.5, and 1.5, x squared gives 2.25, 0.25, and 2.25. Validation MSE is also zero for these authored points.\n\nCompare a matching quadratic with a high-degree interpolator · Introduce the higher-capacity rule\nUsing coefficients in ascending powers [constant, x, x squared, x cubed, x to the fourth, x to the fifth], [0, 8, 1, -10, 0, 2] means y_hat = 8x + x squared - 10x cubed + 2x to the fifth. It also predicts every training target exactly, so its training MSE is zero.\n\nCompare a matching quadratic with a high-degree interpolator · Trace one held-out miss\nAt x = -1.5, the degree-five rule predicts 8.8125 instead of 2.25. Its residual is 6.5625, whose squared contribution is about 43.066.\n\nCompare a matching quadratic with a high-degree interpolator · Compare the complete held-out result\nAcross all three validation rows, the degree-five rule has MSE about 31.348. Equal zero training loss therefore hides very different held-out behavior.\n\nCompare a matching quadratic with a high-degree interpolator · State the protocol boundary\nThis is a capacity comparison on one fixed train-validation split. It is not a learning curve because the models were not refitted across several nested training sizes.\n\nCompare a matching quadratic with a high-degree interpolator\nCapacity diagnoses require both training and held-out loss, while a learning-curve claim additionally requires repeated fits across controlled training sizes.\n\nCommon confusions\nTraining loss shows fit to seen examples. Compare held-out loss to determine whether that fit transfers under the evaluation protocol.\n\nCommon confusions\nUse the pair. High training and held-out losses support underfit; low training loss with a large held-out gap supports overfit.\n\nCommon confusions\nA valid learning curve uses several training sizes and preserves one validation protocol so the size trend is interpretable.\n\nBefore you predict\nHigher polynomial degree expands the family of representable curves but does not guarantee lower held-out loss.\n\nBefore you predict\nDiagnose underfit or overfit from training loss, held-out loss, and the gap between them.\n\nBefore you predict\nReserve the term learning curve for repeated fits across multiple training sizes under one fixed evaluation protocol.\n\nCapacity is a set of possible rules\nA linear polynomial can represent straight lines. Adding squared and higher-power terms expands the set of curves the model can fit.\nMore capacity is not automatically better. It can represent the underlying pattern, but it can also follow sample-specific noise. In the visual lab, a fixed coefficient-size penalty of 0.000001 adds that coefficient times the sum of squared coefficient values to the fitting objective. The tiny term slightly changes every fitted solution and selects one reproducible coefficient vector when the rows do not uniquely determine all coefficients; it is not merely a divide-by-zero guard.\n\nHigh train and validation loss indicate underfit\nIf training and held-out loss are both high and close under one fixed evaluation protocol, the current features, model family, or optimization may not capture the pattern.\nMore observations alone do not repair a rule that cannot represent the relationship. First test one controlled change to capacity, features, objective, or optimization.\n\nA persistent gap indicates overfit\nVery low training loss with substantially higher validation loss means the fitted rule captured details that did not transfer to validation cases.\nReducing capacity or constraining the fit can shrink this gap. More representative data is another possible intervention, but this lesson's local code does not test a data-size sequence.\n\nTwo fitted data states are not a learning curve\nThe visual compares a five-row fit evaluated on three added rows with an eight-row fit evaluated on a separate probe set. Because both the training rows and held-out rows change, the grouped bars are a capacity comparison, not a learning curve.\nA learning curve refits across several nested training sizes while preserving one validation or cross-validation protocol. It can support a data-scaling diagnosis for that fixed pipeline and represented distribution, but it never justifies repeated test inspection.",
    "activityId": "06-curves-explanation",
    "activityPrompt": "Explain how train and held-out loss pairs distinguish a capacity bottleneck from sample-specific overfit. Then explain why the two authored data states do not form a learning curve and why slider movement or low training loss alone proves neither diagnosis.",
    "activityGuidance": "Use both losses and their gap, then name what a multi-size learning-curve protocol would hold fixed.",
    "criteria": [
      {
        "id": "06-underfit-pattern",
        "label": "identify high close training and validation losses as underfit evidence"
      },
      {
        "id": "06-overfit-pattern",
        "label": "identify low train loss and high validation loss as overfit evidence"
      },
      {
        "id": "06-protocol-boundary",
        "label": "distinguish the two-state comparison from a learning curve"
      },
      {
        "id": "06-not-activity",
        "label": "reject control movement or train fit as sufficient evidence"
      }
    ],
    "demonstratedFeedback": "You tied each diagnosis to a train-held-out pattern and kept the two-state capacity comparison distinct from a learning curve.",
    "unsupportedFeedback": "Describe training loss, held-out loss, their gap, and what a valid multi-size protocol would keep fixed."
  },
  {
    "lessonId": "capacity-curves",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Capacity changes what a model can fit",
    "lessonContext": "Diagnose capacity with paired training and held-out evidence\nModel capacity is the range of rules a model family can represent. A polynomial combines powers of an input, such as a constant, x, x squared, and higher powers. Its degree is the highest power included. A degree-one polynomial can draw a line; adding squared and higher-power terms allows more varied curves. Greater capacity expands what can be fitted, but it does not guarantee better predictions on new cases.\n\nDiagnose capacity with paired training and held-out evidence\nTraining loss measures errors on examples used to fit the model. Held-out loss measures errors on separate examples not used for that fit. Underfitting occurs when the current procedure cannot capture the relevant pattern, often appearing as high training and held-out losses that are both similar. Overfitting occurs when the fitted rule follows details specific to the training sample, appearing as very low training loss with substantially higher held-out loss.\n\nDiagnose capacity with paired training and held-out evidence\nA learning curve is a sequence of training and validation scores obtained by refitting the same procedure across several training-set sizes under one fixed validation protocol. It is not any graph with two loss bars. Learning curves matter because they can show how fit and held-out behavior change as more training examples are used. The test set still remains outside repeated diagnosis and model selection.\n\nTerms you need · Model capacity\nThe set of input-to-prediction rules that a model family can represent.\n\nTerms you need · Polynomial degree\nThe highest input power in a polynomial rule; higher degrees permit a wider family of curves.\n\nTerms you need · Held-out loss\nLoss measured on separate examples that were not used to fit the evaluated model.\n\nTerms you need · Underfitting\nFailure to capture the pattern, supported by high, similar training and held-out losses under a fixed protocol.\n\nTerms you need · Overfitting\nFitting training-sample details that do not transfer, supported by low training loss and much higher held-out loss.\n\nTerms you need · Learning curve\nTraining and validation performance measured after refitting across multiple training sizes with one fixed validation protocol.\n\nCompare a matching quadratic with a high-degree interpolator\nTraining rows follow y = x squared at x values -2, -1, 0, 1, and 2. Validation rows use x values -1.5, 0.5, and 1.5 with targets 2.25, 0.25, and 2.25.\n\nCompare a matching quadratic with a high-degree interpolator · Evaluate the quadratic rule\nThe degree-two rule y_hat = x squared predicts 4, 1, 0, 1, and 4 on training. Every residual is zero, so training MSE is zero.\n\nCompare a matching quadratic with a high-degree interpolator · Check the quadratic on validation\nAt -1.5, 0.5, and 1.5, x squared gives 2.25, 0.25, and 2.25. Validation MSE is also zero for these authored points.\n\nCompare a matching quadratic with a high-degree interpolator · Introduce the higher-capacity rule\nUsing coefficients in ascending powers [constant, x, x squared, x cubed, x to the fourth, x to the fifth], [0, 8, 1, -10, 0, 2] means y_hat = 8x + x squared - 10x cubed + 2x to the fifth. It also predicts every training target exactly, so its training MSE is zero.\n\nCompare a matching quadratic with a high-degree interpolator · Trace one held-out miss\nAt x = -1.5, the degree-five rule predicts 8.8125 instead of 2.25. Its residual is 6.5625, whose squared contribution is about 43.066.\n\nCompare a matching quadratic with a high-degree interpolator · Compare the complete held-out result\nAcross all three validation rows, the degree-five rule has MSE about 31.348. Equal zero training loss therefore hides very different held-out behavior.\n\nCompare a matching quadratic with a high-degree interpolator · State the protocol boundary\nThis is a capacity comparison on one fixed train-validation split. It is not a learning curve because the models were not refitted across several nested training sizes.\n\nCompare a matching quadratic with a high-degree interpolator\nCapacity diagnoses require both training and held-out loss, while a learning-curve claim additionally requires repeated fits across controlled training sizes.\n\nCommon confusions\nTraining loss shows fit to seen examples. Compare held-out loss to determine whether that fit transfers under the evaluation protocol.\n\nCommon confusions\nUse the pair. High training and held-out losses support underfit; low training loss with a large held-out gap supports overfit.\n\nCommon confusions\nA valid learning curve uses several training sizes and preserves one validation protocol so the size trend is interpretable.\n\nBefore you predict\nHigher polynomial degree expands the family of representable curves but does not guarantee lower held-out loss.\n\nBefore you predict\nDiagnose underfit or overfit from training loss, held-out loss, and the gap between them.\n\nBefore you predict\nReserve the term learning curve for repeated fits across multiple training sizes under one fixed evaluation protocol.\n\nCapacity is a set of possible rules\nA linear polynomial can represent straight lines. Adding squared and higher-power terms expands the set of curves the model can fit.\nMore capacity is not automatically better. It can represent the underlying pattern, but it can also follow sample-specific noise. In the visual lab, a fixed coefficient-size penalty of 0.000001 adds that coefficient times the sum of squared coefficient values to the fitting objective. The tiny term slightly changes every fitted solution and selects one reproducible coefficient vector when the rows do not uniquely determine all coefficients; it is not merely a divide-by-zero guard.\n\nHigh train and validation loss indicate underfit\nIf training and held-out loss are both high and close under one fixed evaluation protocol, the current features, model family, or optimization may not capture the pattern.\nMore observations alone do not repair a rule that cannot represent the relationship. First test one controlled change to capacity, features, objective, or optimization.\n\nA persistent gap indicates overfit\nVery low training loss with substantially higher validation loss means the fitted rule captured details that did not transfer to validation cases.\nReducing capacity or constraining the fit can shrink this gap. More representative data is another possible intervention, but this lesson's local code does not test a data-size sequence.\n\nTwo fitted data states are not a learning curve\nThe visual compares a five-row fit evaluated on three added rows with an eight-row fit evaluated on a separate probe set. Because both the training rows and held-out rows change, the grouped bars are a capacity comparison, not a learning curve.\nA learning curve refits across several nested training sizes while preserving one validation or cross-validation protocol. It can support a data-scaling diagnosis for that fixed pipeline and represented distribution, but it never justifies repeated test inspection.",
    "activityId": "06-spectrometer-transfer",
    "activityPrompt": "A spectrometer calibration compares polynomial degrees on one fixed train-validation split. Model A, degree 1, has high similar train and validation errors. Model B, degree 9, has nearly zero train error and high validation error. Diagnose each, prescribe one controlled capacity test for each, and explain why these fits are not a learning curve.",
    "activityGuidance": "Map each loss pair to a capacity diagnosis, then state the missing multi-size protocol.",
    "criteria": [
      {
        "id": "06-transfer-a",
        "label": "diagnose Model A as underfit or otherwise capacity-limited"
      },
      {
        "id": "06-transfer-a-test",
        "label": "test one added capacity or feature intervention for A"
      },
      {
        "id": "06-transfer-b",
        "label": "diagnose Model B as overfit and test lower capacity"
      },
      {
        "id": "06-transfer-protocol",
        "label": "deny a learning-curve claim without nested training sizes"
      }
    ],
    "demonstratedFeedback": "You matched each calibration failure to capacity evidence, proposed controlled tests, and stated the learning-curve boundary.",
    "unsupportedFeedback": "Model A cannot fit either split; Model B fits training too specifically. Neither comparison varies training size under one held-out protocol."
  },
  {
    "lessonId": "logistic-link",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "A linear score becomes a probability",
    "lessonContext": "From a weighted score to a modeled probability\nBinary classification means choosing between two possible target classes, such as failure and no failure. A feature is an input measurement, a weight says how strongly that feature contributes, and a bias is an adjustable starting value. Logistic regression first combines them exactly as a linear model does: multiply each feature by its weight, add those products, and add the bias.\n\nFrom a weighted score to a modeled probability\nThat result is called the logit. In logistic regression, the logit is also the log-odds of the target class. If its probability is p, the other class has probability 1 - p, the odds are p / (1 - p), and the log-odds are z = ln(p / (1 - p)). Odds compare the target class with the other class: p = 0.75 gives odds 0.75 / 0.25 = 3, read as three to one.\n\nFrom a weighted score to a modeled probability\nA logit can be any real number, so it is not itself a probability. The sigmoid p = 1 / (1 + exp(-z)) converts log-odds z to a probability, while z = ln(p / (1 - p)) converts that probability back to log-odds. The two transformations are inverses. A zero logit maps to 0.5, positive logits map above 0.5, and negative logits map below 0.5. Because sigmoid preserves order, it changes the scale without changing which example has the larger score.\n\nFrom a weighted score to a modeled probability\nTraining needs a loss that judges the probability against what actually happened. The natural logarithm ln reverses exp: exp(ln(a)) = a for positive a. Binary log loss reads the probability assigned to the observed target and takes its negative natural logarithm. Assigning a large probability to the observed class gives a small loss; assigning it a tiny probability gives a large loss. Training adjusts weights and bias to reduce average training loss, while held-out data is still needed to check whether the rule generalizes.\n\nTerms you need · Binary target\nThe observed outcome with two allowed classes, conventionally written as 0 and 1.\n\nTerms you need · Feature\nAn input value supplied to the model, such as temperature or vibration.\n\nTerms you need · Weight and bias\nLearned numbers that scale feature contributions and shift the overall score.\n\nTerms you need · Odds and logit\nOdds are p / (1 - p), comparing the target-class probability p with the other-class probability. The logit is the log-odds z = ln(p / (1 - p)); logistic regression models it as the unbounded weighted sum z = w dot x + b.\n\nTerms you need · Sigmoid\nThe inverse of the log-odds transformation: p = 1 / (1 + exp(-z)) maps a finite logit z back to a mathematical probability strictly between zero and one.\n\nTerms you need · Log loss\nThe negative natural logarithm of the probability assigned to the class that was actually observed.\n\nTerms you need · Exponential exp\nThe operation exp(a) = e raised to power a; sigmoid uses exp(-z) to convert a logit.\n\nTerms you need · Natural logarithm ln\nThe inverse of exp for positive values, so exp(ln(a)) = a and ln(exp(a)) = a.\n\nScore one machine outcome from beginning to end\nA bearing model produces logit z = ln(3) for the target class failure. Trace that one score into both possible target losses.\n\nScore one machine outcome from beginning to end · Keep the raw score\nThe model's weighted feature calculation has produced z = ln(3), about 1.099. This is a positive logit, but it is not yet a probability or a final class.\n\nScore one machine outcome from beginning to end · Read the score as log-odds\nBecause z = ln(p / (1 - p)), exponentiating both sides gives p / (1 - p) = exp(z) = 3. The target-class odds are therefore three to one. Solving p = 3(1 - p) gives 4p = 3, so p = 0.75.\n\nScore one machine outcome from beginning to end · Apply sigmoid\nSubstitute the same z into p = 1 / (1 + exp(-z)). Because -ln(3) = ln(1/3), exp(-ln(3)) = 1/3. Therefore p(failure) = 1 / (1 + 1/3) = 1 / (4/3) = 0.75. Sigmoid recovers the same probability because it is the inverse of log-odds.\n\nScore one machine outcome from beginning to end · Find the other class probability\nThe two binary probabilities must add to one, so p(no failure) = 1 - 0.75 = 0.25.\n\nScore one machine outcome from beginning to end · Score an observed failure\nIf the target is 1, log loss reads p(failure): -ln(0.75), which is about 0.288.\n\nScore one machine outcome from beginning to end · Score an observed non-failure\nIf the target is 0, log loss reads p(no failure): -ln(0.25), which is about 1.386. The same prediction is penalized more because it assigned less probability to what happened.\n\nScore one machine outcome from beginning to end\nThe calculation has three distinct objects: an unrestricted linear score, a bounded probability, and a target-dependent loss. Keeping them separate makes both prediction and training easier to trace.\n\nCommon confusions\nA positive logit only places the example on one side of logit zero. Sigmoid must still convert that unrestricted score to a probability.\n\nCommon confusions\nAt a 0.5 cutoff, sigmoid maps back to logit zero, so the boundary remains w dot x + b = 0 in the original feature space.\n\nCommon confusions\nLoss also depends on the observed target: p is read for target 1, while 1 - p is read for target 0.\n\nBefore you predict\nFirst recompute the logit from the contributions that changed; do not treat the old probability as an input.\n\nBefore you predict\nUse logit zero and probability 0.5 as the shared reference point when reasoning about sigmoid.\n\nBefore you predict\nAfter finding a probability, identify the observed target before deciding which probability log loss will read.\n\nKeep the score visible\nA binary classifier can begin with the same weighted sum used by a linear model: z = w1 x1 + w2 x2 + b. This unbounded number is the logit.\nThe logit is not yet a class label or a probability. Its sign says which side of the boundary the example lies on, and its distance from zero records how strongly the linear score favors that side.\n\nSigmoid bends the number line\nThe sigmoid is p = 1 / (1 + exp(-z)). At z = 0, p = 0.5. Mathematically, every finite logit maps strictly between zero and one. In finite-precision arithmetic, an extreme finite logit may round to 0.0 or 1.0; that endpoint is numerical saturation, not the exact mathematical value.\nFor z = ln(3), exp(-z) = 1/3, so p = 1 / (1 + 1/3) = 0.75. Reading backward, the odds are p / (1 - p) = 0.75 / 0.25 = 3 and ln(3) returns z. Sigmoid converts log-odds to probability; z = ln(p / (1 - p)) converts probability back to log-odds. The inverse transformations change the scale, not the ordering.\n\nThe boundary remains linear\nUsing probability 0.5 as the decision cutoff is equivalent to using logit zero, because sigmoid(0) = 0.5. The boundary is therefore the set of inputs where w dot x + b = 0.\nSigmoid changes how the score is interpreted, but it does not curve the boundary in the original feature space. Nonlinear boundaries require nonlinear features or a nonlinear composition.\n\nLog loss scores the probability of what happened\nFor target y = 1, log loss is -log(p). For target y = 0, it is -log(1 - p). In both cases, the loss reads the probability assigned to the observed target.\nPredicting p = 0.8 gives loss about 0.223 when y = 1 but about 1.609 when y = 0. A confident probability aimed at the wrong class is expensive because the probability assigned to the actual target is small.\n\nTraining still adjusts parameters\nTraining adjusts the weights and bias to reduce mean training log loss, tending to increase the probability assigned to observed targets in aggregate; an individual training example can still worsen. Inference holds those learned parameters fixed and computes logits and probabilities for new examples.\nA low training log loss is not proof of generalization. The same split discipline from regression still determines whether the probability rule works on unseen data.",
    "activityId": "logistic-mechanism-explanation",
    "activityPrompt": "Explain the complete path from features to log loss. Include why logit zero maps to probability 0.5 and why the same probability has different loss for targets 0 and 1.",
    "activityGuidance": "Name the weighted sum, sigmoid transformation, target-dependent probability, and negative logarithm.",
    "criteria": [
      {
        "id": "logit-weighted-sum",
        "label": "identify the logit as a weighted sum plus bias"
      },
      {
        "id": "sigmoid-bounds",
        "label": "state that mathematical sigmoid maps the logit strictly between zero and one"
      },
      {
        "id": "zero-midpoint",
        "label": "connect logit zero to probability 0.5"
      },
      {
        "id": "target-probability",
        "label": "make log loss depend on probability assigned to the target"
      }
    ],
    "demonstratedFeedback": "You traced the classifier from an unbounded score to a target-dependent probabilistic loss.",
    "unsupportedFeedback": "Trace four named objects in order: logit, sigmoid probability, observed target, and negative log probability."
  },
  {
    "lessonId": "logistic-link",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "A linear score becomes a probability",
    "lessonContext": "From a weighted score to a modeled probability\nBinary classification means choosing between two possible target classes, such as failure and no failure. A feature is an input measurement, a weight says how strongly that feature contributes, and a bias is an adjustable starting value. Logistic regression first combines them exactly as a linear model does: multiply each feature by its weight, add those products, and add the bias.\n\nFrom a weighted score to a modeled probability\nThat result is called the logit. In logistic regression, the logit is also the log-odds of the target class. If its probability is p, the other class has probability 1 - p, the odds are p / (1 - p), and the log-odds are z = ln(p / (1 - p)). Odds compare the target class with the other class: p = 0.75 gives odds 0.75 / 0.25 = 3, read as three to one.\n\nFrom a weighted score to a modeled probability\nA logit can be any real number, so it is not itself a probability. The sigmoid p = 1 / (1 + exp(-z)) converts log-odds z to a probability, while z = ln(p / (1 - p)) converts that probability back to log-odds. The two transformations are inverses. A zero logit maps to 0.5, positive logits map above 0.5, and negative logits map below 0.5. Because sigmoid preserves order, it changes the scale without changing which example has the larger score.\n\nFrom a weighted score to a modeled probability\nTraining needs a loss that judges the probability against what actually happened. The natural logarithm ln reverses exp: exp(ln(a)) = a for positive a. Binary log loss reads the probability assigned to the observed target and takes its negative natural logarithm. Assigning a large probability to the observed class gives a small loss; assigning it a tiny probability gives a large loss. Training adjusts weights and bias to reduce average training loss, while held-out data is still needed to check whether the rule generalizes.\n\nTerms you need · Binary target\nThe observed outcome with two allowed classes, conventionally written as 0 and 1.\n\nTerms you need · Feature\nAn input value supplied to the model, such as temperature or vibration.\n\nTerms you need · Weight and bias\nLearned numbers that scale feature contributions and shift the overall score.\n\nTerms you need · Odds and logit\nOdds are p / (1 - p), comparing the target-class probability p with the other-class probability. The logit is the log-odds z = ln(p / (1 - p)); logistic regression models it as the unbounded weighted sum z = w dot x + b.\n\nTerms you need · Sigmoid\nThe inverse of the log-odds transformation: p = 1 / (1 + exp(-z)) maps a finite logit z back to a mathematical probability strictly between zero and one.\n\nTerms you need · Log loss\nThe negative natural logarithm of the probability assigned to the class that was actually observed.\n\nTerms you need · Exponential exp\nThe operation exp(a) = e raised to power a; sigmoid uses exp(-z) to convert a logit.\n\nTerms you need · Natural logarithm ln\nThe inverse of exp for positive values, so exp(ln(a)) = a and ln(exp(a)) = a.\n\nScore one machine outcome from beginning to end\nA bearing model produces logit z = ln(3) for the target class failure. Trace that one score into both possible target losses.\n\nScore one machine outcome from beginning to end · Keep the raw score\nThe model's weighted feature calculation has produced z = ln(3), about 1.099. This is a positive logit, but it is not yet a probability or a final class.\n\nScore one machine outcome from beginning to end · Read the score as log-odds\nBecause z = ln(p / (1 - p)), exponentiating both sides gives p / (1 - p) = exp(z) = 3. The target-class odds are therefore three to one. Solving p = 3(1 - p) gives 4p = 3, so p = 0.75.\n\nScore one machine outcome from beginning to end · Apply sigmoid\nSubstitute the same z into p = 1 / (1 + exp(-z)). Because -ln(3) = ln(1/3), exp(-ln(3)) = 1/3. Therefore p(failure) = 1 / (1 + 1/3) = 1 / (4/3) = 0.75. Sigmoid recovers the same probability because it is the inverse of log-odds.\n\nScore one machine outcome from beginning to end · Find the other class probability\nThe two binary probabilities must add to one, so p(no failure) = 1 - 0.75 = 0.25.\n\nScore one machine outcome from beginning to end · Score an observed failure\nIf the target is 1, log loss reads p(failure): -ln(0.75), which is about 0.288.\n\nScore one machine outcome from beginning to end · Score an observed non-failure\nIf the target is 0, log loss reads p(no failure): -ln(0.25), which is about 1.386. The same prediction is penalized more because it assigned less probability to what happened.\n\nScore one machine outcome from beginning to end\nThe calculation has three distinct objects: an unrestricted linear score, a bounded probability, and a target-dependent loss. Keeping them separate makes both prediction and training easier to trace.\n\nCommon confusions\nA positive logit only places the example on one side of logit zero. Sigmoid must still convert that unrestricted score to a probability.\n\nCommon confusions\nAt a 0.5 cutoff, sigmoid maps back to logit zero, so the boundary remains w dot x + b = 0 in the original feature space.\n\nCommon confusions\nLoss also depends on the observed target: p is read for target 1, while 1 - p is read for target 0.\n\nBefore you predict\nFirst recompute the logit from the contributions that changed; do not treat the old probability as an input.\n\nBefore you predict\nUse logit zero and probability 0.5 as the shared reference point when reasoning about sigmoid.\n\nBefore you predict\nAfter finding a probability, identify the observed target before deciding which probability log loss will read.\n\nKeep the score visible\nA binary classifier can begin with the same weighted sum used by a linear model: z = w1 x1 + w2 x2 + b. This unbounded number is the logit.\nThe logit is not yet a class label or a probability. Its sign says which side of the boundary the example lies on, and its distance from zero records how strongly the linear score favors that side.\n\nSigmoid bends the number line\nThe sigmoid is p = 1 / (1 + exp(-z)). At z = 0, p = 0.5. Mathematically, every finite logit maps strictly between zero and one. In finite-precision arithmetic, an extreme finite logit may round to 0.0 or 1.0; that endpoint is numerical saturation, not the exact mathematical value.\nFor z = ln(3), exp(-z) = 1/3, so p = 1 / (1 + 1/3) = 0.75. Reading backward, the odds are p / (1 - p) = 0.75 / 0.25 = 3 and ln(3) returns z. Sigmoid converts log-odds to probability; z = ln(p / (1 - p)) converts probability back to log-odds. The inverse transformations change the scale, not the ordering.\n\nThe boundary remains linear\nUsing probability 0.5 as the decision cutoff is equivalent to using logit zero, because sigmoid(0) = 0.5. The boundary is therefore the set of inputs where w dot x + b = 0.\nSigmoid changes how the score is interpreted, but it does not curve the boundary in the original feature space. Nonlinear boundaries require nonlinear features or a nonlinear composition.\n\nLog loss scores the probability of what happened\nFor target y = 1, log loss is -log(p). For target y = 0, it is -log(1 - p). In both cases, the loss reads the probability assigned to the observed target.\nPredicting p = 0.8 gives loss about 0.223 when y = 1 but about 1.609 when y = 0. A confident probability aimed at the wrong class is expensive because the probability assigned to the actual target is small.\n\nTraining still adjusts parameters\nTraining adjusts the weights and bias to reduce mean training log loss, tending to increase the probability assigned to observed targets in aggregate; an individual training example can still worsen. Inference holds those learned parameters fixed and computes logits and probabilities for new examples.\nA low training log loss is not proof of generalization. The same split discipline from regression still determines whether the probability rule works on unseen data.",
    "activityId": "logistic-sensor-transfer",
    "activityPrompt": "A quality-control model predicts whether a part is defective. For one part its defect logit is ln(1/4). Derive p(defect) and p(no defect), place the defect probability relative to 0.5, and explain why observing a defect produces the larger log loss.",
    "activityGuidance": "Use exp(-ln(1/4)) = 4, compute both class probabilities, and compare the probability assigned to each possible observed target.",
    "criteria": [
      {
        "id": "transfer-complement",
        "label": "compute defect probability 0.2 and no-defect probability 0.8"
      },
      {
        "id": "transfer-boundary",
        "label": "place 0.2 below the 0.5 boundary"
      },
      {
        "id": "transfer-loss-comparison",
        "label": "explain that a defect receives the smaller assigned probability"
      }
    ],
    "demonstratedFeedback": "You transferred sigmoid and target-dependent loss to a new domain, sign, and probability pair.",
    "unsupportedFeedback": "Compute both class probabilities first, then ask which probability the loss reads after each possible outcome."
  },
  {
    "lessonId": "decision-costs",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "A probability becomes a decision",
    "lessonContext": "Turn fixed probabilities into accountable decisions\nA classifier can produce a probability without making an operational decision. A decision threshold is the rule that converts the probability into a predicted class. Under the convention used in this lesson, a score at or above the threshold becomes positive and a lower score becomes negative. Moving the threshold changes decisions even when every model score stays fixed.\n\nTurn fixed probabilities into accountable decisions\nEach decision is compared with the observed target and placed in one of four confusion-matrix cells. A true positive and true negative are correct. A false positive calls a negative case positive, while a false negative calls a positive case negative. Precision asks how reliable the positive decisions were; recall asks how many actual positive cases were recovered. Their denominators differ, so they answer different questions.\n\nTurn fixed probabilities into accountable decisions\nThe preferred threshold depends on the cost of mistakes, not on a universal rule that 0.5 is best. Threshold selection should use validation cases while the final test set remains untouched. Calibration is a separate property: among cases assigned a probability such as 0.8, about 80 percent should actually be positive under the evaluation conditions. A model can rank cases well without its probability values having that frequency meaning.\n\nTerms you need · Decision threshold\nThe cutoff that converts a numeric score or probability into a positive or negative decision.\n\nTerms you need · Confusion matrix\nThe four counts formed by crossing predicted class with observed class: TP, FP, TN, and FN.\n\nTerms you need · Precision\nTP / (TP + FP), the fraction of positive decisions that were truly positive.\n\nTerms you need · Recall\nTP / (TP + FN), the fraction of actual positive cases that the policy found.\n\nTerms you need · Decision cost\nA stated consequence assigned to an error type and used to compare candidate operating policies.\n\nTerms you need · Calibration\nAgreement between predicted probabilities and observed outcome frequencies across comparable cases.\n\nAudit one threshold on five validation cases\nUse scores [0.9, 0.7, 0.6, 0.4, 0.2], targets [1, 0, 1, 0, 1], threshold 0.5, false-positive cost 1, and false-negative cost 5.\n\nAudit one threshold on five validation cases · Create the decisions\nScores at or above 0.5 become positive, giving predictions [1, 1, 1, 0, 0]. The scores themselves have not changed.\n\nAudit one threshold on five validation cases · Place every case\nThe five prediction-target pairs are TP, FP, TP, TN, and FN. Therefore TP = 2, FP = 1, TN = 1, and FN = 1.\n\nAudit one threshold on five validation cases · Compute precision\nThere are three positive decisions and two are true positives, so precision = 2 / (2 + 1) = 2/3, about 0.667.\n\nAudit one threshold on five validation cases · Compute recall\nThere are three actual positives and two were found, so recall = 2 / (2 + 1) = 2/3, about 0.667.\n\nAudit one threshold on five validation cases · Compute empirical cost\nThe validation cost is 1 x FP + 5 x FN = 1 x 1 + 5 x 1 = 6 units. Other candidate thresholds must be evaluated on the same fixed cases and costs.\n\nAudit one threshold on five validation cases\nA threshold is a policy choice whose consequences can be counted. Precision, recall, and cost are derived from the same four cells, but they summarize those cells for different purposes.\n\nCommon confusions\nChanging only the threshold leaves all learned parameters and probabilities fixed; it changes the labels assigned by the policy.\n\nCommon confusions\nRanking concerns order, while calibration compares probability values with observed frequencies. One does not establish the other.\n\nCommon confusions\nRecall ignores false positives. A defensible operating point must consider stated costs and constraints on fixed validation data.\n\nBefore you predict\nWhen a threshold changes, begin by listing which fixed scores are now called positive.\n\nBefore you predict\nRebuild the four confusion counts before calculating any metric or cost.\n\nBefore you predict\nKeep score ordering, probability calibration, and the final decision policy as three separate questions.\n\nThe model score and policy are different objects\nA model can output the same probability for an example while two applications make different decisions. The threshold belongs to the decision policy, not to the target and not necessarily to training.\nLowering a threshold makes more examples positive. Raising it makes fewer examples positive. That directional fact holds before any metric is computed.\n\nEvery decision enters one of four cells\nA true positive and true negative are correct decisions. A false positive is a negative target called positive; a false negative is a positive target called negative.\nThe four counts describe decisions at one threshold. If the threshold changes, examples may move between cells even though their scores and targets stay fixed.\n\nPrecision and recall ask different questions\nPrecision = TP / (TP + FP): among positive decisions, how many were truly positive? Recall = TP / (TP + FN): among actual positives, how many did the policy recover?\nIf TP + FP is zero, there were no positive decisions, so precision is undefined; this lesson reports math.nan rather than inventing a measured zero. Recall is likewise undefined when TP + FN is zero.\nA lower threshold often raises recall and lowers precision because it admits more true positives and more false positives. This is a tradeoff, not a law for every finite dataset.\n\nRanking is not calibration\nRanking asks whether higher-scored examples tend to be more positive. Calibration asks whether examples assigned probability 0.7 are positive about 70 percent of the time under the evaluation conditions.\nA monotonic transformation can preserve ranking while changing calibration. A threshold can then produce the same ordering but a different operating point.\n\nCosts make the operating point concrete\nIf a false negative costs 10 units and a false positive costs 1, the empirical total validation cost is 10 x FN + 1 x FP. The best tested threshold is the one with the lowest total across the fixed validation cases.\nThe test set remains untouched while the threshold is chosen. Otherwise the reported test cost becomes part of tuning rather than an independent estimate.",
    "activityId": "decision-metrics-explanation",
    "activityPrompt": "Explain why moving a threshold can change precision and recall without changing model probabilities. Then distinguish a high ranking score from a calibrated probability.",
    "activityGuidance": "Keep the fixed score list, thresholded labels, confusion counts, metric denominators, and observed frequencies separate.",
    "criteria": [
      {
        "id": "fixed-probabilities",
        "label": "state that probabilities remain fixed when only threshold moves"
      },
      {
        "id": "labels-change",
        "label": "connect threshold movement to changed positive labels"
      },
      {
        "id": "metrics-from-counts",
        "label": "connect precision and recall to different confusion counts"
      },
      {
        "id": "calibration-frequency",
        "label": "define calibration through predicted and observed frequencies"
      }
    ],
    "demonstratedFeedback": "You separated model output, decision policy, metric counts, and calibration.",
    "unsupportedFeedback": "Explain the chain in two layers: fixed probabilities become changing labels, while calibration compares probabilities with observed frequencies."
  },
  {
    "lessonId": "decision-costs",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "A probability becomes a decision",
    "lessonContext": "Turn fixed probabilities into accountable decisions\nA classifier can produce a probability without making an operational decision. A decision threshold is the rule that converts the probability into a predicted class. Under the convention used in this lesson, a score at or above the threshold becomes positive and a lower score becomes negative. Moving the threshold changes decisions even when every model score stays fixed.\n\nTurn fixed probabilities into accountable decisions\nEach decision is compared with the observed target and placed in one of four confusion-matrix cells. A true positive and true negative are correct. A false positive calls a negative case positive, while a false negative calls a positive case negative. Precision asks how reliable the positive decisions were; recall asks how many actual positive cases were recovered. Their denominators differ, so they answer different questions.\n\nTurn fixed probabilities into accountable decisions\nThe preferred threshold depends on the cost of mistakes, not on a universal rule that 0.5 is best. Threshold selection should use validation cases while the final test set remains untouched. Calibration is a separate property: among cases assigned a probability such as 0.8, about 80 percent should actually be positive under the evaluation conditions. A model can rank cases well without its probability values having that frequency meaning.\n\nTerms you need · Decision threshold\nThe cutoff that converts a numeric score or probability into a positive or negative decision.\n\nTerms you need · Confusion matrix\nThe four counts formed by crossing predicted class with observed class: TP, FP, TN, and FN.\n\nTerms you need · Precision\nTP / (TP + FP), the fraction of positive decisions that were truly positive.\n\nTerms you need · Recall\nTP / (TP + FN), the fraction of actual positive cases that the policy found.\n\nTerms you need · Decision cost\nA stated consequence assigned to an error type and used to compare candidate operating policies.\n\nTerms you need · Calibration\nAgreement between predicted probabilities and observed outcome frequencies across comparable cases.\n\nAudit one threshold on five validation cases\nUse scores [0.9, 0.7, 0.6, 0.4, 0.2], targets [1, 0, 1, 0, 1], threshold 0.5, false-positive cost 1, and false-negative cost 5.\n\nAudit one threshold on five validation cases · Create the decisions\nScores at or above 0.5 become positive, giving predictions [1, 1, 1, 0, 0]. The scores themselves have not changed.\n\nAudit one threshold on five validation cases · Place every case\nThe five prediction-target pairs are TP, FP, TP, TN, and FN. Therefore TP = 2, FP = 1, TN = 1, and FN = 1.\n\nAudit one threshold on five validation cases · Compute precision\nThere are three positive decisions and two are true positives, so precision = 2 / (2 + 1) = 2/3, about 0.667.\n\nAudit one threshold on five validation cases · Compute recall\nThere are three actual positives and two were found, so recall = 2 / (2 + 1) = 2/3, about 0.667.\n\nAudit one threshold on five validation cases · Compute empirical cost\nThe validation cost is 1 x FP + 5 x FN = 1 x 1 + 5 x 1 = 6 units. Other candidate thresholds must be evaluated on the same fixed cases and costs.\n\nAudit one threshold on five validation cases\nA threshold is a policy choice whose consequences can be counted. Precision, recall, and cost are derived from the same four cells, but they summarize those cells for different purposes.\n\nCommon confusions\nChanging only the threshold leaves all learned parameters and probabilities fixed; it changes the labels assigned by the policy.\n\nCommon confusions\nRanking concerns order, while calibration compares probability values with observed frequencies. One does not establish the other.\n\nCommon confusions\nRecall ignores false positives. A defensible operating point must consider stated costs and constraints on fixed validation data.\n\nBefore you predict\nWhen a threshold changes, begin by listing which fixed scores are now called positive.\n\nBefore you predict\nRebuild the four confusion counts before calculating any metric or cost.\n\nBefore you predict\nKeep score ordering, probability calibration, and the final decision policy as three separate questions.\n\nThe model score and policy are different objects\nA model can output the same probability for an example while two applications make different decisions. The threshold belongs to the decision policy, not to the target and not necessarily to training.\nLowering a threshold makes more examples positive. Raising it makes fewer examples positive. That directional fact holds before any metric is computed.\n\nEvery decision enters one of four cells\nA true positive and true negative are correct decisions. A false positive is a negative target called positive; a false negative is a positive target called negative.\nThe four counts describe decisions at one threshold. If the threshold changes, examples may move between cells even though their scores and targets stay fixed.\n\nPrecision and recall ask different questions\nPrecision = TP / (TP + FP): among positive decisions, how many were truly positive? Recall = TP / (TP + FN): among actual positives, how many did the policy recover?\nIf TP + FP is zero, there were no positive decisions, so precision is undefined; this lesson reports math.nan rather than inventing a measured zero. Recall is likewise undefined when TP + FN is zero.\nA lower threshold often raises recall and lowers precision because it admits more true positives and more false positives. This is a tradeoff, not a law for every finite dataset.\n\nRanking is not calibration\nRanking asks whether higher-scored examples tend to be more positive. Calibration asks whether examples assigned probability 0.7 are positive about 70 percent of the time under the evaluation conditions.\nA monotonic transformation can preserve ranking while changing calibration. A threshold can then produce the same ordering but a different operating point.\n\nCosts make the operating point concrete\nIf a false negative costs 10 units and a false positive costs 1, the empirical total validation cost is 10 x FN + 1 x FP. The best tested threshold is the one with the lowest total across the fixed validation cases.\nThe test set remains untouched while the threshold is chosen. Otherwise the reported test cost becomes part of tuning rather than an independent estimate.",
    "activityId": "decision-fire-transfer",
    "activityPrompt": "A wildfire alert system sends a crew to inspect predicted hotspots. A missed fire costs 20 units; an unnecessary inspection costs 2. Explain which threshold direction you would test first, which confusion cells drive cost, and how you would check whether predicted 0.8 risks are calibrated before deployment.",
    "activityGuidance": "Name the expensive error, the expected directional tradeoff, the cost expression, and an observed-frequency calibration check.",
    "criteria": [
      {
        "id": "fire-lower-threshold",
        "label": "test a lower threshold to reduce missed fires"
      },
      {
        "id": "fire-cost-cells",
        "label": "weight false negatives by 20 and false positives by 2"
      },
      {
        "id": "fire-tradeoff",
        "label": "acknowledge that false positives may increase"
      },
      {
        "id": "fire-calibration",
        "label": "compare 0.8 predictions with an observed rate near 80 percent"
      }
    ],
    "demonstratedFeedback": "You chose and justified an operating point without confusing ranking, calibration, or test-set evaluation.",
    "unsupportedFeedback": "Start with the 20-to-2 cost ratio, identify the corresponding confusion cells, then define what 0.8 should mean across many cases."
  },
  {
    "lessonId": "feature-pipeline",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Features need a reproducible path",
    "lessonContext": "Build one feature definition and replay it everywhere\nModels need numeric feature vectors, but raw rows may contain measurements in different units, named categories, and missing values. Preprocessing turns those raw fields into model inputs. Some preprocessing operations learn values from data, such as a mean, scale, imputation value, or category vocabulary. Those learned values are called fitted state.\n\nBuild one feature definition and replay it everywhere\nFit and transform are different operations. Fit estimates preprocessing state from training rows. Transform applies already fitted state to a row without changing it. Validation, test, and future rows may be transformed, but they must not influence the fitted state. Otherwise information from held-out data changes the representation used by the model, which is a form of leakage.\n\nBuild one feature definition and replay it everywhere\nA pipeline records the ordered transformation steps, their fitted state, and the positions of the resulting features. Numeric values can be imputed and scaled; categories can be assigned stable one-hot positions; a missing indicator can preserve the fact that a raw value was absent. Replaying the same fitted pipeline gives the same raw row the same feature meaning across training and deployment.\n\nTerms you need · Fitted state\nValues learned by preprocessing from data, such as a mean, scale, fill value, or category vocabulary.\n\nTerms you need · Fit\nThe operation that estimates transformation state, using training rows only.\n\nTerms you need · Transform\nThe operation that applies frozen fitted state to rows without estimating it again.\n\nTerms you need · Imputation\nReplacing a missing input with a defined value, such as the mean computed from observed training values.\n\nTerms you need · One-hot encoding\nRepresenting each known category with its own position, containing one 1 and zeros elsewhere.\n\nTerms you need · Pipeline\nAn ordered contract that stores transformations, fitted state, and stable output feature positions.\n\nFit on three rows, then transform a new night row\nTraining rows contain temperatures [10, missing, 30] and shifts [day, night, day]. Transform a complete new row [40, night] without refitting.\n\nFit on three rows, then transform a new night row · Fit the numeric fill value\nUse only observed training temperatures: (10 + 30) / 2 = 20. The stored imputation value is 20.\n\nFit on three rows, then transform a new night row · Prepare the training numeric column\nImputation changes the training values to [10, 20, 30]. A separate missing indicator is [0, 1, 0], preserving which raw row lacked a temperature.\n\nFit on three rows, then transform a new night row · Fit the numeric reference\nThe imputed values [10, 20, 30] differ from mean 20 by [-10, 0, 10]. Their squared deviations are [100, 0, 100], whose mean is population variance (100 + 0 + 100) / 3 = 200 / 3. The fitted scale is its square root, about 8.165.\n\nFit on three rows, then transform a new night row · Fit stable category positions\nThe training vocabulary [day, night] assigns day to [1, 0] and night to [0, 1]. Those positions cannot move when a later row arrives.\n\nFit on three rows, then transform a new night row · Transform the new row\nTemperature 40 becomes (40 - 20) / 8.165, about 2.449. It was observed, so its missing indicator is 0; night becomes [day = 0, night = 1]. In the fixed order [scaled temperature, missing-temperature indicator, day, night], the final vector is [2.449, 0, 0, 1].\n\nFit on three rows, then transform a new night row\nThe new row contributes values to its output vector but contributes nothing to fitted state. That one-way boundary is what keeps preprocessing reproducible and held-out evaluation meaningful.\n\nCommon confusions\nValidation features still contain held-out information. Letting them alter a mean or scale changes the representation before evaluation.\n\nCommon confusions\nImputation supplies a usable numeric placeholder. A separate indicator records absence; neither operation recovers the unknown measurement.\n\nCommon confusions\nOrdinary numeric codes introduce an order and distance. One-hot positions let the model assign separate effects without inventing that geometry.\n\nBefore you predict\nFor every preprocessing step, ask whether it learns state or only applies state.\n\nBefore you predict\nAny learned mean, scale, fill value, or vocabulary belongs to the training split.\n\nBefore you predict\nHeld-out rows travel through the frozen pipeline in the same order and cannot redefine its feature positions.\n\nFit once, transform many times\nSome preprocessing learns state: a mean, a scale, a category vocabulary, or an imputation value. That state must be fitted using training rows only.\nTransform then applies the frozen state to training, validation, test, and future rows. Re-fitting on each split silently gives the model a different input definition.\n\nScaling records a reference system\nIn the shared lesson fixture, training temperatures [10, missing, 30] use a training-mean imputation of 20. The fitted population scale is sqrt(200 / 3), about 8.165, so a complete value of 40 becomes about 2.449 after scaling.\nScaling does not create information. It changes numeric geometry so a distance or penalty does not become dominated merely by a larger unit.\n\nCategories become stable positions\nA training vocabulary [day, night] can become two indicator positions. Day maps to [1, 0], night to [0, 1], and an unseen category needs an authored policy such as an all-zero unknown encoding.\nInteger codes such as day = 1 and night = 2 invent an ordering and distance that the raw categories did not contain.\n\nMissing is a condition, not a number\nReplacing a missing numeric value with the training mean supplies a usable number, but it does not claim the measurement was actually average. A separate binary missing indicator marks only that the field was absent in the raw row; it does not recover the measurement or explain why it was absent.\nThe reason for missingness may differ across environments. Both the imputation state and the missing rate should therefore remain visible.\n\nA pipeline is an ordered contract\nA pipeline fixes transformation order, learned state, output positions, and model application. Feature crosses are built from already defined values, such as scaled temperature multiplied by a night-shift indicator.\nThe same raw row must produce the same vector under the same fitted pipeline revision. This reproducibility is what makes debugging and deployment comparisons possible.",
    "activityId": "pipeline-mechanism-explanation",
    "activityPrompt": "Explain why a scaler mean and imputation value fitted on all rows leak information, while applying training-fitted values to a held-out row does not.",
    "activityGuidance": "Separate learning transformation state from using already frozen state.",
    "criteria": [
      {
        "id": "pipeline-fit-state",
        "label": "identify mean and imputation value as fitted state"
      },
      {
        "id": "pipeline-training-only",
        "label": "fit state on training rows only"
      },
      {
        "id": "pipeline-leak-definition",
        "label": "explain that held-out values would influence the representation"
      },
      {
        "id": "pipeline-transform-safe",
        "label": "apply frozen training state without refitting"
      }
    ],
    "demonstratedFeedback": "You separated learning preprocessing state from replaying that state on unseen rows.",
    "unsupportedFeedback": "Ask which operation estimates a value and which operation only substitutes that already estimated value."
  },
  {
    "lessonId": "feature-pipeline",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Features need a reproducible path",
    "lessonContext": "Build one feature definition and replay it everywhere\nModels need numeric feature vectors, but raw rows may contain measurements in different units, named categories, and missing values. Preprocessing turns those raw fields into model inputs. Some preprocessing operations learn values from data, such as a mean, scale, imputation value, or category vocabulary. Those learned values are called fitted state.\n\nBuild one feature definition and replay it everywhere\nFit and transform are different operations. Fit estimates preprocessing state from training rows. Transform applies already fitted state to a row without changing it. Validation, test, and future rows may be transformed, but they must not influence the fitted state. Otherwise information from held-out data changes the representation used by the model, which is a form of leakage.\n\nBuild one feature definition and replay it everywhere\nA pipeline records the ordered transformation steps, their fitted state, and the positions of the resulting features. Numeric values can be imputed and scaled; categories can be assigned stable one-hot positions; a missing indicator can preserve the fact that a raw value was absent. Replaying the same fitted pipeline gives the same raw row the same feature meaning across training and deployment.\n\nTerms you need · Fitted state\nValues learned by preprocessing from data, such as a mean, scale, fill value, or category vocabulary.\n\nTerms you need · Fit\nThe operation that estimates transformation state, using training rows only.\n\nTerms you need · Transform\nThe operation that applies frozen fitted state to rows without estimating it again.\n\nTerms you need · Imputation\nReplacing a missing input with a defined value, such as the mean computed from observed training values.\n\nTerms you need · One-hot encoding\nRepresenting each known category with its own position, containing one 1 and zeros elsewhere.\n\nTerms you need · Pipeline\nAn ordered contract that stores transformations, fitted state, and stable output feature positions.\n\nFit on three rows, then transform a new night row\nTraining rows contain temperatures [10, missing, 30] and shifts [day, night, day]. Transform a complete new row [40, night] without refitting.\n\nFit on three rows, then transform a new night row · Fit the numeric fill value\nUse only observed training temperatures: (10 + 30) / 2 = 20. The stored imputation value is 20.\n\nFit on three rows, then transform a new night row · Prepare the training numeric column\nImputation changes the training values to [10, 20, 30]. A separate missing indicator is [0, 1, 0], preserving which raw row lacked a temperature.\n\nFit on three rows, then transform a new night row · Fit the numeric reference\nThe imputed values [10, 20, 30] differ from mean 20 by [-10, 0, 10]. Their squared deviations are [100, 0, 100], whose mean is population variance (100 + 0 + 100) / 3 = 200 / 3. The fitted scale is its square root, about 8.165.\n\nFit on three rows, then transform a new night row · Fit stable category positions\nThe training vocabulary [day, night] assigns day to [1, 0] and night to [0, 1]. Those positions cannot move when a later row arrives.\n\nFit on three rows, then transform a new night row · Transform the new row\nTemperature 40 becomes (40 - 20) / 8.165, about 2.449. It was observed, so its missing indicator is 0; night becomes [day = 0, night = 1]. In the fixed order [scaled temperature, missing-temperature indicator, day, night], the final vector is [2.449, 0, 0, 1].\n\nFit on three rows, then transform a new night row\nThe new row contributes values to its output vector but contributes nothing to fitted state. That one-way boundary is what keeps preprocessing reproducible and held-out evaluation meaningful.\n\nCommon confusions\nValidation features still contain held-out information. Letting them alter a mean or scale changes the representation before evaluation.\n\nCommon confusions\nImputation supplies a usable numeric placeholder. A separate indicator records absence; neither operation recovers the unknown measurement.\n\nCommon confusions\nOrdinary numeric codes introduce an order and distance. One-hot positions let the model assign separate effects without inventing that geometry.\n\nBefore you predict\nFor every preprocessing step, ask whether it learns state or only applies state.\n\nBefore you predict\nAny learned mean, scale, fill value, or vocabulary belongs to the training split.\n\nBefore you predict\nHeld-out rows travel through the frozen pipeline in the same order and cannot redefine its feature positions.\n\nFit once, transform many times\nSome preprocessing learns state: a mean, a scale, a category vocabulary, or an imputation value. That state must be fitted using training rows only.\nTransform then applies the frozen state to training, validation, test, and future rows. Re-fitting on each split silently gives the model a different input definition.\n\nScaling records a reference system\nIn the shared lesson fixture, training temperatures [10, missing, 30] use a training-mean imputation of 20. The fitted population scale is sqrt(200 / 3), about 8.165, so a complete value of 40 becomes about 2.449 after scaling.\nScaling does not create information. It changes numeric geometry so a distance or penalty does not become dominated merely by a larger unit.\n\nCategories become stable positions\nA training vocabulary [day, night] can become two indicator positions. Day maps to [1, 0], night to [0, 1], and an unseen category needs an authored policy such as an all-zero unknown encoding.\nInteger codes such as day = 1 and night = 2 invent an ordering and distance that the raw categories did not contain.\n\nMissing is a condition, not a number\nReplacing a missing numeric value with the training mean supplies a usable number, but it does not claim the measurement was actually average. A separate binary missing indicator marks only that the field was absent in the raw row; it does not recover the measurement or explain why it was absent.\nThe reason for missingness may differ across environments. Both the imputation state and the missing rate should therefore remain visible.\n\nA pipeline is an ordered contract\nA pipeline fixes transformation order, learned state, output positions, and model application. Feature crosses are built from already defined values, such as scaled temperature multiplied by a night-shift indicator.\nThe same raw row must produce the same vector under the same fitted pipeline revision. This reproducibility is what makes debugging and deployment comparisons possible.",
    "activityId": "pipeline-maintenance-transfer",
    "activityPrompt": "A maintenance model receives vibration, machine type, and operator shift. Validation contains a new machine type, and some vibration readings are missing. Specify a leak-free transformation path, including the unknown category, missing indicator, and when any feature cross is computed.",
    "activityGuidance": "Describe fit state, transform order, stable output positions, and an explicit unknown-category policy.",
    "criteria": [
      {
        "id": "maintenance-training-fit",
        "label": "fit numeric and categorical state on training rows"
      },
      {
        "id": "maintenance-unknown",
        "label": "use an explicit unknown-category representation"
      },
      {
        "id": "maintenance-missing",
        "label": "impute vibration and preserve a missing indicator"
      },
      {
        "id": "maintenance-cross-order",
        "label": "compute a cross after its component values are defined"
      },
      {
        "id": "maintenance-replay",
        "label": "replay identical state and feature positions on validation"
      }
    ],
    "demonstratedFeedback": "You designed a stable feature contract for missing and previously unseen inputs.",
    "unsupportedFeedback": "List what the training split learns, then walk one validation row through the frozen steps in order."
  },
  {
    "lessonId": "knn-versus-tree",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Two models draw different neighborhoods",
    "lessonContext": "Compare a neighborhood vote with a learned rule path\nA model contains assumptions about which patterns should be easy to learn. These assumptions are called inductive bias. k-nearest neighbors, or kNN, assumes that nearby examples often share a target. It stores labeled training points and waits until a new query arrives before computing distances, selecting the closest k points, and combining their labels.\n\nCompare a neighborhood vote with a learned rule path\nA decision tree learns a different structure. During fitting, it chooses feature-threshold questions that divide training rows into branches. During prediction, a query follows those questions until it reaches a leaf, which returns a prediction. A standard numeric split examines one feature at a time, so repeated splits form axis-aligned, block-like regions.\n\nCompare a neighborhood vote with a learned rule path\nNeither assumption is universally better. kNN depends directly on the distance definition, feature scales, and chosen k. A tree depends on which splits were learned and how deeply it was allowed to grow. A small k or deep tree can follow training detail closely; a larger k or shallow tree can smooth over real detail. Compare them with the same preprocessing, split, and held-out criterion.\n\nTerms you need · Query point\nThe new example whose target the fitted model must predict.\n\nTerms you need · k-nearest neighbors\nA predictor that finds the k closest stored training examples and combines their labels.\n\nTerms you need · Distance\nA numeric measure of separation between feature vectors, meaningful only relative to the chosen representation and scale.\n\nTerms you need · Decision tree\nA predictor that routes an example through learned feature-threshold questions to a leaf.\n\nTerms you need · Leaf\nA terminal tree region that returns a class or numeric prediction.\n\nTerms you need · Inductive bias\nThe structural preference a learning method uses to extend from training examples to unseen cases.\n\nSend one query through both models\nTraining points are (1, class 0), (3, class 0), (6, class 1), and (8, class 1). Predict query x = 5 with k = 3, then with a fitted tree split x < 5.5 whose left leaf predicts 0 and right leaf predicts 1.\n\nSend one query through both models · Measure every kNN distance\nThe absolute distances from x = 5 are 4 to x = 1, 2 to x = 3, 1 to x = 6, and 3 to x = 8.\n\nSend one query through both models · Select exactly k neighbors\nSorting by distance gives x = 6 with class 1, x = 3 with class 0, and x = 8 with class 1 as the closest three.\n\nSend one query through both models · Take the neighbor vote\nThe selected labels are [1, 0, 1]. Two of three are class 1, so this kNN configuration predicts class 1.\n\nSend one query through both models · Route through the tree\nThe tree asks whether 5 < 5.5. The answer is yes, so the query follows the left branch to the leaf that predicts class 0.\n\nSend one query through both models · Explain the disagreement\nkNN used a local majority around the query. The tree used one previously learned threshold region. Different inductive biases can therefore assign different labels to the same input.\n\nSend one query through both models\nA prediction is not just a label: it has a trace. For kNN, inspect distances, selected neighbors, and votes. For a tree, inspect threshold questions, branch directions, and the reached leaf.\n\nCommon confusions\nkNN mainly stores labeled examples; the neighborhood calculation happens when a query is predicted.\n\nCommon confusions\nA larger unit can dominate distance without being more informative. Scaling defines a comparable numeric geometry before kNN is used.\n\nCommon confusions\nA deep tree can isolate individual rows. Its usefulness must be checked on previously unseen data under the same evaluation protocol.\n\nBefore you predict\nFor kNN, write the distance to every candidate before selecting the closest k.\n\nBefore you predict\nFor a tree, evaluate one threshold at a time and follow only the resulting branch.\n\nBefore you predict\nWhen predictions differ, explain the local-neighborhood assumption and threshold-partition assumption before judging either model.\n\nkNN predicts from stored examples\nk-nearest neighbors stores the labeled training examples. For a new point, it computes distances, selects the k closest examples, and combines their labels.\nThe method assumes that nearby points should often have similar targets. That local-similarity assumption is its central inductive bias.\n\nk and scale change the neighborhood\nA small k follows local detail and can react strongly to one mislabeled point. A larger k averages over a wider region and can erase a small but real cluster.\nDistance depends on feature scale. If age ranges over tens and income over hundreds of thousands, unscaled income can dominate which points count as near.\n\nA tree predicts by routing\nA decision tree asks a sequence of questions such as temperature < 18 or pressure >= 4.5. Each split partitions the current rows, and a leaf returns a prediction.\nA standard numeric split is axis-aligned: one feature and one threshold at a time. Repeated splits create rectangular regions in feature space.\n\nDepth controls partition detail\nA shallow tree has few large regions and may miss a real interaction. A deep tree can isolate individual training examples and become unstable when the sample changes.\nUnlike kNN, a fitted tree stores split rules rather than consulting every training point for each prediction. Both can overfit, but through different mechanisms.\n\nModel choice is a claim about structure\nkNN favors local similarity under a chosen distance. A tree favors a hierarchy of threshold rules. Neither bias is universally correct.\nCompare them under the same preprocessing, splits, and metric. A more flexible boundary on training data is not enough; the choice must survive validation and transfer to new cases.",
    "activityId": "knn-tree-explanation",
    "activityPrompt": "Explain how kNN and a decision tree can assign different labels to the same point. Trace each computation and name the structural assumption responsible.",
    "activityGuidance": "For kNN, name distance, k, and voting. For the tree, name learned thresholds, routing, and leaves.",
    "criteria": [
      {
        "id": "knn-trace",
        "label": "trace kNN through distance, k closest points, and vote"
      },
      {
        "id": "tree-trace",
        "label": "trace the tree through threshold branches to a leaf"
      },
      {
        "id": "knn-bias",
        "label": "name local similarity as the kNN assumption"
      },
      {
        "id": "tree-bias",
        "label": "name hierarchical axis-aligned partitioning as the tree assumption"
      }
    ],
    "demonstratedFeedback": "You traced both predictors and made their different inductive biases explicit.",
    "unsupportedFeedback": "Describe the exact route to a label for each model before comparing their assumptions."
  },
  {
    "lessonId": "knn-versus-tree",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Two models draw different neighborhoods",
    "lessonContext": "Compare a neighborhood vote with a learned rule path\nA model contains assumptions about which patterns should be easy to learn. These assumptions are called inductive bias. k-nearest neighbors, or kNN, assumes that nearby examples often share a target. It stores labeled training points and waits until a new query arrives before computing distances, selecting the closest k points, and combining their labels.\n\nCompare a neighborhood vote with a learned rule path\nA decision tree learns a different structure. During fitting, it chooses feature-threshold questions that divide training rows into branches. During prediction, a query follows those questions until it reaches a leaf, which returns a prediction. A standard numeric split examines one feature at a time, so repeated splits form axis-aligned, block-like regions.\n\nCompare a neighborhood vote with a learned rule path\nNeither assumption is universally better. kNN depends directly on the distance definition, feature scales, and chosen k. A tree depends on which splits were learned and how deeply it was allowed to grow. A small k or deep tree can follow training detail closely; a larger k or shallow tree can smooth over real detail. Compare them with the same preprocessing, split, and held-out criterion.\n\nTerms you need · Query point\nThe new example whose target the fitted model must predict.\n\nTerms you need · k-nearest neighbors\nA predictor that finds the k closest stored training examples and combines their labels.\n\nTerms you need · Distance\nA numeric measure of separation between feature vectors, meaningful only relative to the chosen representation and scale.\n\nTerms you need · Decision tree\nA predictor that routes an example through learned feature-threshold questions to a leaf.\n\nTerms you need · Leaf\nA terminal tree region that returns a class or numeric prediction.\n\nTerms you need · Inductive bias\nThe structural preference a learning method uses to extend from training examples to unseen cases.\n\nSend one query through both models\nTraining points are (1, class 0), (3, class 0), (6, class 1), and (8, class 1). Predict query x = 5 with k = 3, then with a fitted tree split x < 5.5 whose left leaf predicts 0 and right leaf predicts 1.\n\nSend one query through both models · Measure every kNN distance\nThe absolute distances from x = 5 are 4 to x = 1, 2 to x = 3, 1 to x = 6, and 3 to x = 8.\n\nSend one query through both models · Select exactly k neighbors\nSorting by distance gives x = 6 with class 1, x = 3 with class 0, and x = 8 with class 1 as the closest three.\n\nSend one query through both models · Take the neighbor vote\nThe selected labels are [1, 0, 1]. Two of three are class 1, so this kNN configuration predicts class 1.\n\nSend one query through both models · Route through the tree\nThe tree asks whether 5 < 5.5. The answer is yes, so the query follows the left branch to the leaf that predicts class 0.\n\nSend one query through both models · Explain the disagreement\nkNN used a local majority around the query. The tree used one previously learned threshold region. Different inductive biases can therefore assign different labels to the same input.\n\nSend one query through both models\nA prediction is not just a label: it has a trace. For kNN, inspect distances, selected neighbors, and votes. For a tree, inspect threshold questions, branch directions, and the reached leaf.\n\nCommon confusions\nkNN mainly stores labeled examples; the neighborhood calculation happens when a query is predicted.\n\nCommon confusions\nA larger unit can dominate distance without being more informative. Scaling defines a comparable numeric geometry before kNN is used.\n\nCommon confusions\nA deep tree can isolate individual rows. Its usefulness must be checked on previously unseen data under the same evaluation protocol.\n\nBefore you predict\nFor kNN, write the distance to every candidate before selecting the closest k.\n\nBefore you predict\nFor a tree, evaluate one threshold at a time and follow only the resulting branch.\n\nBefore you predict\nWhen predictions differ, explain the local-neighborhood assumption and threshold-partition assumption before judging either model.\n\nkNN predicts from stored examples\nk-nearest neighbors stores the labeled training examples. For a new point, it computes distances, selects the k closest examples, and combines their labels.\nThe method assumes that nearby points should often have similar targets. That local-similarity assumption is its central inductive bias.\n\nk and scale change the neighborhood\nA small k follows local detail and can react strongly to one mislabeled point. A larger k averages over a wider region and can erase a small but real cluster.\nDistance depends on feature scale. If age ranges over tens and income over hundreds of thousands, unscaled income can dominate which points count as near.\n\nA tree predicts by routing\nA decision tree asks a sequence of questions such as temperature < 18 or pressure >= 4.5. Each split partitions the current rows, and a leaf returns a prediction.\nA standard numeric split is axis-aligned: one feature and one threshold at a time. Repeated splits create rectangular regions in feature space.\n\nDepth controls partition detail\nA shallow tree has few large regions and may miss a real interaction. A deep tree can isolate individual training examples and become unstable when the sample changes.\nUnlike kNN, a fitted tree stores split rules rather than consulting every training point for each prediction. Both can overfit, but through different mechanisms.\n\nModel choice is a claim about structure\nkNN favors local similarity under a chosen distance. A tree favors a hierarchy of threshold rules. Neither bias is universally correct.\nCompare them under the same preprocessing, splits, and metric. A more flexible boundary on training data is not enough; the choice must survive validation and transfer to new cases.",
    "activityId": "knn-tree-dispatch-transfer",
    "activityPrompt": "An emergency dispatcher predicts whether a call needs a specialist team from response distance and symptom severity. Similar past calls form curved local clusters, but policy also requires an auditable rule. Compare kNN and a shallow tree, including scaling, boundary shape, noise sensitivity, and the operational tradeoff.",
    "activityGuidance": "Do not name a universal winner. State what each model assumes and what evidence would decide.",
    "criteria": [
      {
        "id": "dispatch-scaling",
        "label": "scale distance and severity before kNN distance"
      },
      {
        "id": "dispatch-knn-shape",
        "label": "connect kNN to curved local clusters and noise sensitivity"
      },
      {
        "id": "dispatch-tree-shape",
        "label": "connect the tree to auditable threshold regions"
      },
      {
        "id": "dispatch-validation",
        "label": "choose under a common held-out protocol"
      }
    ],
    "demonstratedFeedback": "You compared the two biases against both data geometry and operational constraints.",
    "unsupportedFeedback": "Address the geometry first, then the human policy constraint, and finish with a held-out comparison."
  },
  {
    "lessonId": "regularization-path",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Control flexibility without touching the test set",
    "lessonContext": "Constrain fitting, compare settings, and protect the final test\nA flexible model can lower training loss by using large or finely tuned coefficients, yet those details may not repeat on new data. Regularization changes the fitting objective by adding a cost for coefficient size. The regularization strength, usually written lambda, controls how strongly that cost competes with fitting the training targets.\n\nConstrain fitting, compare settings, and protect the final test\nL2 regularization penalizes the sum of squared coefficients and generally spreads shrinkage across them. L1 regularization penalizes the sum of absolute coefficients and can make some fitted coefficients exactly zero. Neither pattern proves that a retained feature is causal or that a zeroed feature is useless. The penalty is a modeling preference, and lambda is a setting that must be selected from evidence.\n\nConstrain fitting, compare settings, and protect the final test\nCross-validation supplies that evidence by rotating a validation role through the available development data. For each candidate lambda, fit on all but one fold and evaluate on the held-out fold, repeating until every fold has been held out. Average the fold losses, select the setting by that predefined criterion, then freeze it. The separate final test remains unopened until preprocessing, model family, and lambda are fixed.\n\nTerms you need · Regularization\nA modification of the training objective that charges for coefficient complexity in addition to data loss.\n\nTerms you need · Lambda\nThe hyperparameter that sets the strength of the regularization penalty.\n\nTerms you need · L1 penalty\nLambda times the sum of absolute coefficient values, a shape that can produce exact zeros.\n\nTerms you need · L2 penalty\nLambda times the sum of squared coefficients, which encourages distributed shrinkage toward zero.\n\nTerms you need · Cross-validation fold\nOne partition that serves as validation while the remaining development partitions are used for fitting.\n\nTerms you need · Hyperparameter\nA setting such as lambda chosen around model fitting rather than optimized as a coefficient within one fit.\n\nChoose one lambda from three validation folds\nKeep the final test sealed. Three candidate lambdas have fold losses: 0.0 -> [0.52, 0.66, 0.61], 0.5 -> [0.48, 0.51, 0.50], and 2.0 -> [0.62, 0.57, 0.65]. Lower loss is better.\n\nChoose one lambda from three validation folds · Preserve comparable folds\nEvery candidate uses the same three held-out folds, and preprocessing is fitted again inside each corresponding training-fold set.\n\nChoose one lambda from three validation folds · Average lambda 0.0\nIts mean validation loss is (0.52 + 0.66 + 0.61) / 3 = 1.79 / 3, about 0.597.\n\nChoose one lambda from three validation folds · Average lambda 0.5\nIts mean is (0.48 + 0.51 + 0.50) / 3 = 1.49 / 3, about 0.497.\n\nChoose one lambda from three validation folds · Average lambda 2.0\nIts mean is (0.62 + 0.57 + 0.65) / 3 = 1.84 / 3, about 0.613.\n\nChoose one lambda from three validation folds · Select by the stated rule\nLambda 0.5 has the lowest mean fold loss, so it is selected. No test outcome participated in that comparison.\n\nChoose one lambda from three validation folds · Refit and evaluate once\nRefit the complete pipeline and regularized model on all development data with lambda fixed at 0.5, then use the final test once to estimate the frozen procedure.\n\nChoose one lambda from three validation folds\nRegularization controls fitting, while cross-validation controls selection. Keeping those roles distinct prevents the final test from quietly becoming another tuning fold.\n\nCommon confusions\nIt constrains fitting and can raise training loss. Validation loss may improve if reduced overfitting outweighs the lost flexibility, but that must be measured.\n\nCommon confusions\nThe selected pattern depends on penalty strength, sample, scaling, and correlated alternatives. A zero is a fitted model result, not a causal conclusion.\n\nCommon confusions\nRepeated test-guided changes tune to the test. Selection belongs inside development data, with the final test reserved for the frozen procedure.\n\nBefore you predict\nHold the data fit fixed in your reasoning and inspect how the penalty term changes as lambda changes.\n\nBefore you predict\nCompare candidate settings with all of their validation folds, not one convenient fold.\n\nBefore you predict\nSeparate coefficient fitting, hyperparameter selection, final refitting, and one-time testing into distinct stages.\n\nPenalize complexity inside the training objective\nRegularized fitting minimizes data loss plus a coefficient penalty. The penalty changes which parameter values count as cheap even when two models fit training data similarly.\nThis lesson's manual ridge calculation uses one feature through the origin and exactly minimizes J_SSE(w) = sum_i (y_i - w * x_i)^2 + lambda_SSE * w^2. There is no fitted intercept in this specimen. Under this summed-squared-error convention, the solution is w = sum_i (x_i * y_i) / (sum_i x_i^2 + lambda_SSE).\nIf the data term is instead mean squared error, the objective is J_MSE(w) = (1/n) * sum_i (y_i - w * x_i)^2 + lambda_MSE * w^2, and the denominator becomes sum_i x_i^2 + n * lambda_MSE. The two objectives produce the same fit only when lambda_SSE = n * lambda_MSE, so the same printed lambda value does not mean the same penalty under both conventions.\nThe regularization strength lambda is a hyperparameter: training optimizes coefficients for a fixed lambda, while validation evidence helps choose lambda.\n\nL2 spreads shrinkage\nL2 adds lambda times the sum of squared coefficients. For one feature through the origin, the fitted weight becomes sum(xy) / (sum(x squared) + lambda).\nWith x = [1, 2] and y = [2, 4], the unregularized weight is 10/5 = 2. At lambda = 5 it becomes 10/10 = 1. Larger lambda pulls it toward zero.\n\nL1 can create exact zeros\nL1 adds lambda times the sum of absolute coefficients. Its sharp corner at zero can make some fitted coefficients exactly zero.\nA zero coefficient does not prove the feature is useless or causal. With correlated features, small data changes can alter which one L1 retains.\n\nCross-validation rotates the validation role\nIn k-fold cross-validation, split the available training data into k folds. Fit on k-1 folds and evaluate on the remaining fold, rotating until every fold has served as validation.\nEach candidate lambda must use the same folds and the complete fitted pipeline inside each training fold. Average validation loss estimates the selection criterion.\n\nSelection ends before final testing\nAfter selecting lambda, refit the pipeline and model on the full development data under that frozen setting. Evaluate the final test set once for an independent estimate.\nRepeatedly checking test performance while changing lambda turns the test set into another validation set. The optimistic result no longer answers how the frozen procedure handles untouched data.",
    "activityId": "regularization-cv-explanation",
    "activityPrompt": "Explain why stronger regularization can raise training loss yet lower cross-validation loss. Include how L1 and L2 differ and why lambda is not chosen on the test set.",
    "activityGuidance": "Connect coefficient flexibility to fit, fold-to-fold generalization, penalty shape, and the role of the untouched test.",
    "criteria": [
      {
        "id": "regularization-training",
        "label": "connect stronger penalty to smaller coefficients and constrained fit"
      },
      {
        "id": "regularization-generalization",
        "label": "explain the possible validation improvement through reduced overfit"
      },
      {
        "id": "regularization-l1-l2",
        "label": "distinguish L1 sparsity from L2 distributed shrinkage"
      },
      {
        "id": "regularization-test-role",
        "label": "reserve test data for the frozen selected procedure"
      }
    ],
    "demonstratedFeedback": "You connected penalty geometry, training fit, cross-validation, and final testing.",
    "unsupportedFeedback": "Explain separately what lambda does during fitting and what cross-validation does during selection."
  },
  {
    "lessonId": "regularization-path",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Control flexibility without touching the test set",
    "lessonContext": "Constrain fitting, compare settings, and protect the final test\nA flexible model can lower training loss by using large or finely tuned coefficients, yet those details may not repeat on new data. Regularization changes the fitting objective by adding a cost for coefficient size. The regularization strength, usually written lambda, controls how strongly that cost competes with fitting the training targets.\n\nConstrain fitting, compare settings, and protect the final test\nL2 regularization penalizes the sum of squared coefficients and generally spreads shrinkage across them. L1 regularization penalizes the sum of absolute coefficients and can make some fitted coefficients exactly zero. Neither pattern proves that a retained feature is causal or that a zeroed feature is useless. The penalty is a modeling preference, and lambda is a setting that must be selected from evidence.\n\nConstrain fitting, compare settings, and protect the final test\nCross-validation supplies that evidence by rotating a validation role through the available development data. For each candidate lambda, fit on all but one fold and evaluate on the held-out fold, repeating until every fold has been held out. Average the fold losses, select the setting by that predefined criterion, then freeze it. The separate final test remains unopened until preprocessing, model family, and lambda are fixed.\n\nTerms you need · Regularization\nA modification of the training objective that charges for coefficient complexity in addition to data loss.\n\nTerms you need · Lambda\nThe hyperparameter that sets the strength of the regularization penalty.\n\nTerms you need · L1 penalty\nLambda times the sum of absolute coefficient values, a shape that can produce exact zeros.\n\nTerms you need · L2 penalty\nLambda times the sum of squared coefficients, which encourages distributed shrinkage toward zero.\n\nTerms you need · Cross-validation fold\nOne partition that serves as validation while the remaining development partitions are used for fitting.\n\nTerms you need · Hyperparameter\nA setting such as lambda chosen around model fitting rather than optimized as a coefficient within one fit.\n\nChoose one lambda from three validation folds\nKeep the final test sealed. Three candidate lambdas have fold losses: 0.0 -> [0.52, 0.66, 0.61], 0.5 -> [0.48, 0.51, 0.50], and 2.0 -> [0.62, 0.57, 0.65]. Lower loss is better.\n\nChoose one lambda from three validation folds · Preserve comparable folds\nEvery candidate uses the same three held-out folds, and preprocessing is fitted again inside each corresponding training-fold set.\n\nChoose one lambda from three validation folds · Average lambda 0.0\nIts mean validation loss is (0.52 + 0.66 + 0.61) / 3 = 1.79 / 3, about 0.597.\n\nChoose one lambda from three validation folds · Average lambda 0.5\nIts mean is (0.48 + 0.51 + 0.50) / 3 = 1.49 / 3, about 0.497.\n\nChoose one lambda from three validation folds · Average lambda 2.0\nIts mean is (0.62 + 0.57 + 0.65) / 3 = 1.84 / 3, about 0.613.\n\nChoose one lambda from three validation folds · Select by the stated rule\nLambda 0.5 has the lowest mean fold loss, so it is selected. No test outcome participated in that comparison.\n\nChoose one lambda from three validation folds · Refit and evaluate once\nRefit the complete pipeline and regularized model on all development data with lambda fixed at 0.5, then use the final test once to estimate the frozen procedure.\n\nChoose one lambda from three validation folds\nRegularization controls fitting, while cross-validation controls selection. Keeping those roles distinct prevents the final test from quietly becoming another tuning fold.\n\nCommon confusions\nIt constrains fitting and can raise training loss. Validation loss may improve if reduced overfitting outweighs the lost flexibility, but that must be measured.\n\nCommon confusions\nThe selected pattern depends on penalty strength, sample, scaling, and correlated alternatives. A zero is a fitted model result, not a causal conclusion.\n\nCommon confusions\nRepeated test-guided changes tune to the test. Selection belongs inside development data, with the final test reserved for the frozen procedure.\n\nBefore you predict\nHold the data fit fixed in your reasoning and inspect how the penalty term changes as lambda changes.\n\nBefore you predict\nCompare candidate settings with all of their validation folds, not one convenient fold.\n\nBefore you predict\nSeparate coefficient fitting, hyperparameter selection, final refitting, and one-time testing into distinct stages.\n\nPenalize complexity inside the training objective\nRegularized fitting minimizes data loss plus a coefficient penalty. The penalty changes which parameter values count as cheap even when two models fit training data similarly.\nThis lesson's manual ridge calculation uses one feature through the origin and exactly minimizes J_SSE(w) = sum_i (y_i - w * x_i)^2 + lambda_SSE * w^2. There is no fitted intercept in this specimen. Under this summed-squared-error convention, the solution is w = sum_i (x_i * y_i) / (sum_i x_i^2 + lambda_SSE).\nIf the data term is instead mean squared error, the objective is J_MSE(w) = (1/n) * sum_i (y_i - w * x_i)^2 + lambda_MSE * w^2, and the denominator becomes sum_i x_i^2 + n * lambda_MSE. The two objectives produce the same fit only when lambda_SSE = n * lambda_MSE, so the same printed lambda value does not mean the same penalty under both conventions.\nThe regularization strength lambda is a hyperparameter: training optimizes coefficients for a fixed lambda, while validation evidence helps choose lambda.\n\nL2 spreads shrinkage\nL2 adds lambda times the sum of squared coefficients. For one feature through the origin, the fitted weight becomes sum(xy) / (sum(x squared) + lambda).\nWith x = [1, 2] and y = [2, 4], the unregularized weight is 10/5 = 2. At lambda = 5 it becomes 10/10 = 1. Larger lambda pulls it toward zero.\n\nL1 can create exact zeros\nL1 adds lambda times the sum of absolute coefficients. Its sharp corner at zero can make some fitted coefficients exactly zero.\nA zero coefficient does not prove the feature is useless or causal. With correlated features, small data changes can alter which one L1 retains.\n\nCross-validation rotates the validation role\nIn k-fold cross-validation, split the available training data into k folds. Fit on k-1 folds and evaluate on the remaining fold, rotating until every fold has served as validation.\nEach candidate lambda must use the same folds and the complete fitted pipeline inside each training fold. Average validation loss estimates the selection criterion.\n\nSelection ends before final testing\nAfter selecting lambda, refit the pipeline and model on the full development data under that frozen setting. Evaluate the final test set once for an independent estimate.\nRepeatedly checking test performance while changing lambda turns the test set into another validation set. The optimistic result no longer answers how the frozen procedure handles untouched data.",
    "activityId": "regularization-sensor-transfer",
    "activityPrompt": "A vibration model has 40 correlated sensor features and only 160 machines. Describe a defensible comparison of L1 and L2, including fold-local preprocessing, the evidence used to select lambda, what coefficient patterns you expect, and when the test set is opened.",
    "activityGuidance": "Account for correlation, pipeline fitting inside folds, repeated validation, and the final frozen evaluation.",
    "criteria": [
      {
        "id": "sensor-fold-pipeline",
        "label": "fit preprocessing inside each training fold"
      },
      {
        "id": "sensor-candidate-comparison",
        "label": "compare candidate lambdas using mean validation loss"
      },
      {
        "id": "sensor-patterns",
        "label": "expect L1 zeros and L2 shared shrinkage under correlation"
      },
      {
        "id": "sensor-test-once",
        "label": "open test data only after freezing model and lambda"
      }
    ],
    "demonstratedFeedback": "You transferred regularized selection to a small, correlated dataset without contaminating final evaluation.",
    "unsupportedFeedback": "Walk through one fold, then selection across folds, then the single final test."
  },
  {
    "lessonId": "ensemble-votes",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Many weak views can form one stronger model",
    "lessonContext": "Combine different learners for a specific reason\nAn ensemble combines predictions from multiple base learners. The combination helps only when the learners contribute meaningfully different errors or corrections. Repeating one deterministic model with identical data and settings merely repeats the same output. The design question is therefore not just how many models to train, but how they differ and how their outputs are combined.\n\nCombine different learners for a specific reason\nBagging creates parallel learners by fitting each one on a bootstrap sample: a same-sized sample drawn from training rows with replacement. Regression outputs are averaged and class outputs can be voted. This is especially useful for unstable learners such as deep trees, whose fitted structure can change when training rows change. A random forest adds random feature subsets at candidate splits to reduce similarity among its trees.\n\nCombine different learners for a specific reason\nBoosting uses a different timeline. It builds learners sequentially, with each new stage aimed at error left by the current ensemble. Under squared error, training first computes residual targets as target minus current prediction, then fits a small learner g(x) to those residuals. The update adds learning_rate times g(x), not each row's raw target or residual. Once fitted, g predicts from features at inference time without receiving the unknown target. Bagging and forests primarily target sample-driven instability, while boosting can reduce systematic underfit; validation is still required for either choice.\n\nTerms you need · Ensemble\nA predictor formed by combining outputs from multiple fitted base learners.\n\nTerms you need · Base learner\nOne component model whose output contributes to the ensemble.\n\nTerms you need · Bootstrap sample\nA same-sized training sample drawn with replacement, so rows can repeat and others can be absent.\n\nTerms you need · Bagging\nTraining diverse learners on bootstrap samples in parallel and aggregating their outputs.\n\nTerms you need · Random forest\nA tree ensemble that combines bootstrap variation with random feature subsets during split selection.\n\nTerms you need · Boosting\nBuilding an ensemble in sequence so each added learner addresses error remaining from earlier stages.\n\nTrace parallel averaging and sequential correction\nFor one regression case with target 12, three bootstrap-fitted trees predict 8, 11, and 14. Separately, a boosting model currently predicts 2 for a different case whose target is 4, with learning rate 0.5.\n\nTrace parallel averaging and sequential correction · Aggregate the bagged trees\nThe regression ensemble prediction is their mean: (8 + 11 + 14) / 3 = 33 / 3 = 11.\n\nTrace parallel averaging and sequential correction · Compare component errors\nUsing prediction minus target, the three errors are -4, -1, and 2. They are not identical, so averaging allows some disagreement to offset.\n\nTrace parallel averaging and sequential correction · Keep the bagging timeline parallel\nEach tree was fitted from its own bootstrap sample without receiving another tree's residual. Their interaction occurs only when outputs are aggregated.\n\nTrace parallel averaging and sequential correction · Compute the boosting residual\nFor the second case, residual target = target - current prediction = 4 - 2 = 2. During training, the next base learner g is fitted across all training rows to approximate their residual targets from features.\n\nTrace parallel averaging and sequential correction · Use the fitted learner's prediction\nAssume the fitted learner predicts g(x) = 1.5 for this case. The ensemble update is F_new(x) = F(x) + learning_rate * g(x) = 2 + 0.5(1.5) = 2.75. The correction can miss the raw residual because g is a constrained learner shared across rows.\n\nTrace parallel averaging and sequential correction · Separate training from inference\nTargets are used to create residual training targets and fit g. For a new case, the saved ensemble evaluates its already fitted learners from the new features; the unknown target is not an input to prediction.\n\nTrace parallel averaging and sequential correction\nBagging combines independently varied fits after training; boosting changes what the next learner is asked to correct. The arithmetic may involve addition in both cases, but the training procedures are different.\n\nCommon confusions\nIdentical outputs have perfectly aligned errors, so their average is unchanged. Stabilization requires useful diversity.\n\nCommon confusions\nIt aggregates many trees and deliberately varies both sampled rows and candidate features to reduce tree correlation.\n\nCommon confusions\nBoosting is sequential: training targets define residuals for fitting the next stage, but at inference each fitted stage predicts its scaled correction from features without the unknown target.\n\nBefore you predict\nBefore averaging, ask what created differences among the component learners.\n\nBefore you predict\nKeep parallel bootstrap fitting separate from sequential residual correction.\n\nBefore you predict\nMatch the proposed ensemble to observed instability or underfit, then require held-out evidence rather than assuming improvement.\n\nAggregation needs disagreement\nAveraging can stabilize predictions only when component errors are not perfectly aligned. Copying one deterministic model many times produces the same prediction and no variance reduction.\nAn ensemble therefore combines two ideas: create meaningfully different base learners, then aggregate or sequence their outputs.\n\nBagging trains in parallel\nBagging draws bootstrap samples from the training rows, fits one base learner per sample, then averages regression predictions or votes on classes.\nDeep trees are unstable: a changed sample can change early splits. Averaging many such trees can reduce this sample-driven variance while retaining flexible fits.\n\nRandom forests also vary the available features\nIf one strong feature dominates every tree, bootstrap trees can remain highly correlated. A random forest offers only a random subset of features at each candidate split.\nFeature subsampling can force different useful structures to appear, reducing correlation among trees. It may weaken individual trees while strengthening their average.\n\nBoosting trains in sequence\nBoosting adds a small learner that focuses on what the current ensemble still gets wrong. For squared error, training computes residual targets as target minus current prediction, then fits the next learner g(x) to approximate those residuals from features.\nThe updated ensemble is F_new(x) = F(x) + learning_rate times g(x). The fitted correction can differ from an individual row's raw residual because one constrained learner must serve multiple rows. At inference, the saved g(x) reads features, not the unknown target.\n\nName the failure before choosing the ensemble\nBagging and random forests primarily address unstable, high-variance learners. Boosting can reduce systematic underfit by composing many small corrections, though it also needs regularization and validation.\nThe labels variance and bias are diagnostic summaries, not guarantees. Compare learning curves, multi-seed variation, and held-out error under the same protocol.",
    "activityId": "ensemble-mechanism-explanation",
    "activityPrompt": "Explain why bootstrap trees can reduce variance, why random feature subsets may improve their average, and why boosting is not bagging.",
    "activityGuidance": "Contrast parallel resampling and aggregation with sequential error correction.",
    "criteria": [
      {
        "id": "bagging-bootstrap",
        "label": "connect bootstrap samples to different parallel trees"
      },
      {
        "id": "bagging-variance",
        "label": "connect averaging imperfectly correlated trees to lower variance"
      },
      {
        "id": "forest-features",
        "label": "explain feature subsampling as tree decorrelation"
      },
      {
        "id": "boosting-sequence",
        "label": "explain boosting as sequential correction"
      }
    ],
    "demonstratedFeedback": "You separated diversity and averaging from staged residual correction.",
    "unsupportedFeedback": "Use two timelines: bagging learners train beside one another; boosting learners train one after another."
  },
  {
    "lessonId": "ensemble-votes",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Many weak views can form one stronger model",
    "lessonContext": "Combine different learners for a specific reason\nAn ensemble combines predictions from multiple base learners. The combination helps only when the learners contribute meaningfully different errors or corrections. Repeating one deterministic model with identical data and settings merely repeats the same output. The design question is therefore not just how many models to train, but how they differ and how their outputs are combined.\n\nCombine different learners for a specific reason\nBagging creates parallel learners by fitting each one on a bootstrap sample: a same-sized sample drawn from training rows with replacement. Regression outputs are averaged and class outputs can be voted. This is especially useful for unstable learners such as deep trees, whose fitted structure can change when training rows change. A random forest adds random feature subsets at candidate splits to reduce similarity among its trees.\n\nCombine different learners for a specific reason\nBoosting uses a different timeline. It builds learners sequentially, with each new stage aimed at error left by the current ensemble. Under squared error, training first computes residual targets as target minus current prediction, then fits a small learner g(x) to those residuals. The update adds learning_rate times g(x), not each row's raw target or residual. Once fitted, g predicts from features at inference time without receiving the unknown target. Bagging and forests primarily target sample-driven instability, while boosting can reduce systematic underfit; validation is still required for either choice.\n\nTerms you need · Ensemble\nA predictor formed by combining outputs from multiple fitted base learners.\n\nTerms you need · Base learner\nOne component model whose output contributes to the ensemble.\n\nTerms you need · Bootstrap sample\nA same-sized training sample drawn with replacement, so rows can repeat and others can be absent.\n\nTerms you need · Bagging\nTraining diverse learners on bootstrap samples in parallel and aggregating their outputs.\n\nTerms you need · Random forest\nA tree ensemble that combines bootstrap variation with random feature subsets during split selection.\n\nTerms you need · Boosting\nBuilding an ensemble in sequence so each added learner addresses error remaining from earlier stages.\n\nTrace parallel averaging and sequential correction\nFor one regression case with target 12, three bootstrap-fitted trees predict 8, 11, and 14. Separately, a boosting model currently predicts 2 for a different case whose target is 4, with learning rate 0.5.\n\nTrace parallel averaging and sequential correction · Aggregate the bagged trees\nThe regression ensemble prediction is their mean: (8 + 11 + 14) / 3 = 33 / 3 = 11.\n\nTrace parallel averaging and sequential correction · Compare component errors\nUsing prediction minus target, the three errors are -4, -1, and 2. They are not identical, so averaging allows some disagreement to offset.\n\nTrace parallel averaging and sequential correction · Keep the bagging timeline parallel\nEach tree was fitted from its own bootstrap sample without receiving another tree's residual. Their interaction occurs only when outputs are aggregated.\n\nTrace parallel averaging and sequential correction · Compute the boosting residual\nFor the second case, residual target = target - current prediction = 4 - 2 = 2. During training, the next base learner g is fitted across all training rows to approximate their residual targets from features.\n\nTrace parallel averaging and sequential correction · Use the fitted learner's prediction\nAssume the fitted learner predicts g(x) = 1.5 for this case. The ensemble update is F_new(x) = F(x) + learning_rate * g(x) = 2 + 0.5(1.5) = 2.75. The correction can miss the raw residual because g is a constrained learner shared across rows.\n\nTrace parallel averaging and sequential correction · Separate training from inference\nTargets are used to create residual training targets and fit g. For a new case, the saved ensemble evaluates its already fitted learners from the new features; the unknown target is not an input to prediction.\n\nTrace parallel averaging and sequential correction\nBagging combines independently varied fits after training; boosting changes what the next learner is asked to correct. The arithmetic may involve addition in both cases, but the training procedures are different.\n\nCommon confusions\nIdentical outputs have perfectly aligned errors, so their average is unchanged. Stabilization requires useful diversity.\n\nCommon confusions\nIt aggregates many trees and deliberately varies both sampled rows and candidate features to reduce tree correlation.\n\nCommon confusions\nBoosting is sequential: training targets define residuals for fitting the next stage, but at inference each fitted stage predicts its scaled correction from features without the unknown target.\n\nBefore you predict\nBefore averaging, ask what created differences among the component learners.\n\nBefore you predict\nKeep parallel bootstrap fitting separate from sequential residual correction.\n\nBefore you predict\nMatch the proposed ensemble to observed instability or underfit, then require held-out evidence rather than assuming improvement.\n\nAggregation needs disagreement\nAveraging can stabilize predictions only when component errors are not perfectly aligned. Copying one deterministic model many times produces the same prediction and no variance reduction.\nAn ensemble therefore combines two ideas: create meaningfully different base learners, then aggregate or sequence their outputs.\n\nBagging trains in parallel\nBagging draws bootstrap samples from the training rows, fits one base learner per sample, then averages regression predictions or votes on classes.\nDeep trees are unstable: a changed sample can change early splits. Averaging many such trees can reduce this sample-driven variance while retaining flexible fits.\n\nRandom forests also vary the available features\nIf one strong feature dominates every tree, bootstrap trees can remain highly correlated. A random forest offers only a random subset of features at each candidate split.\nFeature subsampling can force different useful structures to appear, reducing correlation among trees. It may weaken individual trees while strengthening their average.\n\nBoosting trains in sequence\nBoosting adds a small learner that focuses on what the current ensemble still gets wrong. For squared error, training computes residual targets as target minus current prediction, then fits the next learner g(x) to approximate those residuals from features.\nThe updated ensemble is F_new(x) = F(x) + learning_rate times g(x). The fitted correction can differ from an individual row's raw residual because one constrained learner must serve multiple rows. At inference, the saved g(x) reads features, not the unknown target.\n\nName the failure before choosing the ensemble\nBagging and random forests primarily address unstable, high-variance learners. Boosting can reduce systematic underfit by composing many small corrections, though it also needs regularization and validation.\nThe labels variance and bias are diagnostic summaries, not guarantees. Compare learning curves, multi-seed variation, and held-out error under the same protocol.",
    "activityId": "ensemble-grid-transfer",
    "activityPrompt": "A power-grid tree model changes sharply when a few training days are replaced, and most trees always split first on temperature. A separate shallow model underfits every fold. Propose one ensemble intervention for each failure and state the evidence that would support it.",
    "activityGuidance": "Diagnose instability, correlated trees, and systematic underfit separately.",
    "criteria": [
      {
        "id": "grid-instability-bagging",
        "label": "use bagging for sample-driven tree instability"
      },
      {
        "id": "grid-correlation-forest",
        "label": "use random feature subsets to reduce temperature dominance"
      },
      {
        "id": "grid-underfit-boost",
        "label": "use sequential boosting for residual underfit"
      },
      {
        "id": "grid-evidence",
        "label": "require held-out and multi-sample evidence"
      }
    ],
    "demonstratedFeedback": "You matched each ensemble mechanism to a distinct observed failure and named the evidence needed.",
    "unsupportedFeedback": "Treat the unstable tree, correlated trees, and underfitting shallow model as three separate diagnoses."
  },
  {
    "lessonId": "xor-hidden-space",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Hidden units can rebuild the space",
    "lessonContext": "Create new coordinates before drawing the final boundary\nXOR, short for exclusive OR, is a binary rule that returns 1 when exactly one of two inputs is 1. Its four cases are 0 for (0,0), 1 for (1,0), 1 for (0,1), and 0 for (1,1). Plotted as four corners of a square, the two positive cases sit at opposite corners, so one straight line cannot place both positives on one side and both negatives on the other.\n\nCreate new coordinates before drawing the final boundary\nA neural layer first forms weighted sums plus biases. If one such linear calculation feeds directly into another, substitution collapses them into another single linear calculation. More layers alone therefore do not change the kind of boundary available. An activation function inserts a nonlinear operation between layers. ReLU, the rectified linear unit, returns max(0, a): it keeps a positive input and replaces a negative input with zero.\n\nCreate new coordinates before drawing the final boundary\nA hidden unit is an intermediate calculation, and the values produced by hidden units form a hidden representation. For XOR, two directed differences can create useful new coordinates: h1 = ReLU(x1 - x2) and h2 = ReLU(x2 - x1). Equal inputs map to (0,0), while the two unequal cases activate different coordinates. A final linear sum can then separate the hidden representations with a simple threshold.\n\nTerms you need · XOR\nA two-input binary rule whose output is 1 exactly when the input values differ.\n\nTerms you need · Linear boundary\nA straight dividing line in a two-feature plane, produced by thresholding one weighted sum.\n\nTerms you need · Hidden unit\nAn intermediate weighted calculation followed here by an activation function.\n\nTerms you need · Activation function\nA transformation applied between layers that can make their composition nonlinear.\n\nTerms you need · ReLU\nThe nonlinear function ReLU(a) = max(0, a), which clips negative values to zero.\n\nTerms you need · Hidden representation\nThe new coordinate values computed by hidden units and supplied to the output layer.\n\nTrace all four XOR cases through two hidden units\nUse h1 = ReLU(x1 - x2), h2 = ReLU(x2 - x1), output score s = h1 + h2, and predicted class 1 when s is at least 0.5.\n\nTrace all four XOR cases through two hidden units · Trace input (0,0)\nBoth differences are 0, so the hidden pair is (0,0). The score is 0 + 0 = 0, which is below 0.5 and produces class 0.\n\nTrace all four XOR cases through two hidden units · Trace input (1,0)\nThe directed differences are 1 and -1. ReLU produces hidden pair (1,0); the score is 1, so the output is class 1.\n\nTrace all four XOR cases through two hidden units · Trace input (0,1)\nThe directed differences are -1 and 1. ReLU produces hidden pair (0,1); the score is again 1, so the output is class 1.\n\nTrace all four XOR cases through two hidden units · Trace input (1,1)\nBoth differences are 0, giving hidden pair (0,0), score 0, and class 0.\n\nTrace all four XOR cases through two hidden units · Read the new geometry\nEqual inputs share hidden point (0,0). Unequal inputs have hidden sums of 1. The final threshold is linear in hidden space because the nonlinear work already happened in the ReLU features.\n\nTrace all four XOR cases through two hidden units\nThe network does not force one line to solve the original corner pattern. It first computes a representation in which equal and unequal inputs have a simple numeric separation, then applies a linear output rule.\n\nCommon confusions\nWithout an activation between them, substituting the first affine expression into the second yields another affine expression.\n\nCommon confusions\nEach ReLU hidden unit returns a numeric coordinate. The output layer combines both coordinates and applies the final decision threshold.\n\nCommon confusions\nThese two formulas are deliberately constructed and transparent. Hidden coordinates learned in larger networks need not match named human concepts.\n\nBefore you predict\nWrite the four input-target pairs before reasoning about any proposed XOR model.\n\nBefore you predict\nWhen layers are composed, inspect whether a nonlinear activation actually sits between their weighted sums.\n\nBefore you predict\nTrace input coordinates, hidden coordinates, output score, and threshold as separate stages.\n\nXOR labels opposite corners together\nXOR is 1 when exactly one binary input is 1. Its four cases are (0,0) -> 0, (1,0) -> 1, (0,1) -> 1, and (1,1) -> 0.\nIn the input plane, the positive examples occupy opposite corners. Any single straight boundary that isolates one positive corner places the other on the wrong side.\n\nStacked linear maps remain linear\nIf one layer computes h = A x + a and the next computes z = B h + b, substitution gives z = B A x + B a + b. The composition is still one linear map plus a bias.\nDepth alone does not create a curved or disconnected boundary. A nonlinear activation between layers changes that algebra.\n\nRectification exposes disagreement\nReLU(a) = max(0, a): positive inputs pass through unchanged, while negative inputs become zero. This is the nonlinear activation used in the two authored hidden features.\nLet h1 = ReLU(x1 - x2) and h2 = ReLU(x2 - x1). For (1,0), the hidden pair is (1,0); for (0,1), it is (0,1).\nFor both equal-input cases, (0,0) and (1,1), the hidden pair is (0,0). The hidden coordinates have turned opposite positive corners into points that share a simple property: h1 + h2 is positive.\n\nThe output layer reads the new geometry\nAn output score s = h1 + h2 is 1 for unequal inputs and 0 for equal inputs. Thresholding at 0.5 reproduces the XOR truth table.\nThe output operation is linear in hidden space. The model succeeds because the nonlinear hidden transformation changed the representation before that final line was drawn.\n\nHidden coordinates are computations, not little names\nHere each hidden unit has a transparent authored formula. In a trained network, useful coordinates are learned jointly and need not correspond to stable human concepts.\nA visual boundary can show what the network computes on a small domain, but it does not by itself explain generalization, calibration, or causality.",
    "activityId": "xor-mechanism-explanation",
    "activityPrompt": "Explain why one linear boundary fails on XOR and trace all four inputs through h1 = ReLU(x1 - x2), h2 = ReLU(x2 - x1), and score h1 + h2.",
    "activityGuidance": "Name the input geometry, hidden coordinates for equal and unequal inputs, and the final threshold.",
    "criteria": [
      {
        "id": "xor-opposite-corners",
        "label": "identify positive and negative XOR cases as opposite corners"
      },
      {
        "id": "xor-linear-failure",
        "label": "state that one straight boundary cannot separate both positive corners"
      },
      {
        "id": "xor-row-00",
        "label": "trace input 0,0 to hidden 0,0, score 0, and label 0"
      },
      {
        "id": "xor-row-10",
        "label": "trace input 1,0 to hidden 1,0, score 1, and label 1"
      },
      {
        "id": "xor-row-01",
        "label": "trace input 0,1 to hidden 0,1, score 1, and label 1"
      },
      {
        "id": "xor-row-11",
        "label": "trace input 1,1 to hidden 0,0, score 0, and label 0"
      },
      {
        "id": "xor-output-threshold",
        "label": "sum hidden values and threshold at one half"
      }
    ],
    "demonstratedFeedback": "You showed exactly how nonlinear hidden coordinates turn XOR into a simple output threshold.",
    "unsupportedFeedback": "Build a four-row table with input, h1, h2, score, and label, then explain why no input-space line matches it."
  },
  {
    "lessonId": "xor-hidden-space",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Hidden units can rebuild the space",
    "lessonContext": "Create new coordinates before drawing the final boundary\nXOR, short for exclusive OR, is a binary rule that returns 1 when exactly one of two inputs is 1. Its four cases are 0 for (0,0), 1 for (1,0), 1 for (0,1), and 0 for (1,1). Plotted as four corners of a square, the two positive cases sit at opposite corners, so one straight line cannot place both positives on one side and both negatives on the other.\n\nCreate new coordinates before drawing the final boundary\nA neural layer first forms weighted sums plus biases. If one such linear calculation feeds directly into another, substitution collapses them into another single linear calculation. More layers alone therefore do not change the kind of boundary available. An activation function inserts a nonlinear operation between layers. ReLU, the rectified linear unit, returns max(0, a): it keeps a positive input and replaces a negative input with zero.\n\nCreate new coordinates before drawing the final boundary\nA hidden unit is an intermediate calculation, and the values produced by hidden units form a hidden representation. For XOR, two directed differences can create useful new coordinates: h1 = ReLU(x1 - x2) and h2 = ReLU(x2 - x1). Equal inputs map to (0,0), while the two unequal cases activate different coordinates. A final linear sum can then separate the hidden representations with a simple threshold.\n\nTerms you need · XOR\nA two-input binary rule whose output is 1 exactly when the input values differ.\n\nTerms you need · Linear boundary\nA straight dividing line in a two-feature plane, produced by thresholding one weighted sum.\n\nTerms you need · Hidden unit\nAn intermediate weighted calculation followed here by an activation function.\n\nTerms you need · Activation function\nA transformation applied between layers that can make their composition nonlinear.\n\nTerms you need · ReLU\nThe nonlinear function ReLU(a) = max(0, a), which clips negative values to zero.\n\nTerms you need · Hidden representation\nThe new coordinate values computed by hidden units and supplied to the output layer.\n\nTrace all four XOR cases through two hidden units\nUse h1 = ReLU(x1 - x2), h2 = ReLU(x2 - x1), output score s = h1 + h2, and predicted class 1 when s is at least 0.5.\n\nTrace all four XOR cases through two hidden units · Trace input (0,0)\nBoth differences are 0, so the hidden pair is (0,0). The score is 0 + 0 = 0, which is below 0.5 and produces class 0.\n\nTrace all four XOR cases through two hidden units · Trace input (1,0)\nThe directed differences are 1 and -1. ReLU produces hidden pair (1,0); the score is 1, so the output is class 1.\n\nTrace all four XOR cases through two hidden units · Trace input (0,1)\nThe directed differences are -1 and 1. ReLU produces hidden pair (0,1); the score is again 1, so the output is class 1.\n\nTrace all four XOR cases through two hidden units · Trace input (1,1)\nBoth differences are 0, giving hidden pair (0,0), score 0, and class 0.\n\nTrace all four XOR cases through two hidden units · Read the new geometry\nEqual inputs share hidden point (0,0). Unequal inputs have hidden sums of 1. The final threshold is linear in hidden space because the nonlinear work already happened in the ReLU features.\n\nTrace all four XOR cases through two hidden units\nThe network does not force one line to solve the original corner pattern. It first computes a representation in which equal and unequal inputs have a simple numeric separation, then applies a linear output rule.\n\nCommon confusions\nWithout an activation between them, substituting the first affine expression into the second yields another affine expression.\n\nCommon confusions\nEach ReLU hidden unit returns a numeric coordinate. The output layer combines both coordinates and applies the final decision threshold.\n\nCommon confusions\nThese two formulas are deliberately constructed and transparent. Hidden coordinates learned in larger networks need not match named human concepts.\n\nBefore you predict\nWrite the four input-target pairs before reasoning about any proposed XOR model.\n\nBefore you predict\nWhen layers are composed, inspect whether a nonlinear activation actually sits between their weighted sums.\n\nBefore you predict\nTrace input coordinates, hidden coordinates, output score, and threshold as separate stages.\n\nXOR labels opposite corners together\nXOR is 1 when exactly one binary input is 1. Its four cases are (0,0) -> 0, (1,0) -> 1, (0,1) -> 1, and (1,1) -> 0.\nIn the input plane, the positive examples occupy opposite corners. Any single straight boundary that isolates one positive corner places the other on the wrong side.\n\nStacked linear maps remain linear\nIf one layer computes h = A x + a and the next computes z = B h + b, substitution gives z = B A x + B a + b. The composition is still one linear map plus a bias.\nDepth alone does not create a curved or disconnected boundary. A nonlinear activation between layers changes that algebra.\n\nRectification exposes disagreement\nReLU(a) = max(0, a): positive inputs pass through unchanged, while negative inputs become zero. This is the nonlinear activation used in the two authored hidden features.\nLet h1 = ReLU(x1 - x2) and h2 = ReLU(x2 - x1). For (1,0), the hidden pair is (1,0); for (0,1), it is (0,1).\nFor both equal-input cases, (0,0) and (1,1), the hidden pair is (0,0). The hidden coordinates have turned opposite positive corners into points that share a simple property: h1 + h2 is positive.\n\nThe output layer reads the new geometry\nAn output score s = h1 + h2 is 1 for unequal inputs and 0 for equal inputs. Thresholding at 0.5 reproduces the XOR truth table.\nThe output operation is linear in hidden space. The model succeeds because the nonlinear hidden transformation changed the representation before that final line was drawn.\n\nHidden coordinates are computations, not little names\nHere each hidden unit has a transparent authored formula. In a trained network, useful coordinates are learned jointly and need not correspond to stable human concepts.\nA visual boundary can show what the network computes on a small domain, but it does not by itself explain generalization, calibration, or causality.",
    "activityId": "xor-switch-transfer",
    "activityPrompt": "A safety lamp should turn on when exactly one of two override switches is active, but stay off when both are equal. Map this problem to XOR, give two hidden ReLU features, and explain why the final output can be linear in hidden space.",
    "activityGuidance": "Use the switch states as binary inputs and show the equal versus unequal hidden coordinates.",
    "criteria": [
      {
        "id": "switch-xor-map",
        "label": "map exactly one active switch to XOR positive"
      },
      {
        "id": "switch-hidden-formulas",
        "label": "provide both directed ReLU difference features"
      },
      {
        "id": "switch-equal-zero",
        "label": "show equal switch states map to zero-zero"
      },
      {
        "id": "switch-linear-output",
        "label": "explain that hidden sum separates unequal from equal states"
      }
    ],
    "demonstratedFeedback": "You transferred nonlinear representation building from a geometric puzzle to a control rule.",
    "unsupportedFeedback": "Translate each of the four switch states first, then reuse the two directional difference features."
  },
  {
    "lessonId": "backprop-graph",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Trace credit through a graph",
    "lessonContext": "Follow loss information backward without losing a path\nA computational graph is a step-by-step record of a calculation. In this lesson, x is an input, w is a parameter the model can change, b is an added bias, and target is the desired answer. The graph first computes p = w * x and q = w squared, then combines them as y_hat = p + q + b. The name y_hat means the model's prediction. Finally, L = 0.5 * (y_hat - target) squared produces one loss number, where a smaller loss means the prediction is closer to the target.\n\nFollow loss information backward without losing a path\nBackpropagation asks how sensitive that final loss is to every earlier value. The notation dL/dw means the rate at which L changes when w changes by a very small amount. The letter d marks a derivative, not ordinary division. At each operation, a local derivative describes how that operation's output responds to one input. The chain rule connects operations: multiply sensitivities while moving along one path from the loss toward an earlier value.\n\nFollow loss information backward without losing a path\nA boundary appears when one value feeds more than one later operation. Each route carries a separate contribution, and contributions that return to the same value must be added. Reverse-mode automatic differentiation, or autodiff, performs this bookkeeping in reverse graph order, but it does not decide whether the graph or loss expresses the right problem. A finite-difference check independently perturbs a value and tests the derivative calculation at that point.\n\nTerms you need · Computational graph\nA directed record whose nodes hold values and whose connections show which operations produced later values.\n\nTerms you need · Forward pass\nThe left-to-right evaluation that computes every intermediate value and the final loss.\n\nTerms you need · Derivative\nA local rate of change, such as dL/dw for loss change with respect to parameter w.\n\nTerms you need · Gradient\nThe collection of loss derivatives for the values or parameters being considered.\n\nTerms you need · Chain rule\nThe rule that multiplies local derivatives along a connected path through composed operations.\n\nTerms you need · Reverse-mode autodiff\nAn algorithm that starts at one output and accumulates chain-rule contributions backward through a recorded graph.\n\nTrace both routes from one loss back to w\nUse x = 2, w = 1, b = 0, and target = 1 in p = w * x, q = w squared, y_hat = p + q + b, and L = 0.5 * (y_hat - target) squared.\n\nTrace both routes from one loss back to w · Compute and record the forward state\np = 1 * 2 = 2 and q = 1 squared = 1. Therefore y_hat = 2 + 1 + 0 = 3, the prediction error y_hat - target is 2, and L = 0.5 * 2 squared = 2.\n\nTrace both routes from one loss back to w · Seed the backward pass at the loss\nFor L = 0.5 * (y_hat - target) squared, dL/dy_hat = y_hat - target = 2. This says a small increase in y_hat would increase the current loss at a local rate of 2.\n\nTrace both routes from one loss back to w · Trace the product route\nBecause y_hat includes p with coefficient 1, dL/dp = 2. Since p = w * x, dp/dw = x = 2. Multiplying along this route gives (dL/dp)(dp/dw) = 2 * 2 = 4.\n\nTrace both routes from one loss back to w · Trace the square route\nLikewise dL/dq = 2. Since q = w squared, dq/dw = 2w = 2 at w = 1. This second route contributes (dL/dq)(dq/dw) = 2 * 2 = 4.\n\nTrace both routes from one loss back to w · Join the routes and check the boundary\nBoth routes end at the same w, so dL/dw = 4 + 4 = 8. A centered finite difference with epsilon = 0.001 gives approximately 8.000004, supporting the arithmetic. It checks this derivative implementation, not whether the objective is appropriate.\n\nTrace both routes from one loss back to w\nForward values identify the state being differentiated; backward multiplication handles each route; addition preserves every route that reaches a shared parameter.\n\nCommon confusions\nIt sends sensitivities: derivatives of the loss with respect to intermediate values. The forward loss here is 2, while the final derivative with respect to w is 8.\n\nCommon confusions\nOne parameter can influence the loss through several operations. Omitting either branch discards a real effect and incorrectly gives 4 instead of 8.\n\nCommon confusions\nIt only compares analytical and numerical derivatives near tested inputs. A wrong data pipeline, graph, target, or objective can still have internally correct derivatives.\n\nBefore you predict\nWrite every forward value before differentiating so each local derivative has a known evaluation point.\n\nBefore you predict\nMultiply derivative factors along each route, then add contributions when routes meet at one earlier value.\n\nBefore you predict\nIn the branched graph, the product route contributes 4 and the square route contributes 4, so the gradient dL/dw must accumulate both contributions.\n\nA graph is a record of operations\nFor x = 2, w = 1, b = 0, and target = 1, the graph p = w * x, q = w^2, y_hat = p + q + b, and L = 0.5 * (y_hat - target)^2 produces p = 2, q = 1, y_hat = 3, and L = 2. The shared value w fans out into two operations before their results meet again.\nThe forward pass computes values. It does not yet say which earlier value should change.\n\nBackward credit is a chain of local facts\nAt the loss, dL/dy_hat = y_hat - target = 2. The product path contributes 2 * dp/dw = 2 * x = 4. The square path contributes 2 * dq/dw = 2 * 2w = 4.\nBoth paths originate at the same w, so reverse accumulation adds them: dL/dw = 4 + 4 = 8. The number 8 is the local rate at which this loss would change for a small change in the shared parameter.\n\nProducts follow paths; sums join paths\nAlong one path, chain-rule factors multiply. If a value influences the loss through several paths, the gradient contributions add because each path changes the same final loss.\nThe product branch and square branch each provide one contribution to w. Keeping only either 4 would silently discard a real route from w to the loss.\n\nAutodiff automates bookkeeping, not meaning\nReverse-mode autodiff records the forward operations and applies their derivative rules in reverse topological order. It computes the same chain-rule products and sums as the hand trace.\nAutodiff can return the exact derivative of the program and still optimize the wrong objective or propagate through a mistaken graph.\n\nA nearby perturbation checks the implementation\nFinite differences compare the accumulated analytic gradient with [L(w + epsilon) - L(w - epsilon)] / (2 * epsilon). PyTorch's gradcheck applies this same analytical-versus-numerical idea to floating-point tensor inputs. Agreement across several signed cases supports the derivative code; it does not prove the model or loss is appropriate.",
    "activityId": "backprop-credit-explanation",
    "activityPrompt": "Why must dL/dw in the worked graph contain two terms? Trace both routes, show where multiplication and addition occur, and name a numerical result that would falsify your sum.",
    "activityGuidance": "Name the shared parameter, each local derivative, both path contributions, their sum, and a finite-difference check.",
    "criteria": [
      {
        "id": "backprop-product-route",
        "label": "trace the product route through p = w times x"
      },
      {
        "id": "backprop-square-route",
        "label": "trace the square route through q = w squared"
      },
      {
        "id": "backprop-path-sum",
        "label": "multiply along each route and add the two contributions"
      },
      {
        "id": "backprop-falsifier",
        "label": "state a finite-difference result that would challenge the accumulated gradient"
      }
    ],
    "demonstratedFeedback": "You traced both branches and named a numerical check on their accumulated gradient.",
    "unsupportedFeedback": "Start at the residual, trace w through p and q separately, then add only when both routes return to w."
  },
  {
    "lessonId": "backprop-graph",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Trace credit through a graph",
    "lessonContext": "Follow loss information backward without losing a path\nA computational graph is a step-by-step record of a calculation. In this lesson, x is an input, w is a parameter the model can change, b is an added bias, and target is the desired answer. The graph first computes p = w * x and q = w squared, then combines them as y_hat = p + q + b. The name y_hat means the model's prediction. Finally, L = 0.5 * (y_hat - target) squared produces one loss number, where a smaller loss means the prediction is closer to the target.\n\nFollow loss information backward without losing a path\nBackpropagation asks how sensitive that final loss is to every earlier value. The notation dL/dw means the rate at which L changes when w changes by a very small amount. The letter d marks a derivative, not ordinary division. At each operation, a local derivative describes how that operation's output responds to one input. The chain rule connects operations: multiply sensitivities while moving along one path from the loss toward an earlier value.\n\nFollow loss information backward without losing a path\nA boundary appears when one value feeds more than one later operation. Each route carries a separate contribution, and contributions that return to the same value must be added. Reverse-mode automatic differentiation, or autodiff, performs this bookkeeping in reverse graph order, but it does not decide whether the graph or loss expresses the right problem. A finite-difference check independently perturbs a value and tests the derivative calculation at that point.\n\nTerms you need · Computational graph\nA directed record whose nodes hold values and whose connections show which operations produced later values.\n\nTerms you need · Forward pass\nThe left-to-right evaluation that computes every intermediate value and the final loss.\n\nTerms you need · Derivative\nA local rate of change, such as dL/dw for loss change with respect to parameter w.\n\nTerms you need · Gradient\nThe collection of loss derivatives for the values or parameters being considered.\n\nTerms you need · Chain rule\nThe rule that multiplies local derivatives along a connected path through composed operations.\n\nTerms you need · Reverse-mode autodiff\nAn algorithm that starts at one output and accumulates chain-rule contributions backward through a recorded graph.\n\nTrace both routes from one loss back to w\nUse x = 2, w = 1, b = 0, and target = 1 in p = w * x, q = w squared, y_hat = p + q + b, and L = 0.5 * (y_hat - target) squared.\n\nTrace both routes from one loss back to w · Compute and record the forward state\np = 1 * 2 = 2 and q = 1 squared = 1. Therefore y_hat = 2 + 1 + 0 = 3, the prediction error y_hat - target is 2, and L = 0.5 * 2 squared = 2.\n\nTrace both routes from one loss back to w · Seed the backward pass at the loss\nFor L = 0.5 * (y_hat - target) squared, dL/dy_hat = y_hat - target = 2. This says a small increase in y_hat would increase the current loss at a local rate of 2.\n\nTrace both routes from one loss back to w · Trace the product route\nBecause y_hat includes p with coefficient 1, dL/dp = 2. Since p = w * x, dp/dw = x = 2. Multiplying along this route gives (dL/dp)(dp/dw) = 2 * 2 = 4.\n\nTrace both routes from one loss back to w · Trace the square route\nLikewise dL/dq = 2. Since q = w squared, dq/dw = 2w = 2 at w = 1. This second route contributes (dL/dq)(dq/dw) = 2 * 2 = 4.\n\nTrace both routes from one loss back to w · Join the routes and check the boundary\nBoth routes end at the same w, so dL/dw = 4 + 4 = 8. A centered finite difference with epsilon = 0.001 gives approximately 8.000004, supporting the arithmetic. It checks this derivative implementation, not whether the objective is appropriate.\n\nTrace both routes from one loss back to w\nForward values identify the state being differentiated; backward multiplication handles each route; addition preserves every route that reaches a shared parameter.\n\nCommon confusions\nIt sends sensitivities: derivatives of the loss with respect to intermediate values. The forward loss here is 2, while the final derivative with respect to w is 8.\n\nCommon confusions\nOne parameter can influence the loss through several operations. Omitting either branch discards a real effect and incorrectly gives 4 instead of 8.\n\nCommon confusions\nIt only compares analytical and numerical derivatives near tested inputs. A wrong data pipeline, graph, target, or objective can still have internally correct derivatives.\n\nBefore you predict\nWrite every forward value before differentiating so each local derivative has a known evaluation point.\n\nBefore you predict\nMultiply derivative factors along each route, then add contributions when routes meet at one earlier value.\n\nBefore you predict\nIn the branched graph, the product route contributes 4 and the square route contributes 4, so the gradient dL/dw must accumulate both contributions.\n\nA graph is a record of operations\nFor x = 2, w = 1, b = 0, and target = 1, the graph p = w * x, q = w^2, y_hat = p + q + b, and L = 0.5 * (y_hat - target)^2 produces p = 2, q = 1, y_hat = 3, and L = 2. The shared value w fans out into two operations before their results meet again.\nThe forward pass computes values. It does not yet say which earlier value should change.\n\nBackward credit is a chain of local facts\nAt the loss, dL/dy_hat = y_hat - target = 2. The product path contributes 2 * dp/dw = 2 * x = 4. The square path contributes 2 * dq/dw = 2 * 2w = 4.\nBoth paths originate at the same w, so reverse accumulation adds them: dL/dw = 4 + 4 = 8. The number 8 is the local rate at which this loss would change for a small change in the shared parameter.\n\nProducts follow paths; sums join paths\nAlong one path, chain-rule factors multiply. If a value influences the loss through several paths, the gradient contributions add because each path changes the same final loss.\nThe product branch and square branch each provide one contribution to w. Keeping only either 4 would silently discard a real route from w to the loss.\n\nAutodiff automates bookkeeping, not meaning\nReverse-mode autodiff records the forward operations and applies their derivative rules in reverse topological order. It computes the same chain-rule products and sums as the hand trace.\nAutodiff can return the exact derivative of the program and still optimize the wrong objective or propagate through a mistaken graph.\n\nA nearby perturbation checks the implementation\nFinite differences compare the accumulated analytic gradient with [L(w + epsilon) - L(w - epsilon)] / (2 * epsilon). PyTorch's gradcheck applies this same analytical-versus-numerical idea to floating-point tensor inputs. Agreement across several signed cases supports the derivative code; it does not prove the model or loss is appropriate.",
    "activityId": "backprop-thermostat-transfer",
    "activityPrompt": "A heater computes direct_heat = gain * command, resistive_heat = gain^2, temperature = direct_heat + resistive_heat + ambient, and discomfort = 0.5 * (temperature - target)^2. Explain d(discomfort)/d(gain) and map both gain paths to the worked graph.",
    "activityGuidance": "Use the residual, command, 2 * gain, both path contributions, and their sum. Do not answer only with a final formula.",
    "criteria": [
      {
        "id": "thermostat-residual",
        "label": "derive the downstream discomfort sensitivity"
      },
      {
        "id": "thermostat-direct-path",
        "label": "identify command as the direct-heat path derivative"
      },
      {
        "id": "thermostat-resistive-path",
        "label": "identify two times gain as the squared path derivative"
      },
      {
        "id": "thermostat-accumulation",
        "label": "multiply the residual along both routes and add at gain"
      }
    ],
    "demonstratedFeedback": "You transferred fan-out and reverse accumulation without relying on the original variable names.",
    "unsupportedFeedback": "Map gain to w, command to x, ambient to b, and preserve both routes from gain to temperature."
  },
  {
    "lessonId": "optimizer-traces",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Read the optimizer's trace",
    "lessonContext": "Read every optimizer update as a state transition\nTraining repeatedly changes model parameters to reduce an objective, usually a loss computed on training examples. Let theta name one parameter and let theta_star name the value that minimizes a simple objective. The error e_t = theta_t - theta_star is the parameter's signed distance from that minimum at step t. A gradient g_t is the local slope of the objective at theta_t. Plain stochastic gradient descent, or SGD, updates theta by theta_(t+1) = theta_t - eta * g_t, where eta is the learning rate.\n\nRead every optimizer update as a state transition\nThe learning rate scales the proposed move. A positive multiplier in the error recurrence keeps the error on the same side of the minimum; a negative multiplier crosses the minimum. A multiplier whose absolute value is below 1 shrinks the error, one with absolute value equal to 1 preserves its size, and one above 1 grows it. This sign-and-magnitude test turns a training curve into a traceable mechanism instead of a visual guess.\n\nRead every optimizer update as a state transition\nMini-batch SGD recomputes g_t from a subset of examples at each step, so the gradient itself can vary. Momentum also carries a velocity-like state v_t made from current and earlier gradients. Adam carries two states: m_t, a decaying average of gradients, and v_t, a decaying average of squared gradients, then bias-corrects both before updating. These optimizers transform gradients; they do not choose the objective, repair data, or guarantee useful generalization.\n\nTerms you need · Parameter\nA model value, written theta here, that the optimizer is allowed to change during training.\n\nTerms you need · Objective\nThe numerical quantity training is set up to minimize or maximize; this lesson minimizes a loss.\n\nTerms you need · Gradient\nThe objective's local slope with respect to a parameter at the current state.\n\nTerms you need · Learning rate\nThe positive step-size factor eta that scales an optimizer's proposed parameter change.\n\nTerms you need · Momentum state\nA carried value that combines the current gradient with a decaying history of earlier gradients.\n\nTerms you need · Adam moments\nCarried first- and second-moment states that track gradients and squared gradients before bias correction.\n\nTerms you need · Adam decay rates\nBeta1 and beta2 are numbers between zero and one that control how much earlier first- and second-moment state is retained.\n\nTerms you need · Epsilon\nA small positive stabilizer added after the square root of Adam's corrected second moment, preventing division by zero.\n\nLocate the exact boundary between shrinking and persistent error\nMinimize J(theta) = (theta - 3) squared. Here theta_star = 3, e_t = theta_t - 3, g_t = 2e_t, and plain SGD uses theta_(t+1) = theta_t - eta * g_t. Start at theta_0 = 5.\n\nLocate the exact boundary between shrinking and persistent error · Expose the initial state\ne_0 = 5 - 3 = 2, J(theta_0) = 2 squared = 4, and g_0 = 2e_0 = 4. The state before the update is therefore theta = 5, error = 2, loss = 4, gradient = 4.\n\nLocate the exact boundary between shrinking and persistent error · Derive the error transition\nSubtract theta_star from the SGD update: e_(t+1) = e_t - eta * 2e_t = (1 - 2eta)e_t. The multiplier 1 - 2eta completely determines this fixed quadratic replay.\n\nLocate the exact boundary between shrinking and persistent error · Check a shrinking case\nWith eta = 0.25, the multiplier is 0.5. The first update is theta_1 = 5 - 0.25 * 4 = 4, so e_1 = 1 and the loss falls from 4 to 1 without crossing the minimum.\n\nLocate the exact boundary between shrinking and persistent error · Substitute the boundary value\nWith eta = 1, the multiplier is 1 - 2 = -1. From theta_0 = 5, the update gives theta_1 = 5 - 1 * 4 = 1, so e_1 = -2: the sign flips, but the magnitude remains 2.\n\nLocate the exact boundary between shrinking and persistent error · Advance one more state\nAt theta_1 = 1, g_1 = 2(1 - 3) = -4. The next update gives theta_2 = 1 - 1 * (-4) = 5, so e_2 = 2 and the loss is again 4. The trace oscillates forever without shrinking.\n\nLocate the exact boundary between shrinking and persistent error · Mark the optimizer boundary\nThis recurrence describes plain SGD with a fixed full gradient and no carried state. Momentum would also require v_t; Adam would require m_t and v_t. Those states cannot be inferred from theta alone.\n\nLocate the exact boundary between shrinking and persistent error\nFor a fixed quadratic, inspect the update multiplier's sign and absolute value; at eta = 1, the multiplier -1 means alternating sides with unchanged error size.\n\nCommon confusions\nOscillation only describes sign changes. Its magnitude may shrink, stay constant, or grow; eta = 1 produces constant-magnitude oscillation in this replay.\n\nCommon confusions\nChanging batches also changes gradient estimates. Hold batch order, initialization, model, and seed fixed before attributing a trace to learning rate.\n\nCommon confusions\nTheir defining carried states summarize gradients. The exact convention matters, including how the first momentum buffer and Adam's bias corrections are initialized.\n\nBefore you predict\nTranslate the parameter update into an error recurrence before classifying the trace.\n\nBefore you predict\nUse the multiplier's sign for crossing behavior and its absolute value for shrinking, persistence, or growth.\n\nBefore you predict\nAt eta = 1, e_(t+1) = -e_t, so the error changes sign while keeping the same magnitude.\n\nA mini-batch is a noisy view of the objective\nThe full gradient averages every training example. A mini-batch averages a selected subset, so its direction can differ even when the model and loss are unchanged.\nSmaller batches usually expose more variation between updates. That variation is not automatically evidence of a bug.\n\nInitialization chooses the first regime\nIdentical zero initialization can keep symmetric hidden units identical. Excessively large values can push nonlinear units into saturated regions or make early activations and gradients unstable.\nInitialization changes where optimization starts; it does not change which examples belong to the training objective.\n\nLearning rate scales every proposed update\nFor gradient 4, plain gradient descent with rate 0.01 moves by 0.04; rate 0.5 moves by 2. A very small rate can look flat, while a large rate can cross the minimum repeatedly or diverge.\nA jagged curve alone does not identify the cause: batch order, initialization, and optimizer state must be held fixed before blaming the learning rate.\n\nAdam updates moments, corrects them, then moves the parameter\nMomentum accumulates a decaying history of gradients, so it can preserve motion through a shallow region and overshoot after direction changes. For this lesson's standard Adam update, start with m_0 = 0 and v_0 = 0. At step t, g_t is the current gradient, beta1 and beta2 are decay rates, eta is the learning rate, and epsilon is a small positive stabilizer.\nThe complete update is m_t = beta1 * m_(t-1) + (1 - beta1) * g_t; v_t = beta2 * v_(t-1) + (1 - beta2) * g_t^2; m_hat_t = m_t / (1 - beta1^t); v_hat_t = v_t / (1 - beta2^t); theta_t = theta_(t-1) - eta * m_hat_t / (sqrt(v_hat_t) + epsilon). Epsilon is outside the square root: the denominator is sqrt(v_hat_t) + epsilon, not sqrt(v_hat_t + epsilon).\nFor one numerical update, let theta_0 = 4, g_1 = 2, m_0 = v_0 = 0, beta1 = 0.9, beta2 = 0.999, eta = 0.1, and epsilon = 1e-8. Then m_1 = 0.2 and v_1 = 0.004. Bias correction gives m_hat_1 = 0.2 / 0.1 = 2 and v_hat_1 = 0.004 / 0.001 = 4. Therefore theta_1 = 4 - 0.1 * 2 / (sqrt(4) + 1e-8), approximately 3.9.\nNeither optimizer chooses the objective, fixes bad data, or guarantees generalization.\n\nChange one cause, then replay\nA useful diagnosis preserves data order, initialization, model, and seed while changing one optimizer mechanism. Compare exact traces rather than interpreting one smooth or dramatic curve in isolation.",
    "activityId": "optimizer-instability-explanation",
    "activityPrompt": "A replay with the same seed, batches, and initialization becomes oscillatory only after the learning rate is increased. Explain the causal chain, name the nearest competing explanation, and state a result that would falsify the learning-rate diagnosis.",
    "activityGuidance": "Connect gradient, step scale, crossing the minimum, the held-fixed controls, and a controlled replay.",
    "criteria": [
      {
        "id": "optimizer-step-scale",
        "label": "connect learning rate to update magnitude"
      },
      {
        "id": "optimizer-oscillation",
        "label": "connect large steps to overshoot or oscillation"
      },
      {
        "id": "optimizer-competitor",
        "label": "name batch order or initialization as a competing cause"
      },
      {
        "id": "optimizer-falsifier",
        "label": "state a same-control replay that would falsify the diagnosis"
      }
    ],
    "demonstratedFeedback": "You isolated the update-scale mechanism and made the diagnosis testable.",
    "unsupportedFeedback": "Use the SGD equation, then separate the changed rate from batch and initialization alternatives."
  },
  {
    "lessonId": "optimizer-traces",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Read the optimizer's trace",
    "lessonContext": "Read every optimizer update as a state transition\nTraining repeatedly changes model parameters to reduce an objective, usually a loss computed on training examples. Let theta name one parameter and let theta_star name the value that minimizes a simple objective. The error e_t = theta_t - theta_star is the parameter's signed distance from that minimum at step t. A gradient g_t is the local slope of the objective at theta_t. Plain stochastic gradient descent, or SGD, updates theta by theta_(t+1) = theta_t - eta * g_t, where eta is the learning rate.\n\nRead every optimizer update as a state transition\nThe learning rate scales the proposed move. A positive multiplier in the error recurrence keeps the error on the same side of the minimum; a negative multiplier crosses the minimum. A multiplier whose absolute value is below 1 shrinks the error, one with absolute value equal to 1 preserves its size, and one above 1 grows it. This sign-and-magnitude test turns a training curve into a traceable mechanism instead of a visual guess.\n\nRead every optimizer update as a state transition\nMini-batch SGD recomputes g_t from a subset of examples at each step, so the gradient itself can vary. Momentum also carries a velocity-like state v_t made from current and earlier gradients. Adam carries two states: m_t, a decaying average of gradients, and v_t, a decaying average of squared gradients, then bias-corrects both before updating. These optimizers transform gradients; they do not choose the objective, repair data, or guarantee useful generalization.\n\nTerms you need · Parameter\nA model value, written theta here, that the optimizer is allowed to change during training.\n\nTerms you need · Objective\nThe numerical quantity training is set up to minimize or maximize; this lesson minimizes a loss.\n\nTerms you need · Gradient\nThe objective's local slope with respect to a parameter at the current state.\n\nTerms you need · Learning rate\nThe positive step-size factor eta that scales an optimizer's proposed parameter change.\n\nTerms you need · Momentum state\nA carried value that combines the current gradient with a decaying history of earlier gradients.\n\nTerms you need · Adam moments\nCarried first- and second-moment states that track gradients and squared gradients before bias correction.\n\nTerms you need · Adam decay rates\nBeta1 and beta2 are numbers between zero and one that control how much earlier first- and second-moment state is retained.\n\nTerms you need · Epsilon\nA small positive stabilizer added after the square root of Adam's corrected second moment, preventing division by zero.\n\nLocate the exact boundary between shrinking and persistent error\nMinimize J(theta) = (theta - 3) squared. Here theta_star = 3, e_t = theta_t - 3, g_t = 2e_t, and plain SGD uses theta_(t+1) = theta_t - eta * g_t. Start at theta_0 = 5.\n\nLocate the exact boundary between shrinking and persistent error · Expose the initial state\ne_0 = 5 - 3 = 2, J(theta_0) = 2 squared = 4, and g_0 = 2e_0 = 4. The state before the update is therefore theta = 5, error = 2, loss = 4, gradient = 4.\n\nLocate the exact boundary between shrinking and persistent error · Derive the error transition\nSubtract theta_star from the SGD update: e_(t+1) = e_t - eta * 2e_t = (1 - 2eta)e_t. The multiplier 1 - 2eta completely determines this fixed quadratic replay.\n\nLocate the exact boundary between shrinking and persistent error · Check a shrinking case\nWith eta = 0.25, the multiplier is 0.5. The first update is theta_1 = 5 - 0.25 * 4 = 4, so e_1 = 1 and the loss falls from 4 to 1 without crossing the minimum.\n\nLocate the exact boundary between shrinking and persistent error · Substitute the boundary value\nWith eta = 1, the multiplier is 1 - 2 = -1. From theta_0 = 5, the update gives theta_1 = 5 - 1 * 4 = 1, so e_1 = -2: the sign flips, but the magnitude remains 2.\n\nLocate the exact boundary between shrinking and persistent error · Advance one more state\nAt theta_1 = 1, g_1 = 2(1 - 3) = -4. The next update gives theta_2 = 1 - 1 * (-4) = 5, so e_2 = 2 and the loss is again 4. The trace oscillates forever without shrinking.\n\nLocate the exact boundary between shrinking and persistent error · Mark the optimizer boundary\nThis recurrence describes plain SGD with a fixed full gradient and no carried state. Momentum would also require v_t; Adam would require m_t and v_t. Those states cannot be inferred from theta alone.\n\nLocate the exact boundary between shrinking and persistent error\nFor a fixed quadratic, inspect the update multiplier's sign and absolute value; at eta = 1, the multiplier -1 means alternating sides with unchanged error size.\n\nCommon confusions\nOscillation only describes sign changes. Its magnitude may shrink, stay constant, or grow; eta = 1 produces constant-magnitude oscillation in this replay.\n\nCommon confusions\nChanging batches also changes gradient estimates. Hold batch order, initialization, model, and seed fixed before attributing a trace to learning rate.\n\nCommon confusions\nTheir defining carried states summarize gradients. The exact convention matters, including how the first momentum buffer and Adam's bias corrections are initialized.\n\nBefore you predict\nTranslate the parameter update into an error recurrence before classifying the trace.\n\nBefore you predict\nUse the multiplier's sign for crossing behavior and its absolute value for shrinking, persistence, or growth.\n\nBefore you predict\nAt eta = 1, e_(t+1) = -e_t, so the error changes sign while keeping the same magnitude.\n\nA mini-batch is a noisy view of the objective\nThe full gradient averages every training example. A mini-batch averages a selected subset, so its direction can differ even when the model and loss are unchanged.\nSmaller batches usually expose more variation between updates. That variation is not automatically evidence of a bug.\n\nInitialization chooses the first regime\nIdentical zero initialization can keep symmetric hidden units identical. Excessively large values can push nonlinear units into saturated regions or make early activations and gradients unstable.\nInitialization changes where optimization starts; it does not change which examples belong to the training objective.\n\nLearning rate scales every proposed update\nFor gradient 4, plain gradient descent with rate 0.01 moves by 0.04; rate 0.5 moves by 2. A very small rate can look flat, while a large rate can cross the minimum repeatedly or diverge.\nA jagged curve alone does not identify the cause: batch order, initialization, and optimizer state must be held fixed before blaming the learning rate.\n\nAdam updates moments, corrects them, then moves the parameter\nMomentum accumulates a decaying history of gradients, so it can preserve motion through a shallow region and overshoot after direction changes. For this lesson's standard Adam update, start with m_0 = 0 and v_0 = 0. At step t, g_t is the current gradient, beta1 and beta2 are decay rates, eta is the learning rate, and epsilon is a small positive stabilizer.\nThe complete update is m_t = beta1 * m_(t-1) + (1 - beta1) * g_t; v_t = beta2 * v_(t-1) + (1 - beta2) * g_t^2; m_hat_t = m_t / (1 - beta1^t); v_hat_t = v_t / (1 - beta2^t); theta_t = theta_(t-1) - eta * m_hat_t / (sqrt(v_hat_t) + epsilon). Epsilon is outside the square root: the denominator is sqrt(v_hat_t) + epsilon, not sqrt(v_hat_t + epsilon).\nFor one numerical update, let theta_0 = 4, g_1 = 2, m_0 = v_0 = 0, beta1 = 0.9, beta2 = 0.999, eta = 0.1, and epsilon = 1e-8. Then m_1 = 0.2 and v_1 = 0.004. Bias correction gives m_hat_1 = 0.2 / 0.1 = 2 and v_hat_1 = 0.004 / 0.001 = 4. Therefore theta_1 = 4 - 0.1 * 2 / (sqrt(4) + 1e-8), approximately 3.9.\nNeither optimizer chooses the objective, fixes bad data, or guarantees generalization.\n\nChange one cause, then replay\nA useful diagnosis preserves data order, initialization, model, and seed while changing one optimizer mechanism. Compare exact traces rather than interpreting one smooth or dramatic curve in isolation.",
    "activityId": "optimizer-sensor-transfer",
    "activityPrompt": "A factory estimates sensor bias from a different small sample each minute and smooths updates with a running velocity. Map mini-batch noise and momentum to this estimator. Explain why a stale velocity can overshoot after the true bias changes sign.",
    "activityGuidance": "Name the sampled evidence, the noisy estimate, the carried state, and the sign change.",
    "criteria": [
      {
        "id": "sensor-batch-map",
        "label": "map each minute's sample to a mini-batch"
      },
      {
        "id": "sensor-noisy-gradient",
        "label": "map sample variation to a noisy update estimate"
      },
      {
        "id": "sensor-velocity",
        "label": "identify velocity as accumulated past updates"
      },
      {
        "id": "sensor-overshoot",
        "label": "explain overshoot after the sign change"
      }
    ],
    "demonstratedFeedback": "You transferred both stochastic evidence and optimizer state to a different iterative system.",
    "unsupportedFeedback": "Treat each minute's sample as the batch and the running velocity as memory from earlier updates."
  },
  {
    "lessonId": "cluster-project",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Three objectives, three geometries",
    "lessonContext": "Ask which objective created the geometry\nA vector is an ordered list of numerical coordinates. A representation is the choice of coordinates used to describe an example, and geometry means the distances and directions among represented examples. Proximity has meaning only after you identify the objective and measurements that produced it.\n\nAsk which objective created the geometry\nK-means, principal component analysis, and learned embeddings create geometry for different reasons. K-means alternates between assigning each point to its nearest centroid and replacing each centroid with the mean of its assigned points. Principal component analysis, or PCA, first centers each feature by subtracting its mean, then finds linear directions that retain as much variance as possible. A learned embedding places examples in coordinates adjusted by a training loss for a predictive or self-supervised task.\n\nAsk which objective created the geometry\nTheir boundaries matter. K-means uses distances, so feature scale, initial centroids, and outliers can change its result. PCA preserves high-variance linear structure, not labels or every detail; fewer dimensions create reconstruction error. Embedding distances inherit the examples, loss, architecture, and optimization. A two-dimensional display can distort a high-dimensional embedding, so a visible neighborhood is not a universal semantic fact.\n\nTerms you need · Representation\nAn ordered set of coordinates used to describe one example for a particular purpose.\n\nTerms you need · Centroid\nThe coordinate-wise arithmetic mean of all points currently assigned to one k-means cluster.\n\nTerms you need · Squared distance\nThe sum of squared coordinate differences between two points, used by standard k-means.\n\nTerms you need · Principal component\nA unit direction chosen by PCA to retain as much remaining projected variance as possible.\n\nTerms you need · Projection\nThe coordinates obtained by expressing a centered point along selected directions.\n\nTerms you need · Embedding objective\nThe training loss whose signal determines which relationships a learned coordinate space tends to preserve.\n\nKeep the points visible while three objectives treat them differently\nUse four two-dimensional points: A = (0, -1), B = (2, 1), C = (8, 1), and D = (10, -1). Start k-means with centroid c1 = A and centroid c2 = D, and use squared Euclidean distance.\n\nKeep the points visible while three objectives treat them differently · Compute the assignment state\nA has squared distances 0 to c1 and 100 to c2. B has 8 to c1 and 68 to c2, so A and B join cluster 1. C has 68 to c1 and 8 to c2, while D has 100 and 0, so C and D join cluster 2.\n\nKeep the points visible while three objectives treat them differently · Relocate both centroids\nThe mean of A and B is c1 = ((0 + 2)/2, (-1 + 1)/2) = (1, 0). The mean of C and D is c2 = ((8 + 10)/2, (1 - 1)/2) = (9, 0). Assignments and centroids are separate intermediate states.\n\nKeep the points visible while three objectives treat them differently · Hold assignments fixed and move one point\nMove B from (2, 1) to (6, 1) without reassigning it. The first centroid becomes ((0 + 6)/2, (-1 + 1)/2) = (3, 0). Its horizontal coordinate moves right from 1 to 3 because a centroid is a mean.\n\nKeep the points visible while three objectives treat them differently · Center the original points for PCA\nBefore the move, the feature mean is (5, 0). The centered points are (-5, -1), (-3, 1), (3, 1), and (5, -1). Horizontal variation is much larger than vertical variation, so the first principal direction is the horizontal axis.\n\nKeep the points visible while three objectives treat them differently · Project and reconstruct\nKeeping one principal component retains horizontal coordinates -5, -3, 3, and 5. Reconstructing adds the mean back and sets the discarded vertical coordinate to 0, so each point loses its original vertical value of -1 or 1.\n\nKeep the points visible while three objectives treat them differently · Change the objective, not the arithmetic\nAn embedding trained to predict left-versus-right shelf membership may place A near B and C near D. One trained to predict top-versus-bottom position may instead place B near C. The task signal, not k-means or PCA, defines that learned proximity.\n\nKeep the points visible while three objectives treat them differently\nExpose assignments, means, centered coordinates, projections, reconstructions, and training objectives separately; the same examples can support different valid geometries.\n\nCommon confusions\nK-means creates assignments that reduce within-cluster squared distance. It does not read labels or prove that a cluster is a real-world category.\n\nCommon confusions\nPCA keeps high-variance linear directions without using the prediction target. A low-variance feature can still be important for prediction.\n\nCommon confusions\nCloseness reflects a particular training signal and data distribution. Interpretation requires checking the objective, original space, examples, and stability.\n\nBefore you predict\nFor k-means, hold assignments fixed before recomputing each centroid as the mean of its members.\n\nBefore you predict\nFor PCA, distinguish centered input, retained projection, discarded coordinates, and reconstructed output.\n\nBefore you predict\nIf one assigned point moves far right, its cluster mean must move right; no class label is created.\n\nGeometry follows an objective\nA plot can show points near one another without saying why proximity matters. K-means, PCA, and learned embeddings each produce geometry under a different success criterion.\nThe same rows can therefore produce different neighborhoods without any algorithm being internally inconsistent.\n\nK-means alternates assignment and relocation\nGiven centroids, assign each point to its nearest centroid under squared distance. Given assignments, replace each centroid with the mean of its assigned points.\nEach step does not increase within-cluster squared distance, but different initial centroids can end at different local solutions. Feature scale and outliers can dominate Euclidean distance.\n\nPCA preserves variance through linear projection\nAfter centering, the first principal direction is the unit direction with greatest projected variance. Keeping only a few directions gives the best linear reconstruction under squared error for that dimensionality.\nPCA does not use class labels and does not promise that the most variable direction is the most predictive one.\n\nLearned embeddings inherit their training signal\nAn embedding is a learned coordinate vector. Which items become close depends on the loss, positive and negative examples, architecture, and data used to train those coordinates.\nA two-dimensional projection of an embedding can distort original distances. Stable neighborhoods should be checked in the original space and across seeds before they support a claim.\n\nName what is optimized before interpreting the picture\nK-means minimizes within-cluster distance, PCA minimizes linear reconstruction error when dimensions are dropped, and a learned embedding minimizes its authored training loss. None of these objectives automatically discovers a unique true grouping.",
    "activityId": "cluster-objective-explanation",
    "activityPrompt": "The same dataset shows one split under k-means and another pattern after PCA. Explain why this is not a contradiction, distinguish both objectives from a learned embedding, and name one observation that would challenge your interpretation.",
    "activityGuidance": "State each objective explicitly and use original-space distances or a replay as the check.",
    "criteria": [
      {
        "id": "cluster-kmeans-objective",
        "label": "state the k-means objective"
      },
      {
        "id": "cluster-pca-objective",
        "label": "state the PCA objective"
      },
      {
        "id": "cluster-embedding-objective",
        "label": "tie embedding geometry to its training loss"
      },
      {
        "id": "cluster-challenge",
        "label": "name an original-space or stability challenge"
      }
    ],
    "demonstratedFeedback": "You separated the three objectives and treated the displayed geometry as testable evidence.",
    "unsupportedFeedback": "Do not compare pictures first. Write the quantity each method optimizes."
  },
  {
    "lessonId": "cluster-project",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Three objectives, three geometries",
    "lessonContext": "Ask which objective created the geometry\nA vector is an ordered list of numerical coordinates. A representation is the choice of coordinates used to describe an example, and geometry means the distances and directions among represented examples. Proximity has meaning only after you identify the objective and measurements that produced it.\n\nAsk which objective created the geometry\nK-means, principal component analysis, and learned embeddings create geometry for different reasons. K-means alternates between assigning each point to its nearest centroid and replacing each centroid with the mean of its assigned points. Principal component analysis, or PCA, first centers each feature by subtracting its mean, then finds linear directions that retain as much variance as possible. A learned embedding places examples in coordinates adjusted by a training loss for a predictive or self-supervised task.\n\nAsk which objective created the geometry\nTheir boundaries matter. K-means uses distances, so feature scale, initial centroids, and outliers can change its result. PCA preserves high-variance linear structure, not labels or every detail; fewer dimensions create reconstruction error. Embedding distances inherit the examples, loss, architecture, and optimization. A two-dimensional display can distort a high-dimensional embedding, so a visible neighborhood is not a universal semantic fact.\n\nTerms you need · Representation\nAn ordered set of coordinates used to describe one example for a particular purpose.\n\nTerms you need · Centroid\nThe coordinate-wise arithmetic mean of all points currently assigned to one k-means cluster.\n\nTerms you need · Squared distance\nThe sum of squared coordinate differences between two points, used by standard k-means.\n\nTerms you need · Principal component\nA unit direction chosen by PCA to retain as much remaining projected variance as possible.\n\nTerms you need · Projection\nThe coordinates obtained by expressing a centered point along selected directions.\n\nTerms you need · Embedding objective\nThe training loss whose signal determines which relationships a learned coordinate space tends to preserve.\n\nKeep the points visible while three objectives treat them differently\nUse four two-dimensional points: A = (0, -1), B = (2, 1), C = (8, 1), and D = (10, -1). Start k-means with centroid c1 = A and centroid c2 = D, and use squared Euclidean distance.\n\nKeep the points visible while three objectives treat them differently · Compute the assignment state\nA has squared distances 0 to c1 and 100 to c2. B has 8 to c1 and 68 to c2, so A and B join cluster 1. C has 68 to c1 and 8 to c2, while D has 100 and 0, so C and D join cluster 2.\n\nKeep the points visible while three objectives treat them differently · Relocate both centroids\nThe mean of A and B is c1 = ((0 + 2)/2, (-1 + 1)/2) = (1, 0). The mean of C and D is c2 = ((8 + 10)/2, (1 - 1)/2) = (9, 0). Assignments and centroids are separate intermediate states.\n\nKeep the points visible while three objectives treat them differently · Hold assignments fixed and move one point\nMove B from (2, 1) to (6, 1) without reassigning it. The first centroid becomes ((0 + 6)/2, (-1 + 1)/2) = (3, 0). Its horizontal coordinate moves right from 1 to 3 because a centroid is a mean.\n\nKeep the points visible while three objectives treat them differently · Center the original points for PCA\nBefore the move, the feature mean is (5, 0). The centered points are (-5, -1), (-3, 1), (3, 1), and (5, -1). Horizontal variation is much larger than vertical variation, so the first principal direction is the horizontal axis.\n\nKeep the points visible while three objectives treat them differently · Project and reconstruct\nKeeping one principal component retains horizontal coordinates -5, -3, 3, and 5. Reconstructing adds the mean back and sets the discarded vertical coordinate to 0, so each point loses its original vertical value of -1 or 1.\n\nKeep the points visible while three objectives treat them differently · Change the objective, not the arithmetic\nAn embedding trained to predict left-versus-right shelf membership may place A near B and C near D. One trained to predict top-versus-bottom position may instead place B near C. The task signal, not k-means or PCA, defines that learned proximity.\n\nKeep the points visible while three objectives treat them differently\nExpose assignments, means, centered coordinates, projections, reconstructions, and training objectives separately; the same examples can support different valid geometries.\n\nCommon confusions\nK-means creates assignments that reduce within-cluster squared distance. It does not read labels or prove that a cluster is a real-world category.\n\nCommon confusions\nPCA keeps high-variance linear directions without using the prediction target. A low-variance feature can still be important for prediction.\n\nCommon confusions\nCloseness reflects a particular training signal and data distribution. Interpretation requires checking the objective, original space, examples, and stability.\n\nBefore you predict\nFor k-means, hold assignments fixed before recomputing each centroid as the mean of its members.\n\nBefore you predict\nFor PCA, distinguish centered input, retained projection, discarded coordinates, and reconstructed output.\n\nBefore you predict\nIf one assigned point moves far right, its cluster mean must move right; no class label is created.\n\nGeometry follows an objective\nA plot can show points near one another without saying why proximity matters. K-means, PCA, and learned embeddings each produce geometry under a different success criterion.\nThe same rows can therefore produce different neighborhoods without any algorithm being internally inconsistent.\n\nK-means alternates assignment and relocation\nGiven centroids, assign each point to its nearest centroid under squared distance. Given assignments, replace each centroid with the mean of its assigned points.\nEach step does not increase within-cluster squared distance, but different initial centroids can end at different local solutions. Feature scale and outliers can dominate Euclidean distance.\n\nPCA preserves variance through linear projection\nAfter centering, the first principal direction is the unit direction with greatest projected variance. Keeping only a few directions gives the best linear reconstruction under squared error for that dimensionality.\nPCA does not use class labels and does not promise that the most variable direction is the most predictive one.\n\nLearned embeddings inherit their training signal\nAn embedding is a learned coordinate vector. Which items become close depends on the loss, positive and negative examples, architecture, and data used to train those coordinates.\nA two-dimensional projection of an embedding can distort original distances. Stable neighborhoods should be checked in the original space and across seeds before they support a claim.\n\nName what is optimized before interpreting the picture\nK-means minimizes within-cluster distance, PCA minimizes linear reconstruction error when dimensions are dropped, and a learned embedding minimizes its authored training loss. None of these objectives automatically discovers a unique true grouping.",
    "activityId": "cluster-library-transfer",
    "activityPrompt": "A library has books described by topic counts. It needs shelves with compact groups, a two-number catalog preview, and recommendations learned from borrowing pairs. Match k-means, PCA, and a learned embedding to the three jobs and justify each match.",
    "activityGuidance": "Use compactness, reconstruction, and predictive co-borrowing as three distinct criteria.",
    "criteria": [
      {
        "id": "library-shelves",
        "label": "use k-means for compact shelf groups"
      },
      {
        "id": "library-preview",
        "label": "use PCA for the low-dimensional preview"
      },
      {
        "id": "library-recommend",
        "label": "use learned embeddings for co-borrowing prediction"
      },
      {
        "id": "library-no-equivalence",
        "label": "state that the resulting neighborhoods need not match"
      }
    ],
    "demonstratedFeedback": "You matched each representation to the job its objective actually supports.",
    "unsupportedFeedback": "Assign one method to compact groups, one to linear reconstruction, and one to co-borrowing prediction."
  },
  {
    "lessonId": "convolution-field",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Reuse one detector across space",
    "lessonContext": "Trace one shared detector across locations and layers\nAn image or time series can be stored as a grid of numbers. A patch is a local window, and a kernel is a small set of learned weights with the same shape. At one output location, convolution multiplies matching patch and kernel entries and adds the products. The result is an activation. Many libraries technically use cross-correlation, but the course follows the standard convention of calling it convolution.\n\nTrace one shared detector across locations and layers\nWeight sharing reuses the same kernel at every location. The kernel does not learn again when it moves; only the patch changes. Stride is how far the window moves, while padding adds values around the boundary. With both fixed, moving a pattern usually moves its matching activation. This behavior can fail at boundaries or when later operations discard location.\n\nTrace one shared detector across locations and layers\nA receptive field is the set of input positions that can possibly influence one later activation. With stride 1, no dilation, and width-3 kernels, one layer sees three positions, two stacked layers can see five, and three can see seven. A residual block adds an identity route: y = x + F(x), where F is a learned branch. Its derivative is dy/dx = 1 + dF/dx, exposing a direct value and gradient path without guaranteeing that every deep network will train well.\n\nTerms you need · Patch\nThe local group of input values aligned with a kernel at one output location.\n\nTerms you need · Kernel\nA small shared array of weights multiplied with each local patch.\n\nTerms you need · Activation\nThe numerical output produced at a location after applying the local weighted calculation.\n\nTerms you need · Weight sharing\nReusing exactly the same kernel parameters at multiple spatial or temporal locations.\n\nTerms you need · Receptive field\nThe input positions that have a computational path to one selected later activation.\n\nTerms you need · Residual path\nA direct identity route that adds an input x to a learned transformation F(x).\n\nSlide one kernel, shift the pattern, and preserve the boundaries\nUse the one-dimensional input [0, 1, 2, 1, 0], kernel [1, 2, 1], stride 1, and valid padding, which means no added boundary values. Each output is the dot product of one width-3 patch and the fixed kernel.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · List the valid patches\nThe kernel fits at three locations. The patches are [0, 1, 2], [1, 2, 1], and [2, 1, 0]. Writing them first separates movement of the window from the shared arithmetic.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Compute every activation\nThe outputs are 0*1 + 1*2 + 2*1 = 4, then 1*1 + 2*2 + 1*1 = 6, then 2*1 + 1*2 + 0*1 = 4. The complete output is [4, 6, 4].\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Move only the input pattern\nShift the pattern one cell right to obtain [0, 0, 1, 2, 1]. Keep the kernel, stride, and padding unchanged. The new patches are [0, 0, 1], [0, 1, 2], and [1, 2, 1].\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Reuse the same kernel\nThe new activations are 1, 4, and 6. The strongest match moved from output position 2 to output position 3. No weight update occurred; the matching patch simply appeared one location later.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Grow the possible influence\nIf a second width-3, stride-1 layer reads the first layer, one of its activations can combine three neighboring first-layer activations whose combined input span is five positions. A third such layer expands that span to seven.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Expose the residual boundary\nFor x = 3 and F(x) = -1, the block output is y = 2. If the learned branch is locally flat so dF/dx = 0, then dy/dx = 1 + 0 = 1: the identity route still carries a gradient.\n\nSlide one kernel, shift the pattern, and preserve the boundaries\nKernel values, input patches, output locations, receptive-field reach, and the residual identity route are distinct states that should never be collapsed into one vague feature story.\n\nCommon confusions\nStandard convolution shares one kernel across locations. Different patches create different activations while the kernel parameters stay fixed.\n\nCommon confusions\nA receptive field marks possible computational influence. Actual influence depends on learned weights, nonlinearities, and the current input.\n\nCommon confusions\nIt provides a direct route for values and gradients, but optimization can still fail because of data, objectives, scales, or other architecture choices.\n\nBefore you predict\nAt each location, identify the patch, multiply by the unchanged kernel, and add the products.\n\nBefore you predict\nKeep stride, padding, and kernel fixed when testing whether a translated pattern produces a translated response.\n\nBefore you predict\nWhen the same pattern moves one valid cell right, its matching activation should also move right rather than trigger new learning.\n\nOne local rule visits every valid location\nA convolution output at one location is the sum of elementwise products between a local input patch and a kernel. Moving one location changes the patch but reuses the same kernel values.\nSharing lets one learned pattern detector operate across space with fewer parameters than a separate detector at every position.\n\nCompute the patch before naming the feature\nFor patch [[1, 2], [0, 1]] and kernel [[1, 0], [0, -1]], the output is 1 * 1 + 2 * 0 + 0 * 0 + 1 * -1 = 0.\nThe arithmetic is known. Calling the result an edge, texture, or object part requires evidence from learned behavior and interventions; the activation alone does not supply that meaning.\n\nReceptive field tracks possible influence\nWith stride 1 and no dilation, each additional width-3 convolution expands the receptive field by two positions. Three such layers give a width-7 receptive field.\nReceptive field says which inputs can influence an output through the graph. It does not say every input has equal actual influence.\n\nA residual block preserves a direct route\nA residual block computes y = x + F(x). Its derivative is dy/dx = 1 + dF/dx, so the identity term provides a direct route for values and gradients.\nResidual paths ease optimization; they do not make every deep model stable or force the learned branch to be small.\n\nSeparate kernel, location, and architecture\nTo test spatial reuse, hold the kernel fixed and move the same pattern. To test receptive field, hold weights fixed and perturb an input at a known distance. To test the residual route, compare the same branch with and without the identity addition.",
    "activityId": "convolution-sharing-explanation",
    "activityPrompt": "Explain why a translated pattern moves a convolution response instead of requiring a new detector. Include the nearest misconception about receptive fields and a perturbation that would falsify spatial reuse.",
    "activityGuidance": "Connect local patches, the same kernel, output location, possible influence, and a controlled shift.",
    "criteria": [
      {
        "id": "convolution-local-patch",
        "label": "identify the changed local patch"
      },
      {
        "id": "convolution-same-kernel",
        "label": "state that the same kernel is reused"
      },
      {
        "id": "convolution-field-boundary",
        "label": "distinguish possible receptive-field influence from equal influence"
      },
      {
        "id": "convolution-falsifier",
        "label": "state a fixed-kernel translation test"
      }
    ],
    "demonstratedFeedback": "You connected weight sharing to the translated response and bounded the receptive-field claim.",
    "unsupportedFeedback": "Follow the same kernel from one input patch to the next, then separate possible from actual influence."
  },
  {
    "lessonId": "convolution-field",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Reuse one detector across space",
    "lessonContext": "Trace one shared detector across locations and layers\nAn image or time series can be stored as a grid of numbers. A patch is a local window, and a kernel is a small set of learned weights with the same shape. At one output location, convolution multiplies matching patch and kernel entries and adds the products. The result is an activation. Many libraries technically use cross-correlation, but the course follows the standard convention of calling it convolution.\n\nTrace one shared detector across locations and layers\nWeight sharing reuses the same kernel at every location. The kernel does not learn again when it moves; only the patch changes. Stride is how far the window moves, while padding adds values around the boundary. With both fixed, moving a pattern usually moves its matching activation. This behavior can fail at boundaries or when later operations discard location.\n\nTrace one shared detector across locations and layers\nA receptive field is the set of input positions that can possibly influence one later activation. With stride 1, no dilation, and width-3 kernels, one layer sees three positions, two stacked layers can see five, and three can see seven. A residual block adds an identity route: y = x + F(x), where F is a learned branch. Its derivative is dy/dx = 1 + dF/dx, exposing a direct value and gradient path without guaranteeing that every deep network will train well.\n\nTerms you need · Patch\nThe local group of input values aligned with a kernel at one output location.\n\nTerms you need · Kernel\nA small shared array of weights multiplied with each local patch.\n\nTerms you need · Activation\nThe numerical output produced at a location after applying the local weighted calculation.\n\nTerms you need · Weight sharing\nReusing exactly the same kernel parameters at multiple spatial or temporal locations.\n\nTerms you need · Receptive field\nThe input positions that have a computational path to one selected later activation.\n\nTerms you need · Residual path\nA direct identity route that adds an input x to a learned transformation F(x).\n\nSlide one kernel, shift the pattern, and preserve the boundaries\nUse the one-dimensional input [0, 1, 2, 1, 0], kernel [1, 2, 1], stride 1, and valid padding, which means no added boundary values. Each output is the dot product of one width-3 patch and the fixed kernel.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · List the valid patches\nThe kernel fits at three locations. The patches are [0, 1, 2], [1, 2, 1], and [2, 1, 0]. Writing them first separates movement of the window from the shared arithmetic.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Compute every activation\nThe outputs are 0*1 + 1*2 + 2*1 = 4, then 1*1 + 2*2 + 1*1 = 6, then 2*1 + 1*2 + 0*1 = 4. The complete output is [4, 6, 4].\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Move only the input pattern\nShift the pattern one cell right to obtain [0, 0, 1, 2, 1]. Keep the kernel, stride, and padding unchanged. The new patches are [0, 0, 1], [0, 1, 2], and [1, 2, 1].\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Reuse the same kernel\nThe new activations are 1, 4, and 6. The strongest match moved from output position 2 to output position 3. No weight update occurred; the matching patch simply appeared one location later.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Grow the possible influence\nIf a second width-3, stride-1 layer reads the first layer, one of its activations can combine three neighboring first-layer activations whose combined input span is five positions. A third such layer expands that span to seven.\n\nSlide one kernel, shift the pattern, and preserve the boundaries · Expose the residual boundary\nFor x = 3 and F(x) = -1, the block output is y = 2. If the learned branch is locally flat so dF/dx = 0, then dy/dx = 1 + 0 = 1: the identity route still carries a gradient.\n\nSlide one kernel, shift the pattern, and preserve the boundaries\nKernel values, input patches, output locations, receptive-field reach, and the residual identity route are distinct states that should never be collapsed into one vague feature story.\n\nCommon confusions\nStandard convolution shares one kernel across locations. Different patches create different activations while the kernel parameters stay fixed.\n\nCommon confusions\nA receptive field marks possible computational influence. Actual influence depends on learned weights, nonlinearities, and the current input.\n\nCommon confusions\nIt provides a direct route for values and gradients, but optimization can still fail because of data, objectives, scales, or other architecture choices.\n\nBefore you predict\nAt each location, identify the patch, multiply by the unchanged kernel, and add the products.\n\nBefore you predict\nKeep stride, padding, and kernel fixed when testing whether a translated pattern produces a translated response.\n\nBefore you predict\nWhen the same pattern moves one valid cell right, its matching activation should also move right rather than trigger new learning.\n\nOne local rule visits every valid location\nA convolution output at one location is the sum of elementwise products between a local input patch and a kernel. Moving one location changes the patch but reuses the same kernel values.\nSharing lets one learned pattern detector operate across space with fewer parameters than a separate detector at every position.\n\nCompute the patch before naming the feature\nFor patch [[1, 2], [0, 1]] and kernel [[1, 0], [0, -1]], the output is 1 * 1 + 2 * 0 + 0 * 0 + 1 * -1 = 0.\nThe arithmetic is known. Calling the result an edge, texture, or object part requires evidence from learned behavior and interventions; the activation alone does not supply that meaning.\n\nReceptive field tracks possible influence\nWith stride 1 and no dilation, each additional width-3 convolution expands the receptive field by two positions. Three such layers give a width-7 receptive field.\nReceptive field says which inputs can influence an output through the graph. It does not say every input has equal actual influence.\n\nA residual block preserves a direct route\nA residual block computes y = x + F(x). Its derivative is dy/dx = 1 + dF/dx, so the identity term provides a direct route for values and gradients.\nResidual paths ease optimization; they do not make every deep model stable or force the learned branch to be small.\n\nSeparate kernel, location, and architecture\nTo test spatial reuse, hold the kernel fixed and move the same pattern. To test receptive field, hold weights fixed and perturb an input at a known distance. To test the residual route, compare the same branch with and without the identity addition.",
    "activityId": "convolution-audio-transfer",
    "activityPrompt": "An audio model scans a waveform for a short click pattern. Explain how convolution detects the click at different times, how stacked layers enlarge temporal context, and how y = x + F(x) preserves a direct signal and gradient route.",
    "activityGuidance": "Map image position to time, spatial patch to waveform window, and the skip connection to the unchanged waveform path.",
    "criteria": [
      {
        "id": "audio-shared-click",
        "label": "reuse one kernel across time"
      },
      {
        "id": "audio-temporal-field",
        "label": "connect stacked layers to wider temporal context"
      },
      {
        "id": "audio-residual-value",
        "label": "identify the direct signal path"
      },
      {
        "id": "audio-residual-gradient",
        "label": "identify the direct gradient term"
      }
    ],
    "demonstratedFeedback": "You transferred locality, field growth, and the residual route from space to time.",
    "unsupportedFeedback": "Replace the image patch with a waveform window and preserve the same three mechanisms."
  },
  {
    "lessonId": "attention-routing",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Route information with attention",
    "lessonContext": "Separate matching, routing, and interpretation in attention\nSelf-attention builds each token representation by routing information from allowed positions. For one receiving position, a query vector q describes what it is matching. Each source has a key vector k for matching and a value vector v containing information to route. A vector is an ordered list of numbers. These vectors come from learned projections, but their weights are fixed during this inference pass.\n\nSeparate matching, routing, and interpretation in attention\nThe mechanism has visible intermediate states. First, a query-key dot product multiplies corresponding coordinates and adds them to form a compatibility score. Scaled dot-product attention divides that score by the square root of d_k, where d_k is the number of key coordinates. A causal mask then removes forbidden future positions. Softmax exponentiates the remaining scores and divides by their sum, producing nonnegative attention weights that add to 1. Finally, those weights multiply value vectors, which are added to form the routed output.\n\nSeparate matching, routing, and interpretation in attention\nSeveral boundaries prevent overclaiming. Keys determine routing weights; values supply content. A causal mask controls available positions but does not encode their full order. This forward pass changes representations, not parameters; training also requires a loss, backpropagation, and optimizer steps. An attention weight is routing, not proof that one token caused a final prediction. Causal claims require intervention and measured output change.\n\nTerms you need · Query\nA vector q from the receiving position that is compared with candidate keys.\n\nTerms you need · Key\nA vector k from a candidate source position that helps determine its routing weight.\n\nTerms you need · Value\nA vector v containing the source information that is multiplied by an attention weight.\n\nTerms you need · Scaled dot-product score\nA query-key dot product divided by the square root of the key dimension d_k.\n\nTerms you need · Softmax\nA normalization that converts allowed finite scores into positive weights summing to 1.\n\nTerms you need · Causal mask\nAn information boundary that gives unavailable future positions zero weight before left-to-right prediction.\n\nRoute two values from equal scores while excluding the future\nFor one receiving token, use q = [1, 0]. Two allowed source positions have k1 = [1, 0], k2 = [1, 0], v1 = [2, 0], and v2 = [0, 4]. A third future position has a high raw match but is blocked by a causal mask. The key dimension d_k is 2.\n\nRoute two values from equal scores while excluding the future · Freeze the inference state\nTreat q, all keys, all values, and the projection parameters that produced them as fixed. Computing attention from these vectors is a forward pass; no gradient or optimizer update appears in this state.\n\nRoute two values from equal scores while excluding the future · Compute the allowed raw matches\nThe dot products are q dot k1 = 1*1 + 0*0 = 1 and q dot k2 = 1*1 + 0*0 = 1. Equal keys relative to this query produce equal raw scores.\n\nRoute two values from equal scores while excluding the future · Scale and apply the mask\nDividing by sqrt(2) gives two allowed scores of approximately 0.707. The future position's score is replaced by negative infinity before softmax, regardless of how large its unmasked match was.\n\nRoute two values from equal scores while excluding the future · Normalize the allowed scores\nBoth allowed exponentials are exp(0.707). Each is divided by their sum, 2*exp(0.707), so the weights are 0.5 and 0.5. The masked future position receives weight 0.\n\nRoute two values from equal scores while excluding the future · Mix values, not keys\nThe output is 0.5*v1 + 0.5*v2 = 0.5*[2, 0] + 0.5*[0, 4] = [1, 2]. Keys affected the weights; the two value vectors supplied the output coordinates.\n\nRoute two values from equal scores while excluding the future · Stop at the interpretation boundary\nThe vector [1, 2] is one attention output that later residual, normalization, and feed-forward operations can transform. The equal weights describe this route only; they do not prove equal causal importance to a final model decision.\n\nRoute two values from equal scores while excluding the future\nA trustworthy attention trace shows fixed projections, query-key scores, masking, softmax denominators, value-weighted sums, and where the inference and explanation claims stop.\n\nCommon confusions\nKeys participate in score calculation. The normalized weights multiply values, and those weighted values are summed into the output.\n\nCommon confusions\nInference uses already learned parameters. Learning requires a loss, backward derivatives, and optimizer updates across training examples.\n\nCommon confusions\nIt is a routing value inside one component. A causal interpretation needs a controlled change to that route and evidence about the resulting output.\n\nBefore you predict\nCompute query-key compatibility only for allowed positions, then apply scaling and masking before normalization.\n\nBefore you predict\nSoftmax gives equal weights to equal finite scores because their exponentials and denominator contributions are equal.\n\nBefore you predict\nWith exactly two allowed equal scores, the next prediction should use weights 0.5 and 0.5, without implying training or causal proof.\n\nThis lesson traces inference, not training\nDuring this forward pass, query, key, and value projection parameters are already fixed. The model computes new token representations; it does not update its weights.\nTraining would add a loss, backpropagate through these operations, and apply optimizer updates across examples. A visible attention pass must not be described as the model learning.\nSelf-attention over content vectors has no inherent token-order signal. Transformers therefore encode position in the input representations or attention computation, for example with positional embeddings, rotary encodings, or position biases. A causal mask only blocks unavailable positions; it is not by itself a full representation of position.\n\nQueries select; values supply content\nFor one token, its query is compared with each allowed key. Scaled dot products become scores; softmax converts them into nonnegative weights summing to one.\nThe output is the weighted sum of value vectors, not key vectors. A key helps determine how much to route; its paired value supplies what is routed.\n\nA causal mask limits available positions\nIn left-to-right inference, the causal mask removes future positions before softmax. Their effective score is negative infinity, so their normalized weight is zero.\nThe mask encodes an information constraint. It is separate from the learned compatibility scores among positions that remain allowed.\n\nA transformer block contains more than attention\nMulti-head attention runs several learned routing projections, then combines their outputs. Residual paths, normalization, and a position-wise feed-forward network transform the representation further.\nTherefore an attention matrix is one intermediate state, not the whole transformer computation.\n\nAttention weight is not causal proof\nA large attention weight shows strong routing under this head and input. It does not by itself prove that the attended token caused the final prediction or expresses the model's reason.\nTrace ML therefore treats a route ablation as an authored causal test: change one route while holding the comparison fixed, then measure whether the output changes. The test can supply intervention evidence; the original attention weight cannot.",
    "activityId": "attention-routing-explanation",
    "activityPrompt": "Explain how increasing one allowed query-key score changes the output representation. Then explain why the resulting larger attention weight is not enough to claim that token caused the final prediction, and name a controlled test.",
    "activityGuidance": "Trace score, softmax weight, value mixture, later paths, and an ablation or replacement.",
    "criteria": [
      {
        "id": "attention-score-weight",
        "label": "connect compatibility score to softmax weight"
      },
      {
        "id": "attention-value-mixture",
        "label": "connect the weight to the value mixture"
      },
      {
        "id": "attention-causal-boundary",
        "label": "deny a complete causal explanation from weights alone"
      },
      {
        "id": "attention-intervention",
        "label": "propose a controlled route intervention"
      }
    ],
    "demonstratedFeedback": "You traced information routing while keeping the causal claim narrower than the visualization.",
    "unsupportedFeedback": "Follow score to softmax to value mixture, then ask what an intervention would need to hold fixed."
  },
  {
    "lessonId": "attention-routing",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Route information with attention",
    "lessonContext": "Separate matching, routing, and interpretation in attention\nSelf-attention builds each token representation by routing information from allowed positions. For one receiving position, a query vector q describes what it is matching. Each source has a key vector k for matching and a value vector v containing information to route. A vector is an ordered list of numbers. These vectors come from learned projections, but their weights are fixed during this inference pass.\n\nSeparate matching, routing, and interpretation in attention\nThe mechanism has visible intermediate states. First, a query-key dot product multiplies corresponding coordinates and adds them to form a compatibility score. Scaled dot-product attention divides that score by the square root of d_k, where d_k is the number of key coordinates. A causal mask then removes forbidden future positions. Softmax exponentiates the remaining scores and divides by their sum, producing nonnegative attention weights that add to 1. Finally, those weights multiply value vectors, which are added to form the routed output.\n\nSeparate matching, routing, and interpretation in attention\nSeveral boundaries prevent overclaiming. Keys determine routing weights; values supply content. A causal mask controls available positions but does not encode their full order. This forward pass changes representations, not parameters; training also requires a loss, backpropagation, and optimizer steps. An attention weight is routing, not proof that one token caused a final prediction. Causal claims require intervention and measured output change.\n\nTerms you need · Query\nA vector q from the receiving position that is compared with candidate keys.\n\nTerms you need · Key\nA vector k from a candidate source position that helps determine its routing weight.\n\nTerms you need · Value\nA vector v containing the source information that is multiplied by an attention weight.\n\nTerms you need · Scaled dot-product score\nA query-key dot product divided by the square root of the key dimension d_k.\n\nTerms you need · Softmax\nA normalization that converts allowed finite scores into positive weights summing to 1.\n\nTerms you need · Causal mask\nAn information boundary that gives unavailable future positions zero weight before left-to-right prediction.\n\nRoute two values from equal scores while excluding the future\nFor one receiving token, use q = [1, 0]. Two allowed source positions have k1 = [1, 0], k2 = [1, 0], v1 = [2, 0], and v2 = [0, 4]. A third future position has a high raw match but is blocked by a causal mask. The key dimension d_k is 2.\n\nRoute two values from equal scores while excluding the future · Freeze the inference state\nTreat q, all keys, all values, and the projection parameters that produced them as fixed. Computing attention from these vectors is a forward pass; no gradient or optimizer update appears in this state.\n\nRoute two values from equal scores while excluding the future · Compute the allowed raw matches\nThe dot products are q dot k1 = 1*1 + 0*0 = 1 and q dot k2 = 1*1 + 0*0 = 1. Equal keys relative to this query produce equal raw scores.\n\nRoute two values from equal scores while excluding the future · Scale and apply the mask\nDividing by sqrt(2) gives two allowed scores of approximately 0.707. The future position's score is replaced by negative infinity before softmax, regardless of how large its unmasked match was.\n\nRoute two values from equal scores while excluding the future · Normalize the allowed scores\nBoth allowed exponentials are exp(0.707). Each is divided by their sum, 2*exp(0.707), so the weights are 0.5 and 0.5. The masked future position receives weight 0.\n\nRoute two values from equal scores while excluding the future · Mix values, not keys\nThe output is 0.5*v1 + 0.5*v2 = 0.5*[2, 0] + 0.5*[0, 4] = [1, 2]. Keys affected the weights; the two value vectors supplied the output coordinates.\n\nRoute two values from equal scores while excluding the future · Stop at the interpretation boundary\nThe vector [1, 2] is one attention output that later residual, normalization, and feed-forward operations can transform. The equal weights describe this route only; they do not prove equal causal importance to a final model decision.\n\nRoute two values from equal scores while excluding the future\nA trustworthy attention trace shows fixed projections, query-key scores, masking, softmax denominators, value-weighted sums, and where the inference and explanation claims stop.\n\nCommon confusions\nKeys participate in score calculation. The normalized weights multiply values, and those weighted values are summed into the output.\n\nCommon confusions\nInference uses already learned parameters. Learning requires a loss, backward derivatives, and optimizer updates across training examples.\n\nCommon confusions\nIt is a routing value inside one component. A causal interpretation needs a controlled change to that route and evidence about the resulting output.\n\nBefore you predict\nCompute query-key compatibility only for allowed positions, then apply scaling and masking before normalization.\n\nBefore you predict\nSoftmax gives equal weights to equal finite scores because their exponentials and denominator contributions are equal.\n\nBefore you predict\nWith exactly two allowed equal scores, the next prediction should use weights 0.5 and 0.5, without implying training or causal proof.\n\nThis lesson traces inference, not training\nDuring this forward pass, query, key, and value projection parameters are already fixed. The model computes new token representations; it does not update its weights.\nTraining would add a loss, backpropagate through these operations, and apply optimizer updates across examples. A visible attention pass must not be described as the model learning.\nSelf-attention over content vectors has no inherent token-order signal. Transformers therefore encode position in the input representations or attention computation, for example with positional embeddings, rotary encodings, or position biases. A causal mask only blocks unavailable positions; it is not by itself a full representation of position.\n\nQueries select; values supply content\nFor one token, its query is compared with each allowed key. Scaled dot products become scores; softmax converts them into nonnegative weights summing to one.\nThe output is the weighted sum of value vectors, not key vectors. A key helps determine how much to route; its paired value supplies what is routed.\n\nA causal mask limits available positions\nIn left-to-right inference, the causal mask removes future positions before softmax. Their effective score is negative infinity, so their normalized weight is zero.\nThe mask encodes an information constraint. It is separate from the learned compatibility scores among positions that remain allowed.\n\nA transformer block contains more than attention\nMulti-head attention runs several learned routing projections, then combines their outputs. Residual paths, normalization, and a position-wise feed-forward network transform the representation further.\nTherefore an attention matrix is one intermediate state, not the whole transformer computation.\n\nAttention weight is not causal proof\nA large attention weight shows strong routing under this head and input. It does not by itself prove that the attended token caused the final prediction or expresses the model's reason.\nTrace ML therefore treats a route ablation as an authored causal test: change one route while holding the comparison fixed, then measure whether the output changes. The test can supply intervention evidence; the original attention weight cannot.",
    "activityId": "attention-dispatch-transfer",
    "activityPrompt": "A dispatcher has one request, three depot descriptions, and three depot payloads containing available inventory. Map request, descriptions, and payloads to query, keys, and values. Explain the weighted response and why a high routing weight does not prove the depot caused the final delivery outcome.",
    "activityGuidance": "Map all three roles, normalization, content mixing, and the downstream causal boundary.",
    "criteria": [
      {
        "id": "dispatch-query",
        "label": "map the request to the query"
      },
      {
        "id": "dispatch-keys",
        "label": "map depot descriptions to keys"
      },
      {
        "id": "dispatch-values",
        "label": "map inventory payloads to values"
      },
      {
        "id": "dispatch-weighted-boundary",
        "label": "describe normalized mixing without claiming delivery causality"
      }
    ],
    "demonstratedFeedback": "You transferred the routing mechanism and preserved the distinction between an intermediate weight and a final cause.",
    "unsupportedFeedback": "Assign request, lookup description, and routed content to three different Q-K-V roles."
  },
  {
    "lessonId": "q-learning",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Learn from delayed consequences",
    "lessonContext": "Trace immediate reward, future value, and the terminal boundary\nReinforcement learning studies an agent that acts and then observes consequences. A state, written s, summarizes the current decision situation. An action a is a choice available in that state. After acting, the environment supplies a reward r and a next state s_prime. A Markov decision process, or MDP, models these states, actions, transition behavior, and rewards. The Markov boundary says the current state contains the information the model uses to describe what happens next.\n\nTrace immediate reward, future value, and the terminal boundary\nQ-learning stores or approximates Q(s, a), an estimate of the expected discounted return for taking action a in state s and then following an optimal target policy. Its update is off-policy: the target uses max_a Q(s_prime, a), while the behavior policy collecting experience may still explore instead of acting greedily. Return means accumulated future rewards. The discount factor gamma lies between 0 and 1 and scales later value relative to immediate reward. For a nonterminal transition, the target is r + gamma * max_a Q(s_prime, a): observed reward plus the best current next-state estimate. Using an estimate inside a target is called bootstrapping.\n\nTrace immediate reward, future value, and the terminal boundary\nThe update exposes one more state. The temporal-difference error is target - current Q(s, a). With learning rate alpha, the new entry is current Q + alpha * error; only the visited state-action entry changes in a table. A terminal transition ends the episode, so no next decision exists and the future-value term must be omitted. This is also the boundary from a stationary context-free bandit: a bandit repeats one decision context, while an MDP represents actions that change future states and opportunities.\n\nTerms you need · State\nThe information s used to describe the current decision situation before an action.\n\nTerms you need · Action\nA choice a available to the agent in a state.\n\nTerms you need · Reward\nA numerical consequence r observed after a transition, not a correct-action label for every state.\n\nTerms you need · Q value\nAn estimate Q(s, a) of expected discounted return for one state-action pair.\n\nTerms you need · Bellman target\nThe immediate reward plus discounted best next-state value, except at a terminal transition.\n\nTerms you need · Temporal-difference error\nThe Bellman target minus the current Q estimate for the visited state-action pair.\n\nUpdate one warehouse decision and then cross the terminal boundary\nA warehouse agent in state s = low stock chooses action a = order. It receives reward r = 2 and reaches state s_prime = stocked. Use gamma = 0.9, current Q(s, a) = 1, best next-state Q value = 5, and learning rate alpha = 0.5.\n\nUpdate one warehouse decision and then cross the terminal boundary · Record the complete transition\nThe observed tuple is (s = low stock, a = order, r = 2, s_prime = stocked, terminal = false). Keeping the terminal flag explicit prevents an unavailable future state from entering the calculation.\n\nUpdate one warehouse decision and then cross the terminal boundary · Form the nonterminal target\nBecause another decision follows, target = r + gamma * max_a Q(s_prime, a) = 2 + 0.9*5 = 6.5. The value 5 is an existing estimate, not an observed reward.\n\nUpdate one warehouse decision and then cross the terminal boundary · Measure the prediction error\nThe temporal-difference error is target - current Q(s, a) = 6.5 - 1 = 5.5. Its positive sign says the visited entry currently underestimates this target.\n\nUpdate one warehouse decision and then cross the terminal boundary · Move the visited entry\nNew Q(s, a) = 1 + 0.5*5.5 = 3.75. The update moves halfway toward 6.5. Other actions and states remain unchanged because this transition supplied no direct update for them.\n\nUpdate one warehouse decision and then cross the terminal boundary · Change only the terminal flag\nNow suppose the same action receives reward 2 and ends the episode. There is no s_prime decision to value, so the target is exactly r = 2. Adding 0.9*5 would invent consequences after termination.\n\nUpdate one warehouse decision and then cross the terminal boundary · Update at termination\nIf the terminal transition's current Q is 1 and alpha remains 0.5, its temporal-difference error is 2 - 1 = 1 and its new Q is 1 + 0.5*1 = 1.5. The target is 2 even though the partial update stops at 1.5.\n\nUpdate one warehouse decision and then cross the terminal boundary\nKeep reward, next-state estimate, terminal flag, target, temporal-difference error, and updated table entry visible as separate quantities.\n\nCommon confusions\nReward evaluates an experienced consequence. The learner must estimate how current actions affect both immediate and later rewards under transitions.\n\nCommon confusions\nIt is included only for a nonterminal transition. At episode termination there is no next action, so the target equals the observed reward.\n\nCommon confusions\nTabular Q-learning changes the visited state-action entry. Other entries change only when their own transitions are updated.\n\nBefore you predict\nWrite the transition and terminal flag before deciding whether a next-state value exists.\n\nBefore you predict\nFor a continuing transition, combine immediate reward with gamma times the best current next-state estimate.\n\nBefore you predict\nFor a terminal transition with reward 2, the Bellman target is 2, not 6.5 or the partially updated Q value.\n\nA bandit has actions but no changing state\nIn the stationary, context-free multi-armed bandit used here, each action produces a reward and the next choice faces the same decision context. Contextual and nonstationary bandits relax those assumptions. The central tension is exploring uncertain actions versus exploiting the best current estimate.\nWhen an action changes what can happen next, the missing state and transition matter. That is the boundary where a Markov decision process is needed.\n\nAn MDP makes consequences explicit\nAn MDP specifies states, available actions, transition behavior, rewards, and a discount factor. The Markov assumption says the current state contains the information used to model the next transition.\nA reward evaluates an observed transition. It is not a correct-action label supplied for every state.\n\nBootstrap from the next state\nSuppose a transition gives reward 2, discount 0.9, and the largest next-state Q value is 5. The Q-learning target is 2 + 0.9 * 5 = 6.5.\nIf the transition ends the episode, there is no future term and the target is only the observed reward.\n\nMove one estimate toward the target\nFor current Q = 1, target 6.5, and learning rate 0.5, the temporal-difference error is 5.5 and the updated Q is 1 + 0.5 * 5.5 = 3.75.\nOnly the visited state-action entry changes in tabular Q-learning. Unvisited estimates do not become correct by association.\n\nThe reward definition is part of the system\nFrom experienced rewards and transitions, Q-learning updates action-value estimates so policy behavior can seek to maximize expected discounted return. It does not optimize the environment's reward function or transition dynamics. Sparse feedback, unsafe exploration, hidden state, changing dynamics, or a misspecified reward can make a numerically correct update produce unwanted behavior.",
    "activityId": "q-bootstrap-explanation",
    "activityPrompt": "Explain why increasing the best next-state Q raises a nonterminal target but cannot affect a terminal target. Distinguish reward from a supervised label and state a transition that would falsify your account.",
    "activityGuidance": "Use reward, discount, next-state value, terminal handling, and one controlled comparison.",
    "criteria": [
      {
        "id": "q-nonterminal-target",
        "label": "state the nonterminal Bellman target"
      },
      {
        "id": "q-terminal-boundary",
        "label": "remove future value at a terminal state"
      },
      {
        "id": "q-reward-not-label",
        "label": "distinguish reward from a correct-action label"
      },
      {
        "id": "q-falsifier",
        "label": "state a terminal or discount-zero falsifier"
      }
    ],
    "demonstratedFeedback": "You traced the bootstrap term, its terminal boundary, and the difference between consequence and label.",
    "unsupportedFeedback": "Write separate target equations for continuing and terminal transitions."
  },
  {
    "lessonId": "q-learning",
    "lessonRevision": "2026-08-04-r1",
    "lessonTitle": "Learn from delayed consequences",
    "lessonContext": "Trace immediate reward, future value, and the terminal boundary\nReinforcement learning studies an agent that acts and then observes consequences. A state, written s, summarizes the current decision situation. An action a is a choice available in that state. After acting, the environment supplies a reward r and a next state s_prime. A Markov decision process, or MDP, models these states, actions, transition behavior, and rewards. The Markov boundary says the current state contains the information the model uses to describe what happens next.\n\nTrace immediate reward, future value, and the terminal boundary\nQ-learning stores or approximates Q(s, a), an estimate of the expected discounted return for taking action a in state s and then following an optimal target policy. Its update is off-policy: the target uses max_a Q(s_prime, a), while the behavior policy collecting experience may still explore instead of acting greedily. Return means accumulated future rewards. The discount factor gamma lies between 0 and 1 and scales later value relative to immediate reward. For a nonterminal transition, the target is r + gamma * max_a Q(s_prime, a): observed reward plus the best current next-state estimate. Using an estimate inside a target is called bootstrapping.\n\nTrace immediate reward, future value, and the terminal boundary\nThe update exposes one more state. The temporal-difference error is target - current Q(s, a). With learning rate alpha, the new entry is current Q + alpha * error; only the visited state-action entry changes in a table. A terminal transition ends the episode, so no next decision exists and the future-value term must be omitted. This is also the boundary from a stationary context-free bandit: a bandit repeats one decision context, while an MDP represents actions that change future states and opportunities.\n\nTerms you need · State\nThe information s used to describe the current decision situation before an action.\n\nTerms you need · Action\nA choice a available to the agent in a state.\n\nTerms you need · Reward\nA numerical consequence r observed after a transition, not a correct-action label for every state.\n\nTerms you need · Q value\nAn estimate Q(s, a) of expected discounted return for one state-action pair.\n\nTerms you need · Bellman target\nThe immediate reward plus discounted best next-state value, except at a terminal transition.\n\nTerms you need · Temporal-difference error\nThe Bellman target minus the current Q estimate for the visited state-action pair.\n\nUpdate one warehouse decision and then cross the terminal boundary\nA warehouse agent in state s = low stock chooses action a = order. It receives reward r = 2 and reaches state s_prime = stocked. Use gamma = 0.9, current Q(s, a) = 1, best next-state Q value = 5, and learning rate alpha = 0.5.\n\nUpdate one warehouse decision and then cross the terminal boundary · Record the complete transition\nThe observed tuple is (s = low stock, a = order, r = 2, s_prime = stocked, terminal = false). Keeping the terminal flag explicit prevents an unavailable future state from entering the calculation.\n\nUpdate one warehouse decision and then cross the terminal boundary · Form the nonterminal target\nBecause another decision follows, target = r + gamma * max_a Q(s_prime, a) = 2 + 0.9*5 = 6.5. The value 5 is an existing estimate, not an observed reward.\n\nUpdate one warehouse decision and then cross the terminal boundary · Measure the prediction error\nThe temporal-difference error is target - current Q(s, a) = 6.5 - 1 = 5.5. Its positive sign says the visited entry currently underestimates this target.\n\nUpdate one warehouse decision and then cross the terminal boundary · Move the visited entry\nNew Q(s, a) = 1 + 0.5*5.5 = 3.75. The update moves halfway toward 6.5. Other actions and states remain unchanged because this transition supplied no direct update for them.\n\nUpdate one warehouse decision and then cross the terminal boundary · Change only the terminal flag\nNow suppose the same action receives reward 2 and ends the episode. There is no s_prime decision to value, so the target is exactly r = 2. Adding 0.9*5 would invent consequences after termination.\n\nUpdate one warehouse decision and then cross the terminal boundary · Update at termination\nIf the terminal transition's current Q is 1 and alpha remains 0.5, its temporal-difference error is 2 - 1 = 1 and its new Q is 1 + 0.5*1 = 1.5. The target is 2 even though the partial update stops at 1.5.\n\nUpdate one warehouse decision and then cross the terminal boundary\nKeep reward, next-state estimate, terminal flag, target, temporal-difference error, and updated table entry visible as separate quantities.\n\nCommon confusions\nReward evaluates an experienced consequence. The learner must estimate how current actions affect both immediate and later rewards under transitions.\n\nCommon confusions\nIt is included only for a nonterminal transition. At episode termination there is no next action, so the target equals the observed reward.\n\nCommon confusions\nTabular Q-learning changes the visited state-action entry. Other entries change only when their own transitions are updated.\n\nBefore you predict\nWrite the transition and terminal flag before deciding whether a next-state value exists.\n\nBefore you predict\nFor a continuing transition, combine immediate reward with gamma times the best current next-state estimate.\n\nBefore you predict\nFor a terminal transition with reward 2, the Bellman target is 2, not 6.5 or the partially updated Q value.\n\nA bandit has actions but no changing state\nIn the stationary, context-free multi-armed bandit used here, each action produces a reward and the next choice faces the same decision context. Contextual and nonstationary bandits relax those assumptions. The central tension is exploring uncertain actions versus exploiting the best current estimate.\nWhen an action changes what can happen next, the missing state and transition matter. That is the boundary where a Markov decision process is needed.\n\nAn MDP makes consequences explicit\nAn MDP specifies states, available actions, transition behavior, rewards, and a discount factor. The Markov assumption says the current state contains the information used to model the next transition.\nA reward evaluates an observed transition. It is not a correct-action label supplied for every state.\n\nBootstrap from the next state\nSuppose a transition gives reward 2, discount 0.9, and the largest next-state Q value is 5. The Q-learning target is 2 + 0.9 * 5 = 6.5.\nIf the transition ends the episode, there is no future term and the target is only the observed reward.\n\nMove one estimate toward the target\nFor current Q = 1, target 6.5, and learning rate 0.5, the temporal-difference error is 5.5 and the updated Q is 1 + 0.5 * 5.5 = 3.75.\nOnly the visited state-action entry changes in tabular Q-learning. Unvisited estimates do not become correct by association.\n\nThe reward definition is part of the system\nFrom experienced rewards and transitions, Q-learning updates action-value estimates so policy behavior can seek to maximize expected discounted return. It does not optimize the environment's reward function or transition dynamics. Sparse feedback, unsafe exploration, hidden state, changing dynamics, or a misspecified reward can make a numerically correct update produce unwanted behavior.",
    "activityId": "q-inventory-transfer",
    "activityPrompt": "A warehouse chooses how many units to reorder. The choice changes tomorrow's inventory, storage cost, and stockout risk. Explain why this is not a context-free bandit, then define a state, an action, a reward, and the future term in one Bellman target.",
    "activityGuidance": "Make the changing inventory state and delayed consequence explicit.",
    "criteria": [
      {
        "id": "inventory-not-bandit",
        "label": "explain that the action changes later decisions"
      },
      {
        "id": "inventory-action",
        "label": "define the reorder quantity as the action"
      },
      {
        "id": "inventory-reward",
        "label": "define reward from cost or service outcomes"
      },
      {
        "id": "inventory-future",
        "label": "include discounted best next-state value"
      }
    ],
    "demonstratedFeedback": "You represented the warehouse as a stateful decision process with delayed consequences.",
    "unsupportedFeedback": "The key question is whether today's action changes tomorrow's decision state."
  },
  {
    "lessonId": "shift-monitor",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Diagnose the deployed system",
    "lessonContext": "Localize a deployment failure before changing the model\nA deployed machine-learning system is more than model parameters. It includes an artifact, which is the exact packaged model; preprocessing code that converts raw inputs into model features; a decision threshold that turns a score into an action; and serving code that executes the pipeline. Deployment monitoring compares this live system with a reference period. Distribution shift means the statistical pattern of live inputs or outcomes differs from that reference.\n\nLocalize a deployment failure before changing the model\nDifferent evidence arrives at different times. Immediate monitors can check schemas, missing values, feature distributions, score distributions, model identity, latency, and decision rates. Outcome metrics such as accuracy or false-negative rate require reliable labels and may arrive later. A false negative is an actually positive case predicted negative. A slice is a named subset, such as day parcels or night parcels, examined separately because one overall average can hide a serious local failure.\n\nLocalize a deployment failure before changing the model\nFairness asks how a model's decisions and errors are distributed across stakeholder groups in a particular use context. Demographic parity compares how often groups receive the positive decision, while equality of opportunity compares how often actually positive cases receive that decision. These metrics answer different questions, and neither one alone decides whether the system's consequences are acceptable.\n\nLocalize a deployment failure before changing the model\nDiagnosis begins by listing what changed and what remained fixed. An input alert establishes change but not root cause; the cause might be data collection, preprocessing, serving, model behavior, or delayed-label quality. Pinning versions narrows the search. A mitigation limits harm while evidence is gathered, and a release gate is a measurable condition that must pass before automation resumes. Retraining is only one possible response and should not precede checks of the measurement and evaluation boundaries.\n\nTerms you need · Model artifact\nThe exact immutable package of learned parameters and model structure used for a deployment.\n\nTerms you need · Preprocessing\nThe code and rules that transform raw deployment inputs into the features expected by the model.\n\nTerms you need · Distribution shift\nA measurable difference between reference and live input, prediction, or outcome distributions.\n\nTerms you need · Operational slice\nA deployment subset defined by conditions such as device, site, or time and evaluated separately.\n\nTerms you need · False-negative rate\nFalse negatives divided by all actually positive cases in the evaluated group.\n\nTerms you need · Demographic parity\nA group comparison that asks whether each group receives the model's positive decision at the same rate.\n\nTerms you need · Equality of opportunity\nA group comparison that asks whether actually positive cases receive the model's positive decision at the same true-positive rate.\n\nTerms you need · Release gate\nA predeclared measurable condition that must pass before a system change or automation is released.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time\nA package-damage model moves from mostly bright Scanner A images to a night facility using Scanner B. Model 3.2, artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., serving code 1.8.4, preprocessing 2.3.1, and threshold 0.60 are pinned.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Verify system identity\nThe artifact, training trace, serving code, preprocessing version, and threshold are unchanged between reference and live observations. This does not prove correct behavior, but it makes an unexplained new optimizer update or model replacement an unsupported first hypothesis.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Read immediate input evidence\nMean brightness falls from 0.62 to 0.39, a shift of 0.23 that exceeds the 0.20 alert threshold. Missing barcodes rise from 2 percent to 14 percent.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Add delayed outcome evidence\nWhen reviewed labels arrive, accuracy falls from 92 percent to 83 percent. This confirms outcome degradation, while the earlier input monitors show that measurement changes were already visible before labels.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Expose the slice boundary\nDay false-negative rate is 8/(72 + 8) = 10 percent. Night false-negative rate is 6/(14 + 6) = 30 percent. The 20-point gap exceeds the 15-point alert threshold and would be blurred by one aggregate.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Localize the first investigation\nScanner conditions, brightness, and barcode missingness changed while the model and software identities stayed pinned. The first investigation should therefore compare deployment data and preprocessing for Scanner B, then replay the frozen model on matched scanner slices.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Mitigate and define verification\nPause unsupported night automation, preserve logs, validate Scanner B inputs, and collect reviewed night labels. The current overall false-negative rate of 14 percent fails its 10 percent gate, and the 30 percent night rate fails its 15 percent gate.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time\nIdentity evidence narrows what could have changed; immediate inputs detect the incident early; delayed labels and slices quantify harm; gates define what recovery must demonstrate.\n\nCommon confusions\nAn alert proves a monitored distribution changed. Root cause still requires identity checks, pipeline replay, slice evaluation, and incident context.\n\nCommon confusions\nAn aggregate can hide a smaller slice, and equality on one fairness metric can coexist with a gap on another. Inspect relevant group confusion matrices, label quality, exposure, and consequences.\n\nCommon confusions\nFirst contain harm and verify data collection, preprocessing, serving identity, labels, and evaluation coverage. Retrain only when evidence shows parameters should change.\n\nBefore you predict\nSeparate fixed system identity from changed observations before proposing a cause.\n\nBefore you predict\nUse immediate input monitors for early detection and delayed labeled slices for outcome and reliability evidence.\n\nBefore you predict\nCompute selection rates and true-positive rates separately before making a demographic-parity or equality-of-opportunity claim.\n\nBefore you predict\nWhen scanner measurements move sharply while the artifact and pipeline versions remain pinned, investigate deployment data and preprocessing first.\n\nCapstone incident: the model did not change\nA fixed package-damage model was trained mostly on bright images from Scanner A. The rollout fixture pins model version 3.2, artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., serving code 1.8.4, preprocessing 2.3.1, and threshold 0.60. At the new night facility, Scanner B produces darker images and a rising missing-barcode rate.\nNo lesson, incident detail, or diagnosis branch is generated at runtime. The evidence below is the authored capstone case.\n\nLink each observation to a pipeline layer\nInput brightness mean moves from 0.62 to 0.39 and missing barcodes rise from 2% to 14%. Delayed labels later show accuracy falling from 92% to 83%. False-negative rate is 10% for day parcels and 30% for night parcels.\nThe incident fixture keeps artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., threshold 0.60, serving code 1.8.4, and preprocessing 2.3.1 pinned across the reference and live observations. This evidence first implicates deployment inputs, the measurement path, and operational-slice reliability, not a new optimizer failure.\n\nFairness metrics compare different parts of each group\nAn overall metric averages across the deployment mixture. The day and night scanner slices can expose an operational reliability failure while the aggregate moves only modestly, but that gap alone does not establish a fairness harm.\nWithin each stakeholder group, a confusion matrix counts four outcomes. A true positive (TP) is an actually positive case predicted positive; a false negative (FN) is actually positive but predicted negative; a false positive (FP) is actually negative but predicted positive; and a true negative (TN) is actually negative and predicted negative. Selection rate is (TP + FP) divided by all four counts. True-positive rate is TP / (TP + FN).\nConsider two groups of 100 cases. Group A has TP = 40, FN = 10, FP = 10, and TN = 40, so its selection rate is (40 + 10) / 100 = 50% and its true-positive rate is 40 / (40 + 10) = 80%. Group B has TP = 20, FN = 20, FP = 0, and TN = 60, so its selection rate is 20% and its true-positive rate is 50%. The 30-point selection-rate gap violates demographic parity, and the 30-point true-positive-rate gap violates equality of opportunity.\nThe metrics can trade off. To raise Group B's true-positive rate from 50% to 80% with 40 actually positive cases, 12 false negatives would need to become true positives, giving TP = 32 and FN = 8. Selection would then be at least 32%. Reaching Group A's 50% selection rate as well could, for example, add 18 false positives, giving FP = 18, TN = 42, and a 30% false-positive rate. Group A's false-positive rate is 10 / 50 = 20%, so matching the first two metrics would leave a 10-point false-positive-rate gap.\nMetric choice therefore requires the decision's purpose, affected stakeholders, error costs, group definitions, and label quality. Demographic parity can be computed from decisions and group membership before outcomes arrive; equality of opportunity requires trustworthy outcome labels. Equalizing one metric does not by itself establish fairness.\n\nMonitor before and after outcomes arrive\nImmediate monitors can track schema, missingness, feature distributions, model version, latency, score distributions, decision rates, and pre-label selection-rate parity when reliable group membership is available. Outcome-dependent performance and fairness measures such as false-negative-rate parity or equality of opportunity require reliable labels, which may arrive later.\nA drift alert is evidence that behavior or inputs changed, not proof of the root cause. Diagnosis combines monitors with controlled replay, slice evaluation, and incident context.\n\nMitigate, verify, and keep the boundary visible\nThe authored response is to pause unsupported night automation, preserve logs, validate Scanner B preprocessing, collect reviewed night labels, compare the frozen model on matched scanner slices, and release only after overall and night false-negative gates pass.\nRetraining is not the first automatic answer. Repair the measurement path and evaluation coverage before choosing whether model parameters must change.",
    "activityId": "shift-capstone-diagnosis",
    "activityPrompt": "Diagnose the fixed Scanner B incident. Build the causal evidence chain, reject optimizer failure as the first explanation, identify the hidden night-slice reliability failure, and state a controlled result that would challenge the data-shift diagnosis.",
    "activityGuidance": "Use the pinned deployment identity, changed input measurements, delayed outcome slices, and a matched replay.",
    "criteria": [
      {
        "id": "shift-input-evidence",
        "label": "identify brightness and missingness shift"
      },
      {
        "id": "shift-fixed-model",
        "label": "use frozen model evidence against optimizer failure"
      },
      {
        "id": "shift-slice-reliability",
        "label": "identify the night-slice false-negative reliability gap"
      },
      {
        "id": "shift-challenge",
        "label": "state a matched scanner or preprocessing replay"
      }
    ],
    "demonstratedFeedback": "You localized the incident from changed measurements to an operational-slice reliability failure and proposed a test that could overturn the diagnosis.",
    "unsupportedFeedback": "Order the evidence by time: pinned deployment identity, immediate input drift, then delayed operational-slice outcomes."
  },
  {
    "lessonId": "shift-monitor",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Diagnose the deployed system",
    "lessonContext": "Localize a deployment failure before changing the model\nA deployed machine-learning system is more than model parameters. It includes an artifact, which is the exact packaged model; preprocessing code that converts raw inputs into model features; a decision threshold that turns a score into an action; and serving code that executes the pipeline. Deployment monitoring compares this live system with a reference period. Distribution shift means the statistical pattern of live inputs or outcomes differs from that reference.\n\nLocalize a deployment failure before changing the model\nDifferent evidence arrives at different times. Immediate monitors can check schemas, missing values, feature distributions, score distributions, model identity, latency, and decision rates. Outcome metrics such as accuracy or false-negative rate require reliable labels and may arrive later. A false negative is an actually positive case predicted negative. A slice is a named subset, such as day parcels or night parcels, examined separately because one overall average can hide a serious local failure.\n\nLocalize a deployment failure before changing the model\nFairness asks how a model's decisions and errors are distributed across stakeholder groups in a particular use context. Demographic parity compares how often groups receive the positive decision, while equality of opportunity compares how often actually positive cases receive that decision. These metrics answer different questions, and neither one alone decides whether the system's consequences are acceptable.\n\nLocalize a deployment failure before changing the model\nDiagnosis begins by listing what changed and what remained fixed. An input alert establishes change but not root cause; the cause might be data collection, preprocessing, serving, model behavior, or delayed-label quality. Pinning versions narrows the search. A mitigation limits harm while evidence is gathered, and a release gate is a measurable condition that must pass before automation resumes. Retraining is only one possible response and should not precede checks of the measurement and evaluation boundaries.\n\nTerms you need · Model artifact\nThe exact immutable package of learned parameters and model structure used for a deployment.\n\nTerms you need · Preprocessing\nThe code and rules that transform raw deployment inputs into the features expected by the model.\n\nTerms you need · Distribution shift\nA measurable difference between reference and live input, prediction, or outcome distributions.\n\nTerms you need · Operational slice\nA deployment subset defined by conditions such as device, site, or time and evaluated separately.\n\nTerms you need · False-negative rate\nFalse negatives divided by all actually positive cases in the evaluated group.\n\nTerms you need · Demographic parity\nA group comparison that asks whether each group receives the model's positive decision at the same rate.\n\nTerms you need · Equality of opportunity\nA group comparison that asks whether actually positive cases receive the model's positive decision at the same true-positive rate.\n\nTerms you need · Release gate\nA predeclared measurable condition that must pass before a system change or automation is released.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time\nA package-damage model moves from mostly bright Scanner A images to a night facility using Scanner B. Model 3.2, artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., serving code 1.8.4, preprocessing 2.3.1, and threshold 0.60 are pinned.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Verify system identity\nThe artifact, training trace, serving code, preprocessing version, and threshold are unchanged between reference and live observations. This does not prove correct behavior, but it makes an unexplained new optimizer update or model replacement an unsupported first hypothesis.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Read immediate input evidence\nMean brightness falls from 0.62 to 0.39, a shift of 0.23 that exceeds the 0.20 alert threshold. Missing barcodes rise from 2 percent to 14 percent.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Add delayed outcome evidence\nWhen reviewed labels arrive, accuracy falls from 92 percent to 83 percent. This confirms outcome degradation, while the earlier input monitors show that measurement changes were already visible before labels.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Expose the slice boundary\nDay false-negative rate is 8/(72 + 8) = 10 percent. Night false-negative rate is 6/(14 + 6) = 30 percent. The 20-point gap exceeds the 15-point alert threshold and would be blurred by one aggregate.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Localize the first investigation\nScanner conditions, brightness, and barcode missingness changed while the model and software identities stayed pinned. The first investigation should therefore compare deployment data and preprocessing for Scanner B, then replay the frozen model on matched scanner slices.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Mitigate and define verification\nPause unsupported night automation, preserve logs, validate Scanner B inputs, and collect reviewed night labels. The current overall false-negative rate of 14 percent fails its 10 percent gate, and the 30 percent night rate fails its 15 percent gate.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time\nIdentity evidence narrows what could have changed; immediate inputs detect the incident early; delayed labels and slices quantify harm; gates define what recovery must demonstrate.\n\nCommon confusions\nAn alert proves a monitored distribution changed. Root cause still requires identity checks, pipeline replay, slice evaluation, and incident context.\n\nCommon confusions\nAn aggregate can hide a smaller slice, and equality on one fairness metric can coexist with a gap on another. Inspect relevant group confusion matrices, label quality, exposure, and consequences.\n\nCommon confusions\nFirst contain harm and verify data collection, preprocessing, serving identity, labels, and evaluation coverage. Retrain only when evidence shows parameters should change.\n\nBefore you predict\nSeparate fixed system identity from changed observations before proposing a cause.\n\nBefore you predict\nUse immediate input monitors for early detection and delayed labeled slices for outcome and reliability evidence.\n\nBefore you predict\nCompute selection rates and true-positive rates separately before making a demographic-parity or equality-of-opportunity claim.\n\nBefore you predict\nWhen scanner measurements move sharply while the artifact and pipeline versions remain pinned, investigate deployment data and preprocessing first.\n\nCapstone incident: the model did not change\nA fixed package-damage model was trained mostly on bright images from Scanner A. The rollout fixture pins model version 3.2, artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., serving code 1.8.4, preprocessing 2.3.1, and threshold 0.60. At the new night facility, Scanner B produces darker images and a rising missing-barcode rate.\nNo lesson, incident detail, or diagnosis branch is generated at runtime. The evidence below is the authored capstone case.\n\nLink each observation to a pipeline layer\nInput brightness mean moves from 0.62 to 0.39 and missing barcodes rise from 2% to 14%. Delayed labels later show accuracy falling from 92% to 83%. False-negative rate is 10% for day parcels and 30% for night parcels.\nThe incident fixture keeps artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., threshold 0.60, serving code 1.8.4, and preprocessing 2.3.1 pinned across the reference and live observations. This evidence first implicates deployment inputs, the measurement path, and operational-slice reliability, not a new optimizer failure.\n\nFairness metrics compare different parts of each group\nAn overall metric averages across the deployment mixture. The day and night scanner slices can expose an operational reliability failure while the aggregate moves only modestly, but that gap alone does not establish a fairness harm.\nWithin each stakeholder group, a confusion matrix counts four outcomes. A true positive (TP) is an actually positive case predicted positive; a false negative (FN) is actually positive but predicted negative; a false positive (FP) is actually negative but predicted positive; and a true negative (TN) is actually negative and predicted negative. Selection rate is (TP + FP) divided by all four counts. True-positive rate is TP / (TP + FN).\nConsider two groups of 100 cases. Group A has TP = 40, FN = 10, FP = 10, and TN = 40, so its selection rate is (40 + 10) / 100 = 50% and its true-positive rate is 40 / (40 + 10) = 80%. Group B has TP = 20, FN = 20, FP = 0, and TN = 60, so its selection rate is 20% and its true-positive rate is 50%. The 30-point selection-rate gap violates demographic parity, and the 30-point true-positive-rate gap violates equality of opportunity.\nThe metrics can trade off. To raise Group B's true-positive rate from 50% to 80% with 40 actually positive cases, 12 false negatives would need to become true positives, giving TP = 32 and FN = 8. Selection would then be at least 32%. Reaching Group A's 50% selection rate as well could, for example, add 18 false positives, giving FP = 18, TN = 42, and a 30% false-positive rate. Group A's false-positive rate is 10 / 50 = 20%, so matching the first two metrics would leave a 10-point false-positive-rate gap.\nMetric choice therefore requires the decision's purpose, affected stakeholders, error costs, group definitions, and label quality. Demographic parity can be computed from decisions and group membership before outcomes arrive; equality of opportunity requires trustworthy outcome labels. Equalizing one metric does not by itself establish fairness.\n\nMonitor before and after outcomes arrive\nImmediate monitors can track schema, missingness, feature distributions, model version, latency, score distributions, decision rates, and pre-label selection-rate parity when reliable group membership is available. Outcome-dependent performance and fairness measures such as false-negative-rate parity or equality of opportunity require reliable labels, which may arrive later.\nA drift alert is evidence that behavior or inputs changed, not proof of the root cause. Diagnosis combines monitors with controlled replay, slice evaluation, and incident context.\n\nMitigate, verify, and keep the boundary visible\nThe authored response is to pause unsupported night automation, preserve logs, validate Scanner B preprocessing, collect reviewed night labels, compare the frozen model on matched scanner slices, and release only after overall and night false-negative gates pass.\nRetraining is not the first automatic answer. Repair the measurement path and evaluation coverage before choosing whether model parameters must change.",
    "activityId": "shift-clinic-fairness-explanation",
    "activityPrompt": "A fixed clinic referral model has these reviewed outcomes. Group East: TP = 27, FN = 3, FP = 15, TN = 55. Group West: TP = 36, FN = 24, FP = 4, TN = 36. Compute each group's selection rate and true-positive rate. State what demographic parity and equality of opportunity compare, report which observed gap is smaller and which is larger without assuming a fairness tolerance, then explain one threshold tradeoff and one reason these two metrics alone cannot settle whether the system is fair.",
    "activityGuidance": "Use all four confusion-matrix counts for selection rate, only actually positive cases for true-positive rate, and keep the metric calculation separate from the stakeholder judgment.",
    "criteria": [
      {
        "id": "clinic-selection-rates",
        "label": "compute East and West selection rates as 42% and 40%"
      },
      {
        "id": "clinic-true-positive-rates",
        "label": "compute East and West true-positive rates as 90% and 60%"
      },
      {
        "id": "clinic-demographic-parity",
        "label": "report the two-point selection-rate gap as smaller than the 30-point true-positive-rate gap"
      },
      {
        "id": "clinic-equality-opportunity",
        "label": "identify the 30-point true-positive-rate gap as unequal opportunity"
      },
      {
        "id": "clinic-tradeoff-boundary",
        "label": "connect a threshold change to another error and preserve the stakeholder-context boundary"
      }
    ],
    "demonstratedFeedback": "You computed both group comparisons, compared their observed gaps without inventing a fairness tolerance, and kept the metric tradeoff separate from the final fairness judgment.",
    "unsupportedFeedback": "Recompute selection from every case and true-positive rate from actually positive cases, then name what a threshold changes and what stakeholder evidence is still missing."
  },
  {
    "lessonId": "shift-monitor",
    "lessonRevision": "2026-08-04-r2",
    "lessonTitle": "Diagnose the deployed system",
    "lessonContext": "Localize a deployment failure before changing the model\nA deployed machine-learning system is more than model parameters. It includes an artifact, which is the exact packaged model; preprocessing code that converts raw inputs into model features; a decision threshold that turns a score into an action; and serving code that executes the pipeline. Deployment monitoring compares this live system with a reference period. Distribution shift means the statistical pattern of live inputs or outcomes differs from that reference.\n\nLocalize a deployment failure before changing the model\nDifferent evidence arrives at different times. Immediate monitors can check schemas, missing values, feature distributions, score distributions, model identity, latency, and decision rates. Outcome metrics such as accuracy or false-negative rate require reliable labels and may arrive later. A false negative is an actually positive case predicted negative. A slice is a named subset, such as day parcels or night parcels, examined separately because one overall average can hide a serious local failure.\n\nLocalize a deployment failure before changing the model\nFairness asks how a model's decisions and errors are distributed across stakeholder groups in a particular use context. Demographic parity compares how often groups receive the positive decision, while equality of opportunity compares how often actually positive cases receive that decision. These metrics answer different questions, and neither one alone decides whether the system's consequences are acceptable.\n\nLocalize a deployment failure before changing the model\nDiagnosis begins by listing what changed and what remained fixed. An input alert establishes change but not root cause; the cause might be data collection, preprocessing, serving, model behavior, or delayed-label quality. Pinning versions narrows the search. A mitigation limits harm while evidence is gathered, and a release gate is a measurable condition that must pass before automation resumes. Retraining is only one possible response and should not precede checks of the measurement and evaluation boundaries.\n\nTerms you need · Model artifact\nThe exact immutable package of learned parameters and model structure used for a deployment.\n\nTerms you need · Preprocessing\nThe code and rules that transform raw deployment inputs into the features expected by the model.\n\nTerms you need · Distribution shift\nA measurable difference between reference and live input, prediction, or outcome distributions.\n\nTerms you need · Operational slice\nA deployment subset defined by conditions such as device, site, or time and evaluated separately.\n\nTerms you need · False-negative rate\nFalse negatives divided by all actually positive cases in the evaluated group.\n\nTerms you need · Demographic parity\nA group comparison that asks whether each group receives the model's positive decision at the same rate.\n\nTerms you need · Equality of opportunity\nA group comparison that asks whether actually positive cases receive the model's positive decision at the same true-positive rate.\n\nTerms you need · Release gate\nA predeclared measurable condition that must pass before a system change or automation is released.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time\nA package-damage model moves from mostly bright Scanner A images to a night facility using Scanner B. Model 3.2, artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., serving code 1.8.4, preprocessing 2.3.1, and threshold 0.60 are pinned.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Verify system identity\nThe artifact, training trace, serving code, preprocessing version, and threshold are unchanged between reference and live observations. This does not prove correct behavior, but it makes an unexplained new optimizer update or model replacement an unsupported first hypothesis.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Read immediate input evidence\nMean brightness falls from 0.62 to 0.39, a shift of 0.23 that exceeds the 0.20 alert threshold. Missing barcodes rise from 2 percent to 14 percent.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Add delayed outcome evidence\nWhen reviewed labels arrive, accuracy falls from 92 percent to 83 percent. This confirms outcome degradation, while the earlier input monitors show that measurement changes were already visible before labels.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Expose the slice boundary\nDay false-negative rate is 8/(72 + 8) = 10 percent. Night false-negative rate is 6/(14 + 6) = 30 percent. The 20-point gap exceeds the 15-point alert threshold and would be blurred by one aggregate.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Localize the first investigation\nScanner conditions, brightness, and barcode missingness changed while the model and software identities stayed pinned. The first investigation should therefore compare deployment data and preprocessing for Scanner B, then replay the frozen model on matched scanner slices.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time · Mitigate and define verification\nPause unsupported night automation, preserve logs, validate Scanner B inputs, and collect reviewed night labels. The current overall false-negative rate of 14 percent fails its 10 percent gate, and the 30 percent night rate fails its 15 percent gate.\n\nDiagnose the fixed Scanner B incident one evidence layer at a time\nIdentity evidence narrows what could have changed; immediate inputs detect the incident early; delayed labels and slices quantify harm; gates define what recovery must demonstrate.\n\nCommon confusions\nAn alert proves a monitored distribution changed. Root cause still requires identity checks, pipeline replay, slice evaluation, and incident context.\n\nCommon confusions\nAn aggregate can hide a smaller slice, and equality on one fairness metric can coexist with a gap on another. Inspect relevant group confusion matrices, label quality, exposure, and consequences.\n\nCommon confusions\nFirst contain harm and verify data collection, preprocessing, serving identity, labels, and evaluation coverage. Retrain only when evidence shows parameters should change.\n\nBefore you predict\nSeparate fixed system identity from changed observations before proposing a cause.\n\nBefore you predict\nUse immediate input monitors for early detection and delayed labeled slices for outcome and reliability evidence.\n\nBefore you predict\nCompute selection rates and true-positive rates separately before making a demographic-parity or equality-of-opportunity claim.\n\nBefore you predict\nWhen scanner measurements move sharply while the artifact and pipeline versions remain pinned, investigate deployment data and preprocessing first.\n\nCapstone incident: the model did not change\nA fixed package-damage model was trained mostly on bright images from Scanner A. The rollout fixture pins model version 3.2, artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., serving code 1.8.4, preprocessing 2.3.1, and threshold 0.60. At the new night facility, Scanner B produces darker images and a rising missing-barcode rate.\nNo lesson, incident detail, or diagnosis branch is generated at runtime. The evidence below is the authored capstone case.\n\nLink each observation to a pipeline layer\nInput brightness mean moves from 0.62 to 0.39 and missing barcodes rise from 2% to 14%. Delayed labels later show accuracy falling from 92% to 83%. False-negative rate is 10% for day parcels and 30% for night parcels.\nThe incident fixture keeps artifact sha256:a65bfe6cceda..., training trace sha256:912fe7955410..., threshold 0.60, serving code 1.8.4, and preprocessing 2.3.1 pinned across the reference and live observations. This evidence first implicates deployment inputs, the measurement path, and operational-slice reliability, not a new optimizer failure.\n\nFairness metrics compare different parts of each group\nAn overall metric averages across the deployment mixture. The day and night scanner slices can expose an operational reliability failure while the aggregate moves only modestly, but that gap alone does not establish a fairness harm.\nWithin each stakeholder group, a confusion matrix counts four outcomes. A true positive (TP) is an actually positive case predicted positive; a false negative (FN) is actually positive but predicted negative; a false positive (FP) is actually negative but predicted positive; and a true negative (TN) is actually negative and predicted negative. Selection rate is (TP + FP) divided by all four counts. True-positive rate is TP / (TP + FN).\nConsider two groups of 100 cases. Group A has TP = 40, FN = 10, FP = 10, and TN = 40, so its selection rate is (40 + 10) / 100 = 50% and its true-positive rate is 40 / (40 + 10) = 80%. Group B has TP = 20, FN = 20, FP = 0, and TN = 60, so its selection rate is 20% and its true-positive rate is 50%. The 30-point selection-rate gap violates demographic parity, and the 30-point true-positive-rate gap violates equality of opportunity.\nThe metrics can trade off. To raise Group B's true-positive rate from 50% to 80% with 40 actually positive cases, 12 false negatives would need to become true positives, giving TP = 32 and FN = 8. Selection would then be at least 32%. Reaching Group A's 50% selection rate as well could, for example, add 18 false positives, giving FP = 18, TN = 42, and a 30% false-positive rate. Group A's false-positive rate is 10 / 50 = 20%, so matching the first two metrics would leave a 10-point false-positive-rate gap.\nMetric choice therefore requires the decision's purpose, affected stakeholders, error costs, group definitions, and label quality. Demographic parity can be computed from decisions and group membership before outcomes arrive; equality of opportunity requires trustworthy outcome labels. Equalizing one metric does not by itself establish fairness.\n\nMonitor before and after outcomes arrive\nImmediate monitors can track schema, missingness, feature distributions, model version, latency, score distributions, decision rates, and pre-label selection-rate parity when reliable group membership is available. Outcome-dependent performance and fairness measures such as false-negative-rate parity or equality of opportunity require reliable labels, which may arrive later.\nA drift alert is evidence that behavior or inputs changed, not proof of the root cause. Diagnosis combines monitors with controlled replay, slice evaluation, and incident context.\n\nMitigate, verify, and keep the boundary visible\nThe authored response is to pause unsupported night automation, preserve logs, validate Scanner B preprocessing, collect reviewed night labels, compare the frozen model on matched scanner slices, and release only after overall and night false-negative gates pass.\nRetraining is not the first automatic answer. Repair the measurement path and evaluation coverage before choosing whether model parameters must change.",
    "activityId": "shift-water-transfer",
    "activityPrompt": "A water-quality alert model moves to a river with a replacement turbidity sensor. Labels arrive weekly, and false negatives rise mainly during storm flow. Design the immediate monitors, delayed slice checks, first mitigation, and a controlled test separating sensor shift from model failure.",
    "activityGuidance": "Name pre-label signals, the storm slice, a reversible safety action, and a matched-sample replay.",
    "criteria": [
      {
        "id": "water-immediate-monitors",
        "label": "monitor sensor distribution and missingness before labels"
      },
      {
        "id": "water-delayed-slice",
        "label": "check false negatives on the storm-flow slice"
      },
      {
        "id": "water-mitigation",
        "label": "state a reversible safety mitigation"
      },
      {
        "id": "water-controlled-test",
        "label": "compare old and replacement sensors on matched water"
      }
    ],
    "demonstratedFeedback": "You transferred the layered diagnosis to a delayed-label safety system with an explicit control.",
    "unsupportedFeedback": "Separate signals available today from outcome labels available next week, then compare both sensors on the same water."
  }
]
